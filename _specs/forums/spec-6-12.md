# Forums Wave: Spec 6.12 — Phase 6 Cutover

**Master Plan Reference:** `_forums_master_plan/round3-master-plan.md` → Spec 6.12 (lines 6336–6349). Stub is intentionally minimal — Size S, Risk Low, four-AC checklist (every hero feature manually verified, cutover checklist completed, Phase 6 officially done, Universal Rule 17 axe-core smoke test passes).
**Brief Source:** None. The cutover IS the work — a structured catalog of what shipped this phase, a manual QA walkthrough, an accessibility smoke pass, and a phase-done marker. No design intent to extract from a brief.
**Branch:** `forums-wave-continued` (per W1 — CC never alters git state; Phase 6 specs accumulate on this branch — see commits `a48e059 Spec 6-11`, `2fd25d2 Spec 6-11`, `2a4ccbe Spec 6-11 MD`, `0d519cd Spec 6-10 MD`, `2b189e0 Spec 6-9`).
**Date:** 2026-05-15.
**Spec ID:** `round3-phase06-spec12-phase6-cutover`
**Phase:** 6 (Engagement Features) — the closing spec.
**Size:** S
**Risk:** Low — the cutover ships zero production code. Risk is bounded to "missed a hero-feature regression that real QA would have caught" — mitigated by the manual checklist (Section B of the cutover document).
**Tier:** Standard — verification, not engineering.

---

## Affected Frontend Routes

**N/A — verification spec, no production code.** The hero-feature manual QA walkthrough (Section B of the cutover checklist) and the axe-core accessibility smoke (Section C) cover routes that already shipped in 6.1–6.11b. Routes Eric walks through during cutover:

- `/` (Dashboard — Quick Lift activity recording inheritance, 6.2)
- `/daily` (PrayLengthPicker, PraySession — 6.2b)
- `/prayer-wall` (PrayerWallHero, PrayerCard, InteractionBar, IntercessorTimeline, NightWatchChip, WatchIndicator, CrisisResourcesBanner, PresenceIndicator, VerseFindsYou — 6.1, 6.3, 6.4, 6.5, 6.6, 6.8, 6.11b)
- `/prayer-wall/:id` (PrayerDetail — 6.1, 6.5, 6.6, 6.7, 6.11b)
- `/prayer-wall/answered` (AnsweredWall — 6.6 + 6.6b)
- `/prayer-wall/answered?category={health|family|work|grief|gratitude}` (AnsweredCategoryFilter — 6.6b)
- `/prayer-wall/user/:id` (PrayerWallProfile — 6.1, 6.5, 6.11b)
- `/prayer-wall/dashboard` (PrayerWallDashboard — 6.1, 6.5, 6.11b)
- `/settings` (NotificationsSection sound toggle, GentleExtrasSection Verse-Finds-You + Live Presence toggles, WatchToggle, Night Mode preference — 6.3, 6.4, 6.8, 6.11, 6.11b)

`/verify-with-playwright` is NOT applicable — the Universal-Rule-17 axe-core scan in Section C of the cutover checklist is the verification step for 6.12. No new visual surfaces to verify.

---

## STAY ON BRANCH

Same as the rest of the wave (6.1 through 6.11b). Stay on `forums-wave-continued`. No `git checkout`, `git branch`, `git commit`, `git push`, `git stash`, `git reset`, `git rebase`, `git merge`, `gh pr create`, or any other git-state-mutating command at any phase (spec, plan, execute, review, cutover). Eric handles git manually. Read-only git inspection (`git status`, `git diff`, `git log`, `git show`) is permitted.

The cutover is verification, not engineering. Section A of the checklist runs `./mvnw test` and `pnpm test` as observation gates, not as build-and-fix steps. If anything fails, STOP and surface to Eric. Do not fix during cutover.

---

## Spec-time Recon Summary (verified on disk 2026-05-15)

This section confirms the as-shipped state of every artifact referenced in Section 1 below. Each fact is read from live code, not narrated from memory. The cutover checklist's Section A re-confirms these at cutover time.

| Item | Status | Evidence |
|---|---|---|
| Phase 6 spec files present in `_specs/forums/` | ✅ | 15 files: `spec-6-1.md` through `spec-6-11b.md` (plus `spec-6-6b.md`, `spec-6-6b-deferred-2.md`, `spec-6-10.md` deferred-brief preserved). No `spec-6-12.md` before this run. |
| Phase 6 plan files present in `_plans/forums/` | ✅ | Dated execution plans: `2026-05-12-spec-6-1.md`, `-6-2.md`, `-6-2b.md`, `2026-05-13-spec-6-3.md`, `-6-4.md`, `-6-5.md`, `2026-05-14-spec-6-6.md`, `-6-6b.md`, `-6-6b-deferred-2.md`, `-6-7.md`, `-6-8.md`, `2026-05-15-spec-6-9.md`, `-6-11.md`, `-6-11b.md` |
| `verse-finds-you.json` exists at expected path | ✅ | `backend/src/main/resources/verses/verse-finds-you.json` — 180 `"reference"` entries verified via `grep -c '"reference"'` |
| `CuratedVerseSetValidationTest` ACTIVE | ✅ | `backend/src/test/java/com/worshiproom/verse/CuratedVerseSetValidationTest.java` — 7 `@Test` annotations, 0 `@Disabled` |
| 6.6b-deferred-2 late-detection block | ✅ | `backend/src/main/java/com/worshiproom/post/PostService.java` lines 545–575 — `wantsContentEdit \|\| answeredTextChanged` gate, single detector call, AFTER_COMMIT event one-way ratchet |
| 6.6b-deferred-2 test home | ✅ | `backend/src/test/java/com/worshiproom/post/MarkAndUnmarkAnsweredTest.java` — 12 `@Test` annotations (9 new + 3 pre-existing per the spec) |
| 6.7 EXIF-free comment block | ✅ | `frontend/src/lib/prayer-wall/imageGen.ts` lines 45–58 — preserves the Spec 6.7 EXIF/metadata posture documentation |
| Answered Wall hero subhead `"Gratitude, not comparison."` | ✅ | `frontend/src/constants/answered-wall-copy.ts:25` — `ANSWERED_WALL_SUBHEAD_6_6B = 'Gratitude, not comparison.'`; consumed at `frontend/src/pages/AnsweredWall.tsx:173` |
| AnsweredCategoryFilter deliberate Mental-Health omission | ✅ | `frontend/src/components/prayer-wall/AnsweredCategoryFilter.tsx:12–20` — code comments cite Gate-G-MH-OMISSION and require a future spec to revisit |
| Praising + Celebrate reactions on AnsweredWall only | ✅ | `frontend/src/pages/AnsweredWall.tsx:234,236` wires `showPraising` + `showCelebrate`; grep across `PrayerDetail.tsx`, `PrayerWallProfile.tsx`, `PrayerWallDashboard.tsx` returned zero wires — deferred-4 still parked |
| CommentInput hardcoded placeholder | ✅ | `frontend/src/components/prayer-wall/CommentInput.tsx:124` — `placeholder="Write a comment..."` with no `isAnsweredPost` variant. 6.6b-deferred-1 still parked. |
| PrayerCard author-affordance JWT-id check | ✅ | `frontend/src/components/prayer-wall/PrayerCard.tsx:110` — `authUser !== null && prayer.userId !== null && authUser.id === prayer.userId`. 6.6b-deferred-3 anonymous-author gap still present. |
| PresenceIndicator suppression unit-tested | ✅ | `frontend/src/components/prayer-wall/__tests__/PresenceIndicator.test.tsx:16,24,30,36,42,48,54,60` — multiple `suppressed={true\|false}` tests including "renders nothing when suppressed=true even if count is positive" |
| PresenceIndicator wired on PrayerWall page | ✅ | `frontend/src/pages/PrayerWall.tsx:32` (import), `:141` (Gate-G-CRISIS-SUPPRESSION comment), `:998` (`<PresenceIndicator suppressed={hasCrisisFlag} />`) |
| PresenceIndicator wired on PrayerDetail page | ✅ | `frontend/src/pages/PrayerDetail.tsx:11` (import), `:381` (`<PresenceIndicator suppressed={crisisFlag} />`) — variable name `crisisFlag` instead of `hasCrisisFlag` because PrayerDetail renders one post and reads the boolean directly from that post |
| PresenceIndicator wired on PrayerWallProfile page | ✅ | `frontend/src/pages/PrayerWallProfile.tsx:12` (import), `:93` (Gate-G-CRISIS-SUPPRESSION comment), `:268` (`<PresenceIndicator suppressed={hasCrisisFlag} />`) |
| PresenceIndicator wired on PrayerWallDashboard page | ✅ | `frontend/src/pages/PrayerWallDashboard.tsx:11` (import), `:365` (`<PresenceIndicator suppressed={hasCrisisFlag} />`) — confirms "all 4 routes" claim in Section 1's 6.11b paragraph and Section B's manual QA |
| 6.11 AS-BUILT RECONCILIATION block in master plan | ✅ | `_forums_master_plan/round3-master-plan.md` lines **6186–6199** — block IS present (follows the original stub at 6171–6184, NOT inside it). Pattern matches 6.5's Path B amendment. Documents the audit + coverage scope, the three gate-layer call paths, and the new coverage test at `frontend/src/__tests__/sound-effects-toggle-coverage.test.tsx`. |
| Redis dev port remap to 6380 | ✅ | `docker-compose.yml:29–30` — `ports: ["6380:6379"]`; comment cites `backend/docs/redis-conventions.md § "Dev port"` |
| Sound Effects toggle in NotificationsSection (6.11 audit confirmation) | ✅ | `frontend/src/components/settings/NotificationsSection.tsx:20` (key declaration), `:151` (id="notif-sound-effects"), `:154` (label="Sound Effects") — was present pre-6.11 |
| GentleExtras dual toggle inline duplication | ✅ | `frontend/src/components/settings/GentleExtrasSection.tsx:55` (Verse Finds You), `:110` (Count me as present when I'm reading), `:119` ("Same shape as the Verse Finds You toggle above — kept identical") |
| Upload test fixtures @Disabled (spec-4-6b followup, still parked) | ✅ | `UploadServiceTest.java` 12 `@Disabled` + `UploadControllerIntegrationTest.java` 10 `@Disabled` = 22 — matches the tracker entry |
| PrayCeremony runaway-timer test still present | ✅ | `frontend/src/components/prayer-wall/__tests__/PrayCeremony.test.tsx:218` — "rapid toggle cancels pending toasts" — recursive setTimeout signature unchanged |

**Recon override:** none. The cutover documents observed reality; it does not modify it.

---

## Section 1 — Phase 6 As-Shipped Catalog

Eleven specs shipped, one deferred to Phase 8. Each paragraph below states what shipped, the routes/surfaces it touches, the test count from the live test suite, the gates it honored, deferred items it spawned, and as-built deviations from the brief. **File paths and line numbers cite live code as of 2026-05-15.**

### 6.1 Prayer Receipt (L, Medium, ✅)

Shipped a per-author confirmation moment that surfaces after a prayer post receives `praying_count > 0`: a frosted modal showing how many people prayed plus a daily-rotating scripture accompaniment from a 60-WEB-verse Eric-curated set, with a three-display-variant privacy model (author-only count, share-as-PNG that strips identifying metadata, dashboard mini-receipt). Touches `/prayer-wall/:postId` (author view via `<PrayerReceipt>` mounted above `<InteractionBar>`), `/prayer-wall/dashboard` (mini-receipt summary via `<PrayerReceiptMini>`), `/settings` (`prayerReceiptsVisible` toggle in `prayerWall` namespace, default `true`). Live test count: 22 backend (`PrayerReceiptIntegrationTest` 14 + `PrayerReceiptReadRateLimitServiceTest` 4 + `PrayerReceiptShareRateLimitServiceTest` 4) + 41 frontend (`PrayerReceipt.test.tsx` 13 + `PrayerReceiptModal.test.tsx` 16 + `PrayerReceiptMini.test.tsx` 6 + `PrayerReceiptImage.test.tsx` 6) = 63 new tests. Gates honored: Gate-29 (Eric's 60-verse curation landed at `frontend/src/constants/prayer-receipt-verses.ts` BEFORE execute), Gate-30 (`html2canvas` ≤30KB gzipped delta enforced), the Anti-Pressure Design Decisions block on the brief's "empty receipt" copy (rendered as a daily verse with no "0 people prayed" pressure). Deferred items: none specific to 6.1; the share-as-image infrastructure became 6.7's reuse target. As-built deviation: none — 6.1 was the first spec authored from the corrected post-drift live master plan and shipped clean against its brief.

### 6.2 Quick Lift (M, Low, ✅)

Shipped a one-tap silent-prayer gesture: tapping Quick Lift on any prayer card opens `<QuickLiftOverlay>` (frontend/src/components/prayer-wall/QuickLiftOverlay.tsx) for a server-authoritative 30-second hold; on completion the user earns 20 faith points (`ACTIVITY_POINTS.quickLift = 20`) and progresses toward the new "Faithful Watcher" badge (10 Quick Lifts → unlock). Touches `/prayer-wall`, `/prayer-wall/:id`, `/prayer-wall/dashboard`, `/prayer-wall/user/:id` (Quick Lift button via `<InteractionBar>` on every PrayerCard), and indirectly `/` (Dashboard activity checklist inherits the `quickLift` `ActivityType`). Live test count: 5 frontend (`QuickLiftOverlay.test.tsx`) + the activity engine + badge integration tests in the backend. Gates honored: brief MPD-4 (no declarative badge catalog refactor — slotted into existing `BadgeService` switch pattern per R-OVR-1 / R-OVR-2), server-authoritative timer (not client-trustable). As-built deviation: brief's R-OVR-1 correctly identified that `PointValues.java` and `BadgeCatalog.java` already shipped from Phase 2 — Quick Lift integrated via the established pattern rather than the brief's hypothetical declarative-criteria architecture. Deferred items: none.

### 6.2b Prayer Length Options (M, Low, ✅)

Shipped a frontend-only 1/5/10-minute guided prayer-session picker on the Daily Hub Pray tab plus a transient full-screen `<PraySession>` view (`frontend/src/pages/daily/PraySession.tsx`) that drives the session lifecycle. Touches `/daily?tab=pray` (existing tab gains the new `<PrayLengthPicker>`), `/daily?tab=pray&length={1|5|10}` (deep-link variants — picker bypassed, session mounts immediately), `/daily?tab=pray&length=invalid` (graceful — picker visible, no error toast per Gate-G-DEEP-LINK-GRACEFUL). Live test count: 11 frontend (`PrayLengthPicker.test.tsx` 5 + `PraySession.test.tsx` 6). Gates honored: Gate-G-A11Y, Gate-G-ANTI-PRESSURE (no shaming language on early exit), Gate-G-DEEP-LINK-GRACEFUL. As-built deviation: brief MPD-4 + D-FetchPattern assumed `useFaithPoints.recordActivity` already forwarded arbitrary metadata to the backend; R-OVR-1 caught that the hook signature lacked the metadata parameter — plan-recon Option A widened `RecordActivityOptions` to include `metadata?: Record<string, unknown>`. No design change; the path from frontend hook to backend POST was the only thing that shifted. Reuses the existing `'pray'` `ActivityType` (R-OVR-3), not a new type. Deferred items: none.

### 6.3 Night Mode (L, Medium, ✅)

Shipped a frontend-only Prayer-Wall-scoped night dimming that activates between 9pm and 6am local time when the user's `wr_settings.prayerWall.nightMode` preference is `'auto'` (default) or always when `'on'`. Touches `/prayer-wall`, `/prayer-wall/:id`, `/prayer-wall/dashboard`, `/prayer-wall/user/:id` (root container receives `data-night-mode="on|off"`; hero subtitle, compose-FAB tooltip, composer placeholder, empty-state copy swap to night variants; `<NightWatchChip>` mounts in page header), `/settings` (3-radio Night Mode preference UI under "Sensitive features"). Live test count: 9 (`useNightMode.test.ts`). Gates honored: Gate-G-NO-FOUC (inline `index.html` no-FOUC script reads `wr_night_mode_hint` + `wr_settings.prayerWall.nightMode` + browser hour at boot per Plan-Time Divergence #3), Gate-G-COPY-PAIRS (every Prayer Wall copy string has a paired night variant), Gate-G-A11Y, Gate-G-SCOPE-PRAYER-WALL-ONLY (cross-route Playwright test asserts `/daily` etc. strip the attribute), Gate-G-LIVE-TRANSITION (boundary-crossing without page reload). As-built deviation: bootstrap hint is a secondary signal — the primary signal is `wr_settings.prayerWall.nightMode` + browser hour recomputed inline (Plan-Time Divergence #3 promoted the inline computation over the localStorage hint). Deferred items: none.

### 6.4 3am Watch v1 (L, HIGH, ✅)

Shipped the Watch framework: an opt-in (default `'off'`, Gate-G-FAIL-CLOSED-OPT-IN) toggle under Settings → "Sensitive features" + `<WatchOptInConfirmModal>` confirmation gate before enabling; when active during late-night hours, the Prayer Wall surfaces `<CrisisResourcesBanner>` at the top of the feed, swaps composer placeholder to a Watch variant, suppresses QOTD, and renders `<WatchIndicator>` in the page header. **v1 deliberately ships framework only without feed slicing per MPD-1** — feed slicing (friends + Mental Health prioritization, crisis-flag exclusion) is 6.4b's territory. Touches `/prayer-wall`, `/prayer-wall/:id`, `/prayer-wall/dashboard`, `/prayer-wall/user/:id` (Watch UI elements mount when active), `/settings` (new Sensitive-features section with the WatchToggle and confirmation modal). Live test count: 26 frontend (`WatchToggle.test.tsx` 11 + `WatchOptInConfirmModal.test.tsx` 6 + `WatchIndicator.test.tsx` 4 + `CrisisResourcesBanner.test.tsx` 5). Gates honored — and these are load-bearing because 6.4 is the highest-risk spec in Phase 6: Gate-G-CRISIS-RESOURCES-ALWAYS-VISIBLE (banner is the load-bearing safety surface, not engagement bait), Gate-G-NO-AI-AUTO-REPLY (`CRITICAL_NO_AI_AUTO_REPLY.md` HARD ENFORCEMENT GATE established by this spec — no LLM ever invoked on crisis-adjacent content), Gate-G-NO-ENGAGEMENT-METRICS (no count of Watch toggles surfaced anywhere), Gate-G-DETERMINISTIC-FEED (when slicing arrives in 6.4b, ordering must be deterministic), Gate-G-DEGRADED-MODE (classifier-down path documented; v1 surface stays user-functional even when 10.5/10.6 are unbuilt), Gate-G-FAIL-CLOSED-OPT-IN (toggle defaults `'off'` for new AND existing users — never auto-enabled), Gate-G-TRUST-LEVEL-SERVER-ENFORCED (when 10.5 wires Trust Levels, server enforcement is mandatory — v1 has no client-side trust gate). The opt-in confirmation modal is the UX requirement that prevents accidental enabling. Crisis interaction surface: tapping a CrisisResourcesBanner item routes the user to the canonical `CRISIS_RESOURCES` (988, Crisis Text Line, SAMHSA — imported from `frontend/src/constants/crisis-resources.ts`, not inlined). As-built deviation: brief R2 and R4 ("Friend system DOES NOT EXIST", "Crisis classifier DOES NOT EXIST") were WRONG on disk — R-OVR-S1 and R-OVR-S2 corrected the recon. The friend Java layer shipped in Phase 2.5 and the crisis-detection scaffolding (`PostCrisisDetector`, `CrisisAlertService`, `CrisisDetectedEvent` AFTER_COMMIT pipeline) shipped in Phase 3. **v1 scope unchanged** — MPD-3 (classifier as no-op stub) and MPD-4 (friends omitted) stand, with their rationale reframed as "scope discipline" rather than "infrastructure absence." 6.4b's lift is smaller than the brief assumed. Deferred items: 6.4b (feed slicing with friend lookups + crisis-flagged exclusion + Mental Health prioritization) is documented as the next-spec target.

### 6.5 Intercessor Timeline (L, Medium-High, ✅, Path B as-built)

Shipped a per-card intercessor summary line below post content showing who prayed and when in soft type — author-only visible (or muted to "N intercessors" for non-authors). **This spec is the catastrophic-drift Path B resolution.** The brief described a public per-card summary; the live master plan stub had been rewritten to describe a *private author-only aggregate page* (calendar heatmap, by-post/by-person views, Year-of-Prayer image) — but 6.5 had already executed and merged as the per-card summary before the drift was caught. Eric chose Path B: shipped code is source of truth. The 6.5 master plan stub was amended 2026-05-14 with an AS-BUILT RECONCILIATION block; the original aggregate-page vision is preserved there as **DEFERRED** and may become a future spec. Touches `/prayer-wall`, `/prayer-wall/:id`, `/prayer-wall/user/:id`, `/prayer-wall/dashboard` (the `<IntercessorTimeline>` mounts inside `<PrayerCard>` on every route that displays cards). Live test count: 35 backend (`IntercessorServiceTest` 7 + `IntercessorIntegrationTest` 18 + `IntercessorReadRateLimitServiceTest` 3 + `IntercessorEntryTest` 7) + 11 frontend (`IntercessorTimeline.test.tsx`) = 46 new tests. Gates honored: friend-only display attribution per the brief's Path B design, anonymous-author handling, performance budget on the hot feed path (mid-query inclusion via `intercessorSummary` field; index-backed read), block-cascading cache invalidation (reuses 6.6b's later cache pattern at design time). As-built deviation: this IS the deviation — Path B preservation. No code rewrite. Deferred items: **6.5-deferred aggregate page** (the master plan's calendar heatmap + by-day/by-post/by-person views + Year-of-Prayer image — preserved at the master plan stub's AS-BUILT RECONCILIATION block as a possible future spec).

### 6.6 Answered Wall (L, Medium, ✅, drift-remediated by 6.6b)

Shipped `/prayer-wall/answered` (a new public page) and the `<AnsweredCard>` variant (`AnsweredCard.tsx`) that surfaces wherever an answered post renders. Includes the `'praising'` reaction (DB ALTER changeset 2026-05-14-001 widened the `post_reactions.reaction_type` CHECK constraint), a partial-index-friendly feed query (`WHERE is_answered = TRUE` + `ORDER BY answered_at DESC` per the partial index at changeset 014:180), and a chronological hero feed. Touches `/prayer-wall/answered` (NEW page), `/prayer-wall` (added "Answered" nav entry; main feed continues to default to non-answered behavior), `/prayer-wall/:id` (answered post detail uses AnsweredCard variant), `/prayer-wall/user/:id` (user's answered posts), `/prayer-wall/dashboard` (user's own answered prayers). Live test count: 10 backend (`AnsweredFeedTest` 5 + `AnsweredFeedCacheTest` 5). **6.6 shipped with MATERIAL DRIFT** — the brief was authored from the stale pristine-baseline copy of the master plan and dropped four scope areas the live stub mandated. Those four areas closed in 6.6b (see below). The execute-time decision to ship `'praising'`-only and exclude `'celebrate'` was made in good faith against the drifted brief; 6.6b reinstates `'celebrate'`. Deferred items: see 6.6b (and the four 6.6b-deferred items below).

### 6.6b Answered Wall Drift Remediation (L, Medium-High, ✅)

Shipped the four-gap-area remediation: the hero subhead `"Gratitude, not comparison."` (canonical at `frontend/src/constants/answered-wall-copy.ts:25`, consumed at `frontend/src/pages/AnsweredWall.tsx:173`); the `'celebrate'` reaction wired through the full stack (second permanent-MD5 CHECK-constraint ALTER on `post_reactions`, frontend `<CelebrateReaction>` component, Light-a-Candle→Celebrate swap on answered posts); the `<AnsweredCategoryFilter>` category filter chips with the deliberate Mental-Health omission (5 chips: Health, Family, Work, Grief, Gratitude — plus the All chip = 6 total; Mental Health intentionally NOT a chip per Gate-G-MH-OMISSION, with the rationale embedded as a code comment at `AnsweredCategoryFilter.tsx:12–20`); and the answered-text edit + un-mark-as-answered flow via the in-place `updatePost` Branch 1/2/3 mechanism (no new method, no new handler). Touches `/prayer-wall/answered` (primary surface), `/prayer-wall/answered?category={health|family|work|grief|gratitude}` (chip-driven filter URLs), `/prayer-wall/:id` + `/prayer-wall/user/:id` + `/prayer-wall/dashboard` (Celebrate reaction surfaces on answered posts where InteractionBar receives `showCelebrate`). Live test count: 29 frontend (`AnsweredCategoryFilter.test.tsx` 7 + `CelebrateReaction.test.tsx` 5 + `AnsweredCard.test.tsx` 6 + `AnsweredWall.test.tsx` 6 + `MarkAsAnsweredForm.test.tsx` 5). Gates honored: Gate-G-MH-OMISSION (Mental Health chip prohibited without a dedicated spec that revisits the design), Gate-G-EXTEND-NOT-DUPLICATE (surgical edits to `AnsweredWall.tsx` and `AnsweredCard.tsx`, no parallel implementations), Gate-G-CELEBRATE-NO-INTERCESSION (new `toggleCelebrate_addPath_doesNotFireIntercessionActivity` regression test honors the convention established by `togglePraying_addPath_firesIntercessionActivity` / `togglePraising_addPath_doesNotFireIntercessionActivity` / `toggleCandle_addPath_doesNotFireIntercessionActivity` — Activity suffix verbatim per Gate-G-6.6-REGRESSION-SAFE). **Spawned four deferred items (deferred-1, -3, -4 still parked; deferred-2 shipped):**

  - **6.6b-deferred-1 (CommentInput placeholder)** — DEFERRED. Verified 2026-05-15: `CommentInput.tsx:124` still carries hardcoded `placeholder="Write a comment..."` with no `isAnsweredPost`-aware variant. A future spec adds an `isAnsweredPost?: boolean` prop, plumbs it through `CommentsSection` + `PrayerCard`, and adds the softer-commenter-guidance + `"Say a word of celebration…"` placeholder.
  - **6.6b-deferred-2 (answered_text crisis-scan)** — SHIPPED (see below).
  - **6.6b-deferred-3 (anonymous-author affordances)** — DEFERRED. Verified 2026-05-15: `PrayerCard.tsx:110` uses `authUser !== null && prayer.userId !== null && authUser.id === prayer.userId` for author-affordance gating. Anonymous authors (`userId: null` to the frontend) cannot use the inline "Share an update" / "Un-mark as answered" affordances on `/prayer-wall/answered`; they fall back to `/prayer-wall/dashboard` which DOES surface the affordances via JWT-only ownership. A future spec could pass `isViewerAuthor: boolean` on PostDto. Documented limitation, NOT a regression.
  - **6.6b-deferred-4 (cross-route Celebrate/Praising)** — DEFERRED. Verified 2026-05-15: grep across `PrayerDetail.tsx`, `PrayerWallProfile.tsx`, `PrayerWallDashboard.tsx` for `showPraising` / `showCelebrate` returned zero matches. The reactions surface on `AnsweredWall.tsx` only (lines 234, 236). A future spec wires both reactions on the other three routes atomically.

### 6.6b-deferred-2 Answered-Text Crisis-Scan Coverage (S–M, HIGH, ✅)

Shipped a single-late-detector architecture at `PostService.java` lines 545–575 that covers all three answered-text write branches via post-mutation snapshot diff, gated by `wantsContentEdit || answeredTextChanged`. The change concatenates `answered_text` into `detectionInput` alongside `content` (matching the Spec 4.6b `imageAltText` precedent at lines 297–300), invokes `PostCrisisDetector.detectsCrisis(detectionInput)` exactly once per commit, and fires the AFTER_COMMIT `CrisisDetectedEvent` at most once. The one-way ratchet from R-FINDING-B is preserved verbatim — detection true → set the flag if not already set + fire event; detection false → no change, **NEVER clear an existing flag** (crisis flags survive author edits that remove the trigger words, by design — moderator review state is load-bearing). Touches: backend-only spec; no UI changes. Live test count: 12 backend (`MarkAndUnmarkAnsweredTest.java` — 9 new "answered_text crisis content" cases + 3 pre-existing answered-flow tests). Gates honored: Gate-G-ATOMIC (every place `answered_text` is written, scan it), Gate-G-NO-W29-INCONSISTENCY (create/update/edit all consistent — `createPost` cannot write `answered_text` today so "create scans" and "create doesn't scan" are operationally equivalent; the late-detector covers the actual write paths). As-built deviation: narrow framing chosen over future-proof framing per R-FINDING-A — `createPost` is verified non-write today; a future spec that adds `answeredText` to `CreatePostRequest` MUST add scanning at the same time (next-spec responsibility, not 6.6b-deferred-2's).

### 6.7 Shareable Testimony Cards (L, Medium, ✅, +EXIF one-shot)

Shipped a "Share as image" affordance on `post_type === 'testimony'` posts that captures `<TestimonyCardImage>` to a PNG via the existing 6.1 share-as-image pipeline (`html2canvas` + `imageGen.ts`), with anonymous-attribution routing through the same display-name resolver as `PrayerCard` (Gate-G-ANON-ATTRIBUTION) and a pre-share confirmation modal (`wr_settings.prayerWall.dismissedShareWarning` one-time dismissible flag). Touches `/prayer-wall`, `/prayer-wall/:id` (PrayerDetail surfaces the share modal), `/prayer-wall/user/:id`, `/prayer-wall/dashboard`, `/prayer-wall/answered` (testimony posts inside answered surface the affordance — consistent with Gate-G-TESTIMONY-ONLY). Live test count: 8 frontend (`TestimonyCardImage.test.tsx`). Gates honored: Gate-G-TESTIMONY-ONLY (affordance renders only on testimony post-type), Gate-G-ANON-ATTRIBUTION (anonymous testimonies render "Anonymous" as the captured display name + omit the avatar URL; only stylized initials in a circle). As-built deviation: **the EXIF one-shot follow-up** — 6.7 shipped correctly on 3 of 4 image-privacy ACs (2 of those exceed spec), and the fourth (EXIF/metadata stripping) was incidentally satisfied by canvas-to-PNG (EXIF-free by construction) but neither documented in code nor asserted by a test. The one-shot follow-up landed the EXIF-free comment block at `frontend/src/lib/prayer-wall/imageGen.ts:45–58` (still present and verified 2026-05-15) plus a companion test guard at `__tests__/imageGen.test.ts` asserting `canvas.toBlob` MIME type stays `'image/png'`. The guard is the privacy posture's regression boundary: if a future spec changes the export to JPEG / WebP-with-EXIF / a library that can inject metadata, the test fails and forces explicit stripping. Deferred items: none.

### 6.8 Verse-Finds-You (L, HIGH, ✅, machinery + 180-passage curation)

Shipped the machinery + the curated set: 180 curated WEB passages in `backend/src/main/resources/verses/verse-finds-you.json` (verified 2026-05-15 via `grep -c '"reference"'` = 180), tagged with emotional/contextual tags and `excluded_contexts` for the "NOT hope" mappings (Mental Health and Grief categories deliberately exclude `hope`-tagged verses per the brief's pastoral discernment); four surfacing triggers (post-compose, comment-of-encouragement, reading-time, optional follow-up); 24-hour per-user cooldown; 48-hour crisis-flag suppression gate per Universal Rule 13; settings toggle defaulting OFF for new AND existing users (Gate-G-DEFAULT-OFF) via `wr_settings.verseFindsYou.enabled = false` + `deepMerge` in `settings-storage.ts`; 3-in-a-row dismissal off-ramp prompt (`wr_verse_dismissals`); category-to-tag mapping with the "NOT hope" rules embedded as code comments + tests. The crisis-flag gate is wired through `PrePhase10CrisisFlagGate` and **the feature is functionally dormant until Phase 10 ships** — pre-Phase-10 the crisis-flag read returns "no flag readable" for non-authoring users, which falls through the silent-failure path with `reason='no_match'` (or the shipped equivalent — verify from `VerseSelectionEngine.java` at cutover time). `CuratedVerseSetValidationTest` is ACTIVE (not `@Disabled`; 7 `@Test` annotations all passing — verified 2026-05-15). Touches `/prayer-wall` (post-compose trigger after InlineComposer submit; reading-time trigger after >5min active engagement), `/prayer-wall/:id` (comment-of-encouragement trigger on Mental Health / Grief / Health posts, surfaced in the commenter's view only), `/prayer-wall/dashboard` (reading-time trigger scope per shipped implementation), `/settings` (Gentle extras → "Verse Finds You" toggle). Live test count: 29 backend (`CuratedVerseSetValidationTest` 7 + `VerseFindsYouControllerTest` 8 + `VerseFindsYouServiceTest` 6 + `VerseSelectionEngineTest` 8) + 29 frontend (`useVerseFindsYou.test.tsx` 9 + `VerseFindsYou.test.tsx` 11 + `verse-dismissals-storage.test.ts` 9) = 58 new tests. Gates honored: Gate-G-CURATION-PREREQ (Eric's 180-passage deliverable landed BEFORE execute — gate resolved 2026-05-14), Gate-G-DEFAULT-OFF (toggle defaults OFF for ALL users — new + existing — via `deepMerge`), Gate-G-DORMANT-UNTIL-PHASE-10 (silent-failure path on pre-Phase-10 crisis-flag read), Gate-G-SILENT-FAILURE (pipeline errors return null, never surface a wrong verse), Gate-G-FAIL-CLOSED-COOLDOWN (Redis unreachable → cooldown check defaults to DENY, NOT allow). As-built deviation: brief proposed Liquibase changeset `2026-04-22-003-create-verse-surfacing-log.xml` — plan-recon picked the correct contemporaneous slot per R8 ("likely `2026-05-14-005-create-verse-surfacing-log.xml` or later"). Deferred items: none. Live verification of the crisis-flag suppression branch is deferred until Phase 10 ships (the gate is wired and unit-tested; e2e verification requires real crisis-flagged posts that pre-Phase-10 dev cannot produce).

### 6.9 Prayer Wall Composer Drafts (M, Low, ✅)

Shipped `useComposerDraft` (`frontend/src/hooks/useComposerDraft.ts`) plus `wr_composer_drafts` localStorage key: one draft per `DraftKey = PostType | 'qotd'`, 5-second debounce auto-save while content is dirty (W1 dirty-flag pattern), 7-day TTL enforced on read (an expired record is silently removed by `getDraft` and never surfaces a restore prompt), one-draft-per-post-type, restore prompt surfaces on composer reopen via `<RestoreDraftPrompt>` (`frontend/src/components/prayer-wall/RestoreDraftPrompt.tsx`) with the shipped copy `"You have a saved draft from {timeAgo}. Restore it?"` plus `"Restore draft"` / `"Start fresh"` buttons. Drafts clear on successful post submit AND on logout (`clearAllComposerDrafts()` called inside `AuthContext.logout()` enforces per-device account isolation). The synthetic `'qotd'` key keeps QOTD response drafts independent from `'discussion'` post drafts despite both submitting as `postType: 'discussion'` on the wire. Touches `/prayer-wall` (InlineComposer + QotdComposer) per Plan-Recon R1; `CommentInput.tsx` deliberately NOT wired (preserves 6.6b-deferred-1's placeholder territory). Live test count: 17 frontend (`useComposerDraft.test.ts` 10 + `RestoreDraftPrompt.test.tsx` 7). Gates honored: Gate-1 (5s debounce), Gate-2 (7-day TTL on read), Gate-3 (logout clearance), Gate-4 (one-draft-per-key), Gate-5 (keyboard-reachable + focus-managed restore prompt). As-built deviation: plan-recon decided the restore prompt is a non-modal inline card with a focus ring on the primary button (NOT a full modal requiring `useFocusTrap()`) — appropriate given the prompt's "interrupt-the-composer-reopen" position. Deferred items: none.

### 6.10 Prayer Wall Search by Author (S, Low, ⏸ DEFERRED to Phase 8)

**DEFERRED to after Phase 8 unified-profile.** Plan-recon 2026-05-15 R1+R3 gating returned NO: the route `/u/:username` and the username DTO field don't exist yet (Phase 8 owns them). Brief preserved verbatim at `_plans/forums/spec-6-10-brief.md` for revival when Phase 8 ships. Spec tracker line 195 carries the ‼️ marker with a `** REVISIT 6.10 after Phase 8 is done**` note (line 229). No code shipped under 6.10. No tests shipped. Cutover treats 6.10 as out-of-scope for Phase 6 — Phase 6 closes with this deferral documented, not closed silently.

### 6.11 Sound Effects Settings Polish (S, Low, ✅, audit + coverage)

**Collapsed at plan-recon time to audit + coverage spec.** The brief was written against the stub language "the existing sound effects system gets exposed in settings" — but the toggle already shipped pre-wave at `frontend/src/components/settings/NotificationsSection.tsx:20` (key declaration `SOUND_EFFECTS_KEY = 'wr_sound_effects_enabled'`), `:151` (id="notif-sound-effects"), `:154` (label="Sound Effects") inside the Notifications → Sound subsection. Eric's 2026-05-15 plan-recon decision selected Path A (Audit + Coverage Test), explicitly chose NOT to move the toggle to "Gentle extras" (R3 — leave it where it is; users may already know the location), NOT to refactor `EveningReflection.tsx:107`'s architecturally-inconsistent direct localStorage read (R2 — document as future cleanup, do not absorb per Gate-G-NO-SCOPE-CREEP), and dropped Gate-G-DO-NOT-MASK-PRAYCEREMONY-BUG / W4 / T6 / Acceptance Criterion F because the `PrayCeremony.tsx` production component does NOT exist — only the test file at `PrayCeremony.test.tsx` exists, and the failure is test debt rather than a production concern (R6). 6.11's shipped work: (1) audit confirmed all three gate-layer call paths respect the existing toggle (the central `useSoundEffects` hook with 17+ production consumers, the `quickLiftSound.playWindChime()` path, the `EveningReflection.tsx:107` direct-localStorage read — all read the same key, so the toggle gates everywhere); (2) coverage test extends `NotificationsSection.test.tsx` and `useSoundEffects.test.ts` with the load-bearing "all paths respect toggle" assertion that uses real localStorage manipulation rather than `vi.mock(useSoundEffects)` (which would miss the `quickLiftSound` and `EveningReflection` paths and produce a false-green); (3) AS-BUILT amendment on the master plan stub. Touches `/settings` (verification surface; toggle did not move). Live test count: coverage additions to `NotificationsSection.test.tsx` and `useSoundEffects.test.ts`, plus a cross-cutting load-bearing coverage test at `frontend/src/__tests__/sound-effects-toggle-coverage.test.tsx`. **AS-BUILT amendment IS landed** at `_forums_master_plan/round3-master-plan.md` lines **6186–6199** (the block FOLLOWS the original stub at 6171–6184; recon table row above verifies presence). Block documents the audit + coverage scope, the three gate-layer call paths (useSoundEffects hook with 17+ consumers / quickLiftSound.playWindChime / EveningReflection.tsx:107 direct read), and the new cross-cutting coverage test. Original "build a toggle" framing preserved for history. Deferred items: `EveningReflection.tsx` direct-localStorage architectural cleanup; PrayCeremony test-debt cleanup spec.

### 6.11b Live Presence Component (M, Medium, ✅, Path B wide commit)

Shipped the presence indicator core plus several adjacent fixes absorbed into the same commit per Eric's Path B scope decision. Core: a polling endpoint, a Redis presence-tracking interceptor (`PresenceTrackingInterceptor` issues the 90-day `wr_presence_session` HttpOnly + Secure + SameSite=Lax anonymous cookie scoped to `/api/v1`), a frontend polling hook (`usePresence` with crisis-suppression gate), `<PresenceIndicator>` (top-right of feed header, small font, muted, non-interactive, hidden at N=0 per the anti-pressure rule, suppressed on pages with crisis-flagged posts via Gate-G-CRISIS-SUPPRESSION), a "Count me as present when I'm reading" toggle in Settings → Gentle extras (the second toggle in that section — see "GentleExtrasSection toggle duplication" below), opt-out persistence (localStorage `wr_settings.presence.optedOut` mirrored to backend `presenceOptedOut` field via fire-and-forget PATCH /users/me), anonymous-visitor session tracking, accessibility-aware screen-reader announcements. Path B adjacent fixes absorbed in the same commit: (a) **mobile responsive-stack fix** on 4 feed-header routes (PrayerWall feed, Profile, Dashboard, Detail) — indicator + breadcrumb both visible without clipping at 375px and 768px; (b) **breadcrumb-behind-navbar fix** on 3 routes — `main` className changed from `py-6 sm:py-8` / `py-6` to `pt-28 pb-6 sm:pb-8` / `pt-28 pb-6` (pt-28 = 112px clears 101px navbar with 11px buffer) across 7 `main` elements (PrayerWallDashboard 1, PrayerWallProfile 2, PrayerDetail 4 covering loading/error/notFound/success states); PrayerWall not changed because `<PrayerWallHero>` above main provides natural clearance; (c) **WCAG 2.5.5 touch-target fix** on both Gentle extras toggles — 44×44 click target via `min-h-[44px] min-w-[44px]` outer button + 24×44 visual switch as inner span, kept identical on both so future consolidation can extract them together without drift; (d) **Redis dev port remap** from 6379 → 6380 (verified 2026-05-15 at `docker-compose.yml:29–30` with the sibling-coexistence comment) plus `application.properties` fallback `REDIS_PORT=6380`, `.env.example`, `application-test.properties`, `backend/docs/redis-conventions.md § "Dev port"` symmetry; (e) runbook doc additions; (f) screenshot hygiene; (g) tracker lines. Touches `/prayer-wall`, `/prayer-wall/:id`, `/prayer-wall/user/:id`, `/prayer-wall/dashboard` (PresenceIndicator wired at `PrayerWall.tsx:998` with `suppressed={hasCrisisFlag}` — and on the other three routes), `/settings` (GentleExtras "Count me as present when I'm reading" toggle), `/prayer-wall/answered` (presence rule applies for completeness even though answered posts shouldn't be crisis-flagged). Live test count: 35 backend (`PresenceServiceTest` 12 + `PresenceCookieServiceTest` 8 + `PresenceInterceptorIntegrationTest` 5 + `PresenceControllerIntegrationTest` 4 + `PresenceRateLimitServiceTest` 4 + `PresenceCleanupJobTest` 2) + 24 frontend (`PresenceIndicator.test.tsx` 10 + `usePresence.test.tsx` 7 + `GentleExtrasSection.test.tsx` 7) = 59 new tests. Gates honored: Gate-G-CRISIS-SUPPRESSION (unit-tested at `PresenceIndicator.test.tsx` lines 16–60 via multiple `suppressed={true|false}` cases including "renders nothing when suppressed=true even if count is positive" — **live e2e verification deferred until Phase 10 ships** because `VITE_USE_BACKEND_PRAYER_WALL=false` in dev means mock-mode bypasses `listPosts` → `hasCrisisFlag` stays at its initial `false` → suppression branch never exercised end-to-end; tracker line 175 documents the deferral), Hidden-at-N=0 rule, no animations, no "be the first," no friends enrichment, no comparison metrics. As-built deviation: this IS the deviation — Path B wide commit explicitly absorbed (a)–(g) above. **GentleExtrasSection toggle duplication is tracked as a future consolidation spec recommendation** — `frontend/src/components/settings/GentleExtrasSection.tsx:55` (Verse Finds You) and `:110` (Count me as present when I'm reading) are near-identical inline blocks (`:119` notes "Same shape as the Verse Finds You toggle above — kept identical"). A future spec should extract `<Toggle id checked srOnLabels onChange variant>` to `frontend/src/components/ui/Toggle.tsx` BEFORE any third opt-in toggle is added to this section. Deferred items: live e2e verification of crisis suppression (Phase 10 + Phase 3 cutover prereqs); GentleExtras consolidation (recommended before third toggle); the broader Phase 3 success-path `X-RateLimit-*` header standardization across all dedicated rate-limit services (tracker line 179 — sweep all PostsRateLimitConfig / CommentsRateLimitConfig / ReactionsRateLimitConfig / BookmarksRateLimitConfig / ReportsRateLimitConfig / ChangePasswordRateLimitConfig / PostsIdempotencyService / VerseFindsYouRateLimitService / PresenceAuthRateLimitService / PresenceAnonRateLimitService in one atomic pass via a `RateLimitProbe` value object + a helper).

---

## Section 2 — Deferred Items Catalog

Verification status as of 2026-05-15. Each item carries a confirmed disposition (✅ confirmed-deferred / ⚠️ surface-to-Eric).

### Spec-level deferrals (carry next-spec ownership)

- **Future feed-slicing work referenced by 6.4's MPD-1** — ✅ confirmed-deferred. The 6.4 brief's MPD-1 (and the cutover spec's earlier draft) referred to this as "6.4b" but `grep -nE "6\.4b|round3-phase06-spec04b" _forums_master_plan/round3-master-plan.md` returned ZERO matches — **NO master plan stub exists**. Watch v2 (friend-prioritized + Mental-Health-prioritized feed slicing during Watch hours, crisis-flagged exclusion, Trust Level enforcement) will require Phase 6 follow-up spec authoring before execution. Cross-references: 10.5 (three-tier escalation pipeline + Trust Levels), 10.6 (server enforcement of automated flagging). v1's MPD-3 and MPD-4 stand: classifier as no-op stub, friends omitted — premise reframed as scope discipline rather than infrastructure absence (friend Java layer + crisis-detection scaffolding already exist per R-OVR-S1 / R-OVR-S2 from the 6.4 spec).
- **6.5-deferred aggregate page (Year-of-Prayer image, calendar heatmap, by-day/by-post/by-person views)** — ✅ confirmed-deferred. Path B preserved at the master plan 6.5 stub's AS-BUILT RECONCILIATION block. May become a future spec.
- **6.6b-deferred-1 (CommentInput placeholder)** — ✅ confirmed-deferred. Verified 2026-05-15: `frontend/src/components/prayer-wall/CommentInput.tsx:124` still carries hardcoded `placeholder="Write a comment..."` with no `isAnsweredPost`-aware variant. Future spec: add `isAnsweredPost?: boolean` prop, plumb through `CommentsSection` + `PrayerCard`, add `"Say a word of celebration…"` placeholder + softer commenter guidance to `answered-wall-copy.ts`, ship one vitest assertion.
- **6.6b-deferred-3 (anonymous-author affordances on `/prayer-wall/answered`)** — ✅ confirmed-deferred. Verified 2026-05-15: `frontend/src/components/prayer-wall/PrayerCard.tsx:110` author-affordance gate uses `authUser !== null && prayer.userId !== null && authUser.id === prayer.userId`. Anonymous posts surface `userId: null` to the frontend (backend redacts for anonymity), so anonymous authors who mark their prayer answered cannot use inline "Share an update" / "Un-mark as answered" affordances on the Answered Wall — they fall back to `/prayer-wall/dashboard`. Documented limitation, NOT a regression. Future spec: pass opaque `isViewerAuthor: boolean` on PostDto (computed server-side from JWT vs `users.id`).
- **6.6b-deferred-4 (cross-route Celebrate + Praising on PrayerDetail, PrayerWallProfile, PrayerWallDashboard)** — ✅ confirmed-deferred. Verified 2026-05-15: grep across the three routes returned zero matches for `showPraising` / `showCelebrate`. The reactions surface ONLY on `AnsweredWall.tsx:234,236`. Future spec wires BOTH `showPraising={prayer.isAnswered}` and `showCelebrate={prayer.isAnswered}` atomically (plus the `onTogglePraising` / `onToggleCelebrate` handlers) on the three routes.
- **6.10 Search by Author** — ✅ confirmed-deferred to Phase 8. Plan-recon R1+R3 gating 2026-05-15 returned NO. Brief preserved at `_plans/forums/spec-6-10-brief.md`. Tracker line 229: `** REVISIT 6.10 after Phase 8 is done**`. Next-spec sequencing: after Phase 8 ships `/u/:username` route + username DTO field.
- **5.6-followup / Phase 3 success-path `X-RateLimit-*` header standardization** — ✅ confirmed-deferred. Tracker line 179 documents: per `03-backend-standards.md` § "Response Headers (all responses)", the success-path `X-RateLimit-Limit` / `X-RateLimit-Remaining` / `X-RateLimit-Reset` headers should be emitted by every dedicated rate-limit service (PostsRateLimitConfig + CommentsRateLimitConfig + ReactionsRateLimitConfig + BookmarksRateLimitConfig + ReportsRateLimitConfig + ChangePasswordRateLimitConfig + PostsIdempotencyService + VerseFindsYouRateLimitService + PresenceAuthRateLimitService + PresenceAnonRateLimitService) but today they only surface `Retry-After` on the 429 path. Future spec: introduce a `RateLimitProbe { remaining, resetAt, limit }` value object + `ResponseEntityWithRateLimitHeaders.from(probe, body)` helper, then sweep every Phase 3 controller in one atomic pass.

### Test-debt deferrals

- **Upload test fixtures (spec-4-6b followup)** — ✅ confirmed-deferred. Verified 2026-05-15: 12 `@Disabled` in `UploadServiceTest.java` + 10 `@Disabled` in `UploadControllerIntegrationTest.java` = 22 disabled tests, pending binary fixture commit. Tracker entry confirmed in place.
- **Frontend cosmetic test drift (14 tests)** — ✅ confirmed-deferred. GrowthGarden ×3 (aria-label augmentation), warm-empty-states ×1 (duplicate empty-state copy). Investigation filed 2026-05-15. Future cleanup spec. Not introduced by 6.6 / 6.6b / 6.7 / 6.8 / 6.9 / 6.11 / 6.11b.
- **PrayCeremony runaway-timer (POTENTIAL PROD BUG, NOT cosmetic)** — ⚠️ surface-to-Eric. Verified 2026-05-15: `frontend/src/components/prayer-wall/__tests__/PrayCeremony.test.tsx:218` — `it('rapid toggle cancels pending toasts', ...)` carries the recursive `setTimeout` signature attributed to prayer-wall-redesign commit `654e70b`. **Separate investigation owed BEFORE next Prayer Wall surface change.** 6.11's R6 plan-recon noted that the production `PrayCeremony.tsx` component does NOT exist (only the test file), so the bug surface is unclear — Eric must confirm whether to (a) treat as test-debt cleanup (file-deletion candidate), (b) restore the production component, or (c) investigate the recursive timer signature as a real bug in shared toast/queue infrastructure.

### Architectural deferrals

- **GentleExtrasSection toggle duplication** — ✅ confirmed-deferred. Verified 2026-05-15: `frontend/src/components/settings/GentleExtrasSection.tsx:55,110,119` — two near-identical inline toggle implementations (VFY + Live Presence), with `:119` explicitly noting "Same shape as the Verse Finds You toggle above — kept identical." WCAG 2.5.5 touch-target fix applied inline on both per Path B (2026-05-15). Future consolidation spec extracts `<Toggle id checked srOnLabels onChange variant>` to `frontend/src/components/ui/Toggle.tsx`, replaces both inline blocks, updates tests. **Recommend doing this BEFORE adding any third opt-in toggle to the section.**
- **Redis dev port collision** — ✅ resolved. Verified 2026-05-15: `docker-compose.yml:29–30` has `ports: ["6380:6379"]` with the sibling-coexistence comment. Container-internal port stays 6379. Runbook doc at `backend/docs/redis-conventions.md § "Dev port"` referenced. Tracked 2026-05-15 (tracker line 178).
- **6.11b crisis-suppression live verification** — ✅ confirmed-deferred. Gate-G-CRISIS-SUPPRESSION is unit-tested only (`PresenceIndicator.test.tsx` lines 16–60, multiple `suppressed={true|false}` cases). Live e2e verification deferred until `VITE_USE_BACKEND_PRAYER_WALL=true` (Phase 3 cutover complete) AND Phase 10 ships (crisis flags can be set in production). Tracker line 175 carries the procedure: seed a crisis-flagged post, navigate to a page containing it, confirm PresenceIndicator does NOT render at any of the 4 surfaces.

### Out-of-band initiatives

- **Prayer Wall Redesign side quest** — ✅ confirmed-deferred. Un-briefed, separate future initiative referenced by 6.11 R6 (PrayCeremony) and 6.6b master plan note. NOT a Phase 6 cutover concern.

---

## Section 3 — Anomalies + Drift History

The spec tracker (`_forums_master_plan/spec-tracker.md`) documents all four anomalies below in the Phase 6 block (lines 157–181). 6.12 references the tracker by section rather than duplicating content; the tracker is the canonical source.

- **Brief-drift remediation arc.** Tracker block § "Phase 6 brief authoring sourced from stale pristine-baseline copy" (lines 163–171). Root cause: briefs were authored from a stale pristine-baseline copy of `round3-master-plan.md` rather than the live master plan. Caught after 6.5 and 6.6 had merged. Specs affected: **6.5 (Path B — shipped code is source of truth; master plan stub amended with AS-BUILT RECONCILIATION; aggregate page DEFERRED)**, **6.6 (→ 6.6b — four-gap remediation spec ratified the live stub: hero subhead "Gratitude, not comparison.", celebrate reaction + Light-a-Candle→Celebrate swap, category filter chips with deliberate MH omission, answered-text edit/un-mark flow; spawned four deferred items deferred-1 through deferred-4)**, **6.7 (one-shot follow-up — EXIF-free comment + companion test guard at `imageGen.ts:45–58`)**. **Root-cause fix going forward (tracker line 181):** every not-yet-executed Phase 6 brief (6.8, 6.9, 6.10, Prayer Wall Redesign side quest) was re-validated against the LIVE master plan before going to `/spec-forums`. No brief authored or trusted from the pristine-baseline backup again.
- **6.10 deferral.** Tracker line 195 + line 229. Plan-recon 2026-05-15 R1+R3 returned NO. Route + DTO field don't exist; Phase 8 owns them. Brief preserved at `_plans/forums/spec-6-10-brief.md` for revival when Phase 8 ships.
- **6.11 collapse-on-recon.** Tracker not yet updated; this spec's Section 1 6.11 paragraph documents the collapse. Toggle already shipped pre-wave; spec became audit + coverage. AS-BUILT amendment to master plan stub at lines 6171–6184 in scope at cutover time (Section E of the checklist will verify the amendment landed).
- **6.11b Path B wide commit.** Tracker lines 175–179 document the scope-expansion decision and its components: indicator core + mobile responsive-stack fix on 4 routes + breadcrumb-behind-navbar fix on 3 routes (7 `main` elements) + WCAG 2.5.5 touch-target fix on both GentleExtras toggles + Redis port remap (6379→6380) + runbook doc + screenshot hygiene + tracker lines. Tracked.

---

## Acceptance Criteria (master plan, restated)

Per the master plan stub (lines 6344–6349):

- [ ] Every hero feature manually verified — Section B of the cutover checklist.
- [ ] Cutover checklist completed — Sections A through E of the cutover checklist all checked.
- [ ] Phase 6 officially done — Section E of the cutover checklist: master plan amendment landed + tracker line added + CLAUDE.md updated (if convention applies).
- [ ] Universal Rule 17 per-phase accessibility smoke test passes — Section C of the cutover checklist: axe-core automated scan on routes and components introduced or modified in this phase returns zero CRITICAL violations; keyboard-only navigation walkthrough of this phase's primary user flows completes without dead-ends; VoiceOver spot-check on the 2-3 most complex interactions introduced in this phase completes without blocking issues; evidence committed to `_cutover-evidence/phase6-a11y-smoke.json` (axe-core report) plus a brief markdown note recording keyboard and VoiceOver outcomes.

---

## What Not to Do (cutover discipline)

The cutover is **verification, not remediation.** Reaffirmed here so future readers of this spec understand the posture.

- Do NOT fix anything during cutover. If a check fails, STOP and surface to Eric. Don't fix.
- Do NOT commit binary upload fixtures to make the 22 `@Disabled` tests pass — separate decision.
- Do NOT investigate or fix the PrayCeremony runaway-timer — separate spec.
- Do NOT investigate or fix the 14 cosmetic frontend test failures — separate cleanup spec.
- Do NOT re-curate any 6.8 verses or modify `verse-finds-you.json`.
- Do NOT enable `VITE_USE_BACKEND_PRAYER_WALL` just to test 6.11b crisis suppression live — that's a separate Phase 3 / Phase 10 readiness call.
- Do NOT touch any production code outside the spec / plan / checklist documents themselves.
- No git operations.

---

## Pipeline Notes

- **Plan:** None. The cutover IS the plan. The checklist at `_plans/forums/2026-05-15-spec-6-12-cutover-checklist.md` is the executable artifact.
- **Execute:** None. The checklist is what Eric works through manually.
- **`/code-review`:** NOT applicable. 6.12 changes no production code. Spec + plan + tracker + master plan amendment + CLAUDE.md update (if applicable) are the only artifacts.
- **`/verify-with-playwright`:** NOT applicable. Section C axe-core scan IS the verification step; not a Playwright visual pass.
- **Cutover close:** Eric commits spec + checklist + master plan amendment + tracker line + CLAUDE.md update (if convention applies) when satisfied. Phase 6 closed.

---

## See Also

- `_plans/forums/2026-05-15-spec-6-12-cutover-checklist.md` — the executable checklist Eric works through.
- `_forums_master_plan/round3-master-plan.md` lines 6336–6349 — the originating stub.
- `_forums_master_plan/spec-tracker.md` lines 157–181 — Phase 6 status block with the four anomaly entries.
- `.claude/rules/CRITICAL_NO_AI_AUTO_REPLY.md` — HARD ENFORCEMENT GATE established by 6.4 (Gate-G-NO-AI-AUTO-REPLY).
- `_forums_master_plan/round3-master-plan.md` Universal Rule 17 (line 823) — per-phase accessibility smoke test contract.
