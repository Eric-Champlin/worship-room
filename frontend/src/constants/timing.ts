/** Shared timing constants used across UI components */

/** Delay before resetting "Copied!" text back to default (ms) */
export const COPY_RESET_DELAY = 2000

/** Delay before clearing screen reader announcements (ms) */
export const ANNOUNCEMENT_CLEAR_DELAY = 3000

/** Default search debounce delay (ms) */
export const SEARCH_DEBOUNCE_DELAY = 300
