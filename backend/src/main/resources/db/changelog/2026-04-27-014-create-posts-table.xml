<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

  <!-- Forums Wave Spec 3.1 — canonical unified `posts` table per master plan
       Decision 4 (one table for all 5 post_types: prayer_request, testimony,
       question, discussion, encouragement) and Decision 5 (Light a Candle
       reaction). 26 columns covering core content, optional context (challenge,
       qotd, scripture), visibility, answered state, moderation, soft-delete,
       and 5 denormalized counters (praying, candle, comment, bookmark, report)
       maintained by application-layer increment/decrement logic in Specs 3.5+.
       crisis_flag column ships now (Universal Rule 13) so Spec 3.5's classifier
       has a column to write — no detection logic in this changeset. Once this
       changeset runs, its MD5 is permanent in DATABASECHANGELOG; future schema
       changes go in new YYYY-MM-DD-NNN-*.xml changesets. -->
  <changeSet id="2026-04-27-014-create-posts-table" author="worship-room">

    <createTable tableName="posts">
      <column name="id" type="UUID" defaultValueComputed="gen_random_uuid()">
        <constraints primaryKey="true" nullable="false"/>
      </column>
      <column name="user_id" type="UUID">
        <constraints nullable="false"/>
      </column>
      <column name="post_type" type="VARCHAR(20)">
        <constraints nullable="false"/>
      </column>
      <column name="content" type="TEXT">
        <constraints nullable="false"/>
      </column>
      <column name="category" type="VARCHAR(20)"/>
      <column name="is_anonymous" type="BOOLEAN" defaultValueBoolean="false">
        <constraints nullable="false"/>
      </column>
      <column name="challenge_id" type="VARCHAR(50)"/>
      <column name="qotd_id" type="VARCHAR(50)"/>
      <column name="scripture_reference" type="VARCHAR(100)"/>
      <column name="scripture_text" type="TEXT"/>
      <column name="visibility" type="VARCHAR(20)" defaultValue="public">
        <constraints nullable="false"/>
      </column>
      <column name="is_answered" type="BOOLEAN" defaultValueBoolean="false">
        <constraints nullable="false"/>
      </column>
      <column name="answered_text" type="TEXT"/>
      <column name="answered_at" type="TIMESTAMP WITH TIME ZONE"/>
      <column name="moderation_status" type="VARCHAR(20)" defaultValue="approved">
        <constraints nullable="false"/>
      </column>
      <column name="crisis_flag" type="BOOLEAN" defaultValueBoolean="false">
        <constraints nullable="false"/>
      </column>
      <column name="is_deleted" type="BOOLEAN" defaultValueBoolean="false">
        <constraints nullable="false"/>
      </column>
      <column name="deleted_at" type="TIMESTAMP WITH TIME ZONE"/>
      <column name="praying_count" type="INTEGER" defaultValueNumeric="0">
        <constraints nullable="false"/>
      </column>
      <column name="candle_count" type="INTEGER" defaultValueNumeric="0">
        <constraints nullable="false"/>
      </column>
      <column name="comment_count" type="INTEGER" defaultValueNumeric="0">
        <constraints nullable="false"/>
      </column>
      <column name="bookmark_count" type="INTEGER" defaultValueNumeric="0">
        <constraints nullable="false"/>
      </column>
      <column name="report_count" type="INTEGER" defaultValueNumeric="0">
        <constraints nullable="false"/>
      </column>
      <column name="created_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="NOW()">
        <constraints nullable="false"/>
      </column>
      <column name="updated_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="NOW()">
        <constraints nullable="false"/>
      </column>
      <column name="last_activity_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="NOW()">
        <constraints nullable="false"/>
      </column>
    </createTable>

    <addForeignKeyConstraint
      baseTableName="posts"
      baseColumnNames="user_id"
      constraintName="fk_posts_user"
      referencedTableName="users"
      referencedColumnNames="id"
      onDelete="CASCADE"/>

    <!-- Named CHECK constraints. All enum-like VARCHAR columns are validated at
         the DB level. posts_category_check explicitly allows NULL because
         post_type='discussion' rows may not carry a category. -->
    <sql>
      ALTER TABLE posts ADD CONSTRAINT posts_post_type_check
      CHECK (post_type IN ('prayer_request', 'testimony', 'question', 'discussion', 'encouragement'));
    </sql>

    <sql>
      ALTER TABLE posts ADD CONSTRAINT posts_category_check
      CHECK (category IS NULL OR category IN ('health', 'mental-health', 'family', 'work', 'grief', 'gratitude', 'praise', 'relationships', 'other', 'discussion'));
    </sql>

    <sql>
      ALTER TABLE posts ADD CONSTRAINT posts_visibility_check
      CHECK (visibility IN ('public', 'friends', 'private'));
    </sql>

    <sql>
      ALTER TABLE posts ADD CONSTRAINT posts_moderation_status_check
      CHECK (moderation_status IN ('approved', 'flagged', 'hidden', 'removed'));
    </sql>

    <sql>
      ALTER TABLE posts ADD CONSTRAINT posts_soft_delete_consistency
      CHECK ((is_deleted = FALSE AND deleted_at IS NULL) OR (is_deleted = TRUE AND deleted_at IS NOT NULL));
    </sql>

    <sql>
      ALTER TABLE posts ADD CONSTRAINT posts_answered_consistency
      CHECK ((is_answered = FALSE AND answered_at IS NULL) OR (is_answered = TRUE AND answered_at IS NOT NULL));
    </sql>

    <!-- Plain index for per-user feed (Profile / My Prayers / Activity). -->
    <createIndex tableName="posts" indexName="idx_posts_user_id_created_at">
      <column name="user_id"/>
      <column name="created_at" descending="true"/>
    </createIndex>

    <!-- Partial indexes use raw SQL because Liquibase's <createIndex> does not
         support WHERE clauses. Each predicate matches the spec's read-path
         filter (e.g., the public feed always filters is_deleted=FALSE). -->
    <sql>
      CREATE INDEX idx_posts_visibility_moderation_created
        ON posts (visibility, moderation_status, created_at DESC)
        WHERE is_deleted = FALSE;
    </sql>

    <sql>
      CREATE INDEX idx_posts_category_created
        ON posts (category, created_at DESC)
        WHERE is_deleted = FALSE AND moderation_status = 'approved';
    </sql>

    <sql>
      CREATE INDEX idx_posts_post_type_created
        ON posts (post_type, created_at DESC)
        WHERE is_deleted = FALSE AND moderation_status = 'approved';
    </sql>

    <sql>
      CREATE INDEX idx_posts_last_activity
        ON posts (last_activity_at DESC)
        WHERE is_deleted = FALSE AND moderation_status = 'approved';
    </sql>

    <sql>
      CREATE INDEX idx_posts_qotd_id
        ON posts (qotd_id)
        WHERE qotd_id IS NOT NULL;
    </sql>

    <sql>
      CREATE INDEX idx_posts_challenge_id
        ON posts (challenge_id)
        WHERE challenge_id IS NOT NULL;
    </sql>

    <sql>
      CREATE INDEX idx_posts_crisis_flag
        ON posts (crisis_flag, created_at DESC)
        WHERE crisis_flag = TRUE AND is_deleted = FALSE;
    </sql>

    <sql>
      CREATE INDEX idx_posts_is_answered_created
        ON posts (created_at DESC)
        WHERE is_answered = TRUE AND is_deleted = FALSE;
    </sql>

    <rollback>
      <dropTable tableName="posts"/>
    </rollback>

  </changeSet>
</databaseChangeLog>
