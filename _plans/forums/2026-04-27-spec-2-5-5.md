# Forums Wave Plan: Spec 2.5.5 — Phase 2.5 Cutover

**Spec:** `_specs/forums/spec-2-5-5.md`
**Master Plan:** `_forums_master_plan/round3-master-plan.md` → Spec 2.5.5 (Phase 2.5 cutover)
**Date:** 2026-04-27
**Branch:** `forums-wave-continued` (no per-spec sub-branch)
**Phase:** 2.5 — Friends Migration (Dual-Write)
**Size:** S
**Risk:** Medium (live cutover — flips dual-write from off to on for friends + social; first time the backend receives real friends/social traffic from a real frontend, and first end-to-end run of backend-side milestone-event emission)

---

## Affected Frontend Routes

N/A — backend traffic activation spec. The only code changes are two flag default flips in `frontend/.env.example`. No new components, no UI changes, no new routes. `/verify-with-playwright` is therefore skipped for this spec; runtime verification happens via the manual smoke test documented in the cutover checklist (Eric's responsibility, not CC's).

---

## Universal Rules Checklist

All 17 Universal Rules from `_forums_master_plan/round3-master-plan.md`. Marked applicable (✅), not applicable (N/A), or needs-attention (⚠️):

- [x] Rule 1: ✅ No git operations by CC. Eric handles all git per spec Guardrails.
- [x] Rule 2: ✅ Master Plan + Spec 2.5.5 entry read. Three divergences (two flags not one; checklist path matches existing convention; smoke test verifies milestone emission) and Universal Rule 17 retrofit pinned in spec.
- [x] Rule 3: N/A — no Liquibase changesets. Zero schema changes.
- [x] Rule 4: N/A — `openapi.yaml` unchanged. Zero API changes.
- [x] Rule 5: N/A — no user-facing strings introduced. The cutover checklist is internal operator documentation, not user-facing copy.
- [x] Rule 6: N/A — no automated tests added. Spec § "Tests Required" explicitly states "Adding automated tests for flag flips is busywork." Vitest tests authored in 2.5.4 and 2.5.4b already verify both flag states for each hook.
- [x] Rule 7: N/A — no new `wr_*` localStorage keys.
- [x] Rule 8: N/A — no new components or reactive store consumers.
- [x] Rule 9: ⚠️ — A11y is touched only via Rule 17 retrofit (see below). Scope is "verify existing routes still scan clean," NOT new accessibility work, because Phase 2.5 added no UI (Recon I confirms).
- [x] Rule 10: ⚠️ — `pnpm build` must remain clean with both flags set to `true`. No new bundle weight expected (two string changes in `.env.example`). Verified in Step 5.
- [x] Rule 11: N/A — no new user-facing copy. The phase02-5 checklist follows pastor's-wife voice in operator-internal sections.
- [x] Rule 12: N/A — no anti-pressure copy surfaces touched.
- [x] Rule 13: N/A — no crisis-detection paths touched. The eight Phase 2.5 endpoints already live behind backend rate limits + JWT auth (Phase 1); this cutover changes the FREQUENCY of writes from zero to dual-write but does not introduce new content paths.
- [x] Rule 14: N/A — no user-generated content paths touched.
- [x] Rule 15: N/A — no new endpoints. Phase 2.5's eight endpoints inherit filter-chain defaults (request ID, JWT, security headers). Per-user rate limiting on friends endpoints is Spec 10.9 territory; deferred per Spec 2.5.3's Divergence 1.
- [x] Rule 16: ✅ — phase02-5 checklist mirrors phase02 checklist structure (numbered H2 sections, bracketed placeholders, sign-off footer). Recon verified the existing format precedent at `_plans/forums/phase02-cutover-checklist.md`.
- [x] Rule 17: ⚠️ — Per-phase cutover accessibility smoke test required. CC scaffolds the placeholder evidence files (`_cutover-evidence/phase02-5-a11y-smoke.json` + `_cutover-evidence/phase02-5-a11y-notes.md`); Eric runs the actual axe-core scan + keyboard walkthrough + VoiceOver spot-check during the smoke test execution and replaces the placeholder content. Phase 2.5 added zero new user-visible UI (Recon I), so the scan is a regression check on existing routes.

---

## Architecture Context

From reconnaissance:

### Spec category

**This is a CUTOVER spec.** Per the spec-category guidance, cutover specs flip a feature flag from `false` to `true` and declare a phase complete. Required structure: flag flip step, smoke test step, Universal Rule 17 a11y evidence, rollback readiness step. All four are present in the implementation steps below (smoke test is operator-executed, not code-executed; rollback is documented in the checklist body). This cutover differs from Spec 2.9's in two respects: **two flags flip atomically** (not one), and the **smoke test scope includes backend-side milestone-event emission** end-to-end verification.

### Verified recon facts

- **Flag locations & values (verified):**
  - `frontend/.env.example` line 34 reads `VITE_USE_BACKEND_FRIENDS=false`. Lines 32–33 contain a comment block: "Friends dual-write: POST/PATCH/DELETE on friend mutation endpoints after each localStorage friends write. Cutover (flag flip to true) is owned by Spec 2.5.5."
  - `frontend/.env.example` line 39 reads `VITE_USE_BACKEND_SOCIAL=false`. Lines 36–38 contain a comment block: "Spec 2.5.4b — dual-write social interactions (encouragements, nudges, recap dismissal) to the backend. Default false. Cutover (flag flip to true) is owned by Spec 2.5.5."
  - This spec replaces both values (`false`→`true`) and updates both comment blocks to reflect cutover completion.

- **Flag consumers (verified):**
  - `frontend/src/lib/env.ts` lines 87–103 expose `isBackendFriendsEnabled()` with strict equality `=== 'true'` (fail-closed). Used by `useFriends` (Spec 2.5.4 — Frontend Friends Dual-Write).
  - `frontend/src/lib/env.ts` lines 105–121 expose `isBackendSocialEnabled()` with strict equality `=== 'true'` (fail-closed). Used by `useSocialInteractions` (Spec 2.5.4b — Social/Milestone Dual-Write).
  - `frontend/src/hooks/useFriends.ts` and `frontend/src/hooks/useSocialInteractions.ts` both exist and consume the helpers.
  - Per Architectural Decision #5 in the spec, the `isBackendFriendsEnabled` and `isBackendSocialEnabled` docstrings ("Default: false. Cutover (flag default flip) is owned by Spec 2.5.5.") are courtesy updates, NOT deliverables. Out of scope.

- **No vite.config.ts reference (verified):** `grep -n "VITE_USE_BACKEND_FRIENDS\|VITE_USE_BACKEND_SOCIAL" frontend/vite.config.ts` returns zero matches. Per Architectural Decision #5, do NOT touch `vite.config.ts`.

- **Cutover checklist precedent (verified):** `_plans/forums/phase02-cutover-checklist.md` exists and uses the exact pattern this spec mimics — numbered H2 sections, bracketed placeholders, deferred-with-tracking-note items, sign-off footer. The Phase 2.5 checklist mirrors this structure scoped to friends/social dual-write activation and milestone emission verification.

- **Cutover evidence directory (verified):** `_cutover-evidence/` already exists with `phase1-a11y-smoke.json`, `phase2-a11y-smoke.json`, `phase2-a11y-notes.md`, and `capture-axe-evidence.mjs`. Phase 2.5 evidence files (`phase02-5-a11y-smoke.json` + `phase02-5-a11y-notes.md`) join the same directory. Eric can adapt `capture-axe-evidence.mjs` from prior phases — Phase 2.5 added no new routes (Recon I).

- **Frontend Dockerfile gap (verified):** `frontend/Dockerfile` lines 7–8 wire `ARG VITE_API_BASE_URL` + `ENV VITE_API_BASE_URL=$VITE_API_BASE_URL` only. Neither `VITE_USE_BACKEND_ACTIVITY` (from Spec 2.9) nor `VITE_USE_BACKEND_FRIENDS` / `VITE_USE_BACKEND_SOCIAL` are wired. The phase02-5 checklist instructs Eric to verify and patch the Dockerfile during the prod cutover step; that verification is operator work, not CC work (per Spec 2.9 precedent and this spec's Recon C).

- **Backend endpoints (verified per spec Recon D):** All Phase 2.5 endpoints are live in dev: `POST /api/v1/users/me/friend-requests`, `PATCH /api/v1/friend-requests/{id}`, `DELETE /api/v1/users/me/friends/{friendId}`, `POST /api/v1/users/me/blocks`, `POST /api/v1/social/encouragements`, `POST /api/v1/social/nudges`, `POST /api/v1/social/recap-dismissal`. All require Bearer JWT. All persist to the four Phase 2.5 tables: `friend_relationships`, `friend_requests`, `social_interactions`, `milestone_events`.

- **Backend milestone emission already live (verified per spec Goal):** Per 2.5.4b's Divergence 1, milestone-event emission from `ActivityService.recordActivity` and `FriendsService.acceptRequest` is backend-side and unconditional — no frontend POST, no env-flag gating. The cutover is therefore frontend-side only. Smokes 9–11 in the checklist verify these emissions end-to-end for the first time.

- **CLAUDE.md decision (verified per spec Recon F):** Phase 2.5 is not closed yet (Specs 2.5.6 and 2.5.7 remain). Adding a Phase 2.5 enumeration before 2.5.7 closes the wave is premature. Per Architectural Decision #5, leave CLAUDE.md unchanged.

### Files this spec will not touch

Per spec § "Files NOT to modify" and Guardrails:

- `vite.config.ts`
- `CLAUDE.md`
- `frontend/src/lib/env.ts` (the `isBackendFriendsEnabled` / `isBackendSocialEnabled` docstrings are courtesy updates; not in scope)
- `frontend/src/hooks/useFriends.ts`
- `frontend/src/hooks/useSocialInteractions.ts`
- `frontend/src/lib/api-client.ts` and any `services/api/*` file
- Any `backend/**` file
- `openapi.yaml`
- `master.xml` or any Liquibase changeset
- Any test file
- `_forums_master_plan/spec-tracker.md`

---

## Database Changes

None. Zero schema changes in this spec.

---

## API Changes

None. Zero API changes in this spec.

---

## Assumptions & Pre-Execution Checklist

- [ ] Prerequisite spec 2.5.4 (Frontend Friends Dual-Write) is complete and the six friend mutations dual-write through `useFriends`.
- [ ] Prerequisite spec 2.5.4b (Social/Milestone Dual-Write) is complete and the three social mutations dual-write through `useSocialInteractions`. Backend-side milestone emission from `ActivityService.recordActivity` and `FriendsService.acceptRequest` is live and unconditional.
- [ ] Working tree is clean before execution begins (CC modifies `frontend/.env.example` and creates three new files; Eric handles all git).
- [ ] Branch is `forums-wave-continued`. CC does NOT switch branches.
- [ ] No automated tests added. Vitest baseline matches post-2.5.4b count; verifying the baseline holds after the flag flips is part of Step 6.
- [ ] No backend changes. CC does not start the backend, does not modify Java code, does not edit Liquibase changesets.
- [ ] Phase 2.9 cutover already shipped: `VITE_USE_BACKEND_ACTIVITY=true` is the current `.env.example` default (verified during recon — line 30). Milestone emission verification in the smoke test depends on the activity dual-write path running.

---

## Spec-Category-Specific Guidance

### This is a CUTOVER spec

Per `/plan-forums` skill guidance, cutover specs flip a feature flag from `false` to `true` and declare a phase complete. Required steps:

1. **Flag flip step** — set `VITE_USE_BACKEND_FRIENDS=true` AND `VITE_USE_BACKEND_SOCIAL=true` in `frontend/.env.example`. Update both comment blocks to reflect cutover completion. Atomic — both flags flip in a single edit pass per Architectural Decision #1. **Implemented in Step 1.**
2. **Smoke test step** — exercise the happy path for every flow touched in Phase 2.5 (eight friend/social mutation smokes + three milestone emission smokes). **Implemented in the phase02-5-cutover-checklist.md body (Step 2).** CC writes the checklist; Eric runs the smoke test.
3. **Universal Rule 17 accessibility smoke test** — `_cutover-evidence/phase02-5-a11y-smoke.json` from an axe-core run + keyboard walkthrough + VoiceOver spot-check notes. **Implemented in Steps 3 and 4 as placeholder scaffolds**; Eric runs the actual axe-core scan during the smoke test execution and replaces placeholder content.
4. **Rollback readiness step** — documented in the checklist body, NOT as a code change. **Implemented in Step 2 (the checklist's "Rollback plan" section).**

The four required cutover-spec elements all map to specific implementation steps below.

---

## Edge Cases & Decisions

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Flip flags atomically (one step) or staggered (two steps)? | ATOMIC (one step) | Per Architectural Decision #1 in the spec, both flags flip in a single commit. Functionally a single edit pass to one file with two adjacent comment blocks. Two separate plan steps would be unnecessary ceremony for what is one logical change. |
| Update `vite.config.ts`? | NO | Recon verified zero references in `vite.config.ts`. Per Architectural Decision #5, do NOT touch. |
| Update `lib/env.ts` docstrings ("Default: false. Cutover ... owned by Spec 2.5.5.")? | NO | Per Architectural Decision #5, courtesy updates, not deliverables. Out of scope. |
| Update CLAUDE.md? | NO | Per Architectural Decision #5 and Recon F. Phase 2.5 not closed yet (Specs 2.5.6 and 2.5.7 remain). Phase summary belongs in a post-2.5.7 hygiene update. |
| Generate fake axe-core JSON for the placeholder? | NO | Per spec Guardrail "Do NOT generate the `_cutover-evidence/*` files with fake axe-core JSON output." Placeholder is a clear scaffold noting Eric's authorship, not synthesized scan output. |
| Flip Railway prod env vars as part of this spec? | NO | Per Spec 2.9 precedent and this spec's Recon C, the Railway env vars are operator work documented in the checklist body. CC does not have Railway access. |
| Patch `frontend/Dockerfile` to wire `VITE_USE_BACKEND_FRIENDS` and `VITE_USE_BACKEND_SOCIAL` ARG/ENV? | NO | Recon confirmed Dockerfile only has `VITE_API_BASE_URL` wired today; even `VITE_USE_BACKEND_ACTIVITY` from Spec 2.9 was deferred to operator work. Per Spec 2.9 pattern, the checklist instructs Eric to verify and patch during the prod cutover step. CC does not modify the Dockerfile in this spec. |
| Add automated test for the flag flips? | NO | Per spec Guardrail "Do NOT add automated tests for the flag flips." Existing Vitest tests in 2.5.4 and 2.5.4b verify both flag states for each hook via `vi.stubEnv`. |
| Modify `_forums_master_plan/spec-tracker.md`? | NO | Per spec Guardrail. Eric flips tracker rows manually after commit. |
| Smokes 9–11 (milestone emission) — best-effort or hard requirement? | BEST-EFFORT | Per spec § Smoke 9 and Smoke 10, milestone emission requires near-threshold seed-data state (faith_points near level threshold, current_streak at threshold-1). If seed data doesn't naturally land at threshold-1, document the deviation in the checklist and defer to natural usage. Smoke 11 (FRIEND_MILESTONE) IS confirmable since it falls out of Smoke 2 by construction. |
| Author placeholder JSON as `{}` or as a structured stub with markers? | Structured stub | Per Spec 2.9 precedent. A `{}` empty object is valid JSON but offers no guidance. The placeholder self-identifies as needing replacement and references `capture-axe-evidence.mjs` as the source of real content. |
| If a smoke surfaces a problem mid-execution, what does CC do? | Stop and report | Per spec § Notes for Eric, "If the smoke test surfaces ANY problem, DO NOT commit this spec." CC's role ends with creating the four artifacts; Eric runs the smoke. CC does NOT diagnose backend issues mid-cutover. |

---

## Implementation Steps

### Step 1: Flip both flag defaults and update both comment blocks atomically

**Objective:** The single production code change in this spec. Flips the friends + social dual-write defaults from off to on for new clones and the Vite build pipeline. Updates both explanatory comment blocks so neither claims "Default: false" nor names Spec 2.5.5 as a future cutover owner. Per Architectural Decision #1, both flips happen in a single edit pass — atomic cutover.

**Files to create/modify:**

- `frontend/.env.example` — modify lines 32–39 (both dual-write comment blocks + both flag values).

**Details:**

The current state at lines 32–39 is:

```
# Friends dual-write: POST/PATCH/DELETE on friend mutation endpoints after each
# localStorage friends write. Cutover (flag flip to true) is owned by Spec 2.5.5.
VITE_USE_BACKEND_FRIENDS=false

# Spec 2.5.4b — dual-write social interactions (encouragements, nudges, recap
# dismissal) to the backend. Default false. Cutover (flag flip to true) is
# owned by Spec 2.5.5.
VITE_USE_BACKEND_SOCIAL=false
```

Replace with the post-cutover state:

```
# Friends dual-write: POST/PATCH/DELETE on friend mutation endpoints after each
# localStorage friends write. Activated by Spec 2.5.5 cutover on 2026-04-27.
VITE_USE_BACKEND_FRIENDS=true

# Spec 2.5.4b — dual-write social interactions (encouragements, nudges, recap
# dismissal) to the backend. Activated by Spec 2.5.5 cutover on 2026-04-27.
VITE_USE_BACKEND_SOCIAL=true
```

**Notes about this rewrite:**

- The block-level header at lines 19–27 (the Phase 3 Forums Wave header + post-cutover paragraph from Spec 2.9) is preserved unchanged. It already reads "Default `true` means the dual-write to the backend fires alongside the existing localStorage write" — that paragraph applies to all three flags now.
- The `VITE_USE_BACKEND_ACTIVITY=true` block at lines 28–30 is preserved unchanged (already cutover'd by Spec 2.9).
- The "Activated by Spec 2.5.5 cutover on 2026-04-27." line mirrors the wording Spec 2.9 used for the activity flag.
- The blank line between the two flag blocks is preserved (existing convention).
- Each flag's specific descriptive text ("Friends dual-write: POST/PATCH/DELETE on friend mutation endpoints..." and "Spec 2.5.4b — dual-write social interactions...") is retained — only the cutover-status sentence changes.

**Guardrails (DO NOT):**

- Do NOT modify `vite.config.ts` (recon verified zero references; Architectural Decision #5).
- Do NOT modify `frontend/src/lib/env.ts` (Architectural Decision #5; the docstring updates are a courtesy, not a deliverable).
- Do NOT modify any `.env.local` file (gitignored; Eric handles his local override per checklist).
- Do NOT modify `frontend/src/hooks/useFriends.ts` (Spec 2.5.4's wiring is correct as-is).
- Do NOT modify `frontend/src/hooks/useSocialInteractions.ts` (Spec 2.5.4b's wiring is correct as-is).
- Do NOT add new env vars beyond the existing `VITE_USE_BACKEND_FRIENDS` and `VITE_USE_BACKEND_SOCIAL` lines.
- Do NOT touch `VITE_USE_BACKEND_ACTIVITY=true` (already cutover'd by Spec 2.9).
- Do NOT touch the AI/third-party API key block (lines 41+ in `.env.example`).
- Do NOT touch the `VITE_API_BASE_URL`, `VITE_SITE_URL`, `VITE_HERO_VIDEO_URL`, or `VITE_VAPID_PUBLIC_KEY` blocks.
- Do NOT split this into two steps. Both flag flips happen atomically per Architectural Decision #1.
- Do NOT switch branches.

**Verification:**

- [ ] `grep -n "VITE_USE_BACKEND_FRIENDS=true" frontend/.env.example` returns exactly one match.
- [ ] `grep -n "VITE_USE_BACKEND_SOCIAL=true" frontend/.env.example` returns exactly one match.
- [ ] `grep -n "VITE_USE_BACKEND_FRIENDS=false" frontend/.env.example` returns zero matches.
- [ ] `grep -n "VITE_USE_BACKEND_SOCIAL=false" frontend/.env.example` returns zero matches.
- [ ] `grep -n "Cutover (flag flip to true) is owned by Spec 2.5.5" frontend/.env.example` returns zero matches (both old "owned by" sentences are fully replaced).
- [ ] `grep -n "Activated by Spec 2.5.5 cutover on 2026-04-27" frontend/.env.example` returns exactly two matches (one per flag block).
- [ ] `grep -n "VITE_USE_BACKEND_ACTIVITY=true" frontend/.env.example` still returns one match (Spec 2.9 cutover preserved).
- [ ] `grep -n "VITE_USE_BACKEND_FRIENDS\|VITE_USE_BACKEND_SOCIAL" frontend/vite.config.ts` returns zero matches (vite.config.ts unchanged).

**Test specifications:**

| Test | Type | Description |
|------|------|-------------|
| (none) | — | No new tests. Existing Vitest tests in 2.5.4 and 2.5.4b verify both flag states for each hook via `vi.stubEnv`. The `frontend/src/lib/__tests__/env.test.ts` cases for the helpers are flag-state-agnostic and continue to pass after the flips. |

**Expected state after completion:**

- [ ] `frontend/.env.example` contains `VITE_USE_BACKEND_FRIENDS=true` and `VITE_USE_BACKEND_SOCIAL=true` with both surrounding comment blocks reflecting post-cutover state.
- [ ] `VITE_USE_BACKEND_ACTIVITY=true` is preserved untouched.
- [ ] No other file modified.

---

### Step 2: Author `_plans/forums/phase02-5-cutover-checklist.md`

**Objective:** Produce the cutover checklist that is itself the spec's primary deliverable per Architectural Decision #3. Eric works through the checkbox items during execution; this file is committed to Git and serves as the audit trail for Phase 2.5 partial completion (Specs 2.5.1–2.5.5) and as a template for the Phase 3 cutover.

**Files to create/modify:**

- `_plans/forums/phase02-5-cutover-checklist.md` — new file.

**Details:**

The checklist mirrors `_plans/forums/phase02-cutover-checklist.md` structure (numbered H2 sections with checkbox items, sign-off footer with placeholder dates) scoped to friends/social dual-write activation and milestone emission verification. The major difference from Phase 2 is the smoke test scope: 8 friend/social mutation smokes + 3 milestone emission verifications instead of 5 activity smokes.

**Required sections (in order, matching spec § "Deliverable Structure"):**

1. **Title + frontmatter:** `# Phase 2.5 Cutover Checklist — Friends + Social Dual-Write`. Single-line purpose. Single-line Phase 2.5 status before this spec ships ("5/8 specs complete (2.5.1–2.5.4b). 2.5.5 (this) is the activation. 2.5.6 (block UX) and 2.5.7 (mute UX) remain after."). Single-line cutover date placeholder.

2. **Overview:** 1–2 paragraphs describing what Phase 2.5 delivered (4 backend tables: `friend_relationships`, `friend_requests`, `social_interactions`, `milestone_events`; 2 services with backend-side milestone-event emission integration; 8 endpoints; 2 frontend hook wirings — `useFriends` and `useSocialInteractions`), why this cutover happens now (no drift detection for friends/social, but surface area is small and well-tested by 2.5.4 + 2.5.4b Vitest suites), what changes for users (nothing visible) and for the backend (real friends/social traffic, plus milestone events fire from real user actions for the first time).

3. **`## 1. Pre-flight checks`** — branch verification (`forums-wave-continued`), working tree clean, Docker running, backend Postgres container running (`docker-compose up`), backend dev server running (`./mvnw spring-boot:run`), `/api/v1/health` returns 200, frontend dev server running (`pnpm dev`), able to log in with TWO dev seed users (e.g., `dev1@worshiproom.com` and `dev2@worshiproom.com` — friends flow needs two real users to land real backend rows), `VITE_USE_BACKEND_ACTIVITY=true` is set (already cutover'd by Spec 2.9, needed for milestone emission verification).

4. **`## 2. Code change`** — the five bullets from spec § "Deliverable Structure":
   - Flip `VITE_USE_BACKEND_FRIENDS=true` in `frontend/.env.example` (CC has done this in Step 1; Eric verifies)
   - Flip `VITE_USE_BACKEND_SOCIAL=true` in `frontend/.env.example` (CC has done this in Step 1; Eric verifies)
   - Update both comment blocks: "Activated by Spec 2.5.5 cutover on 2026-04-27." (CC has done this in Step 1; Eric verifies)
   - Set both vars to `true` in local `frontend/.env.local` (Eric does this manually because `.env.local` is gitignored)
   - Restart frontend dev server (Vite env vars are build-time)

5. **`## 3. Build verification`** — `pnpm tsc --noEmit` clean, `pnpm lint` clean, `pnpm build` clean, `pnpm test` clean (Vitest baseline matches post-2.5.4b count; any NEW failing file is a regression).

6. **`## 4. Manual smoke test — friends mutations (between two real users)`** — preface noting log-in pattern (dev1 in tab A, dev2 in tab B, incognito or different browser). Eight numbered subsections per spec § "Smoke 1" through "Smoke 8":
   - **Smoke 1: Send friend request** — dev1 → dev2; verify backend `friend_requests` row + local `backendId` populated. Document expected 404 USER_NOT_FOUND warning when targeting mock-data UUIDs.
   - **Smoke 2: Accept friend request** — dev2 accepts dev1's; verify `status='accepted'` + two `friend_relationships` rows + first FRIEND_MILESTONE emission if first-friend threshold crossed.
   - **Smoke 3: Send encouragement** — dev1 → dev2; verify `social_interactions` row + local entry.
   - **Smoke 4: Send nudge** — dev1 → dev2; verify `social_interactions` row.
   - **Smoke 5: Dismiss recap** — dev1 dismisses weekly recap; verify `social_interactions` row.
   - **Smoke 6: Decline friend request** — dev2 sends new request to dev1; dev1 declines; verify `status='declined'` + no `friend_relationships` row + 409 DUPLICATE on retry.
   - **Smoke 7: Remove friend** — dev1 removes dev2; verify both `friend_relationships` rows deleted in one operation.
   - **Smoke 8: Block user** — dev1 blocks dev2; verify single `friend_relationships` row with `status='blocked'` + any pending `friend_requests` deleted.

7. **`## 5. Manual smoke test — milestone emission verification`** — preface noting this is the broader scope per spec Divergence 3. Three numbered subsections per spec § "Smoke 9" through "Smoke 11":
   - **Smoke 9: Level-up milestone** — dev1 crosses level threshold via Phase 2 dual-write activities; verify `milestone_events` row with `event_type='LEVEL_UP'` + `metadata.newLevel`. Note "best-effort" framing per spec — defer to natural usage if seed data doesn't land at threshold-1.
   - **Smoke 10: Streak milestone** — dev1 pushes current_streak past threshold; verify `milestone_events` row with `event_type='STREAK_MILESTONE'` + `metadata.streakDays`. Note "best-effort" framing.
   - **Smoke 11: Friend milestone** — confirm row from Smoke 2 (FRIEND_MILESTONE on dev2's first friend acceptance). This one is confirmable by construction.

8. **`## 6. Backend verification queries`** — five `psql` `SELECT` queries against the dev DB (use plain triple-backtick `sql` fencing — the spec body's escaped backslashes are an artifact of the spec living inside a markdown block):
   - Most recent `friend_requests` rows (ORDER BY created_at DESC LIMIT 10)
   - Most recent `friend_relationships` rows (ORDER BY created_at DESC LIMIT 10)
   - Most recent `social_interactions` rows (ORDER BY created_at DESC LIMIT 20)
   - Most recent `milestone_events` rows (ORDER BY occurred_at DESC LIMIT 20)
   - Helper: find dev seed user UUIDs by email (`WHERE email IN ('dev1@worshiproom.com', 'dev2@worshiproom.com')`)

9. **`## 7. localStorage canonicality verification`** — refresh the frontend, verify all UI elements (friends list, pending requests, recent encouragements, milestone feed) reflect the SAME state visible before refresh. Confirms localStorage remains source of truth for reads; backend traffic is shadow only.

10. **`## 8. Accessibility smoke (Universal Rule 17)`** — six checkbox items per spec:
    - Run axe-core scan on `/`, `/daily`, `/prayer-wall`, and `/profile/:userId` (Phase 2.5 added no new routes per Recon I — scope is "verify existing routes still scan clean")
    - Save axe-core JSON output to `_cutover-evidence/phase02-5-a11y-smoke.json` (replace placeholder content created in Step 3)
    - Zero CRITICAL violations across the scanned routes
    - Keyboard-only walkthrough of the friends primary flow (open friends panel → send request → accept request) completes without dead-ends
    - VoiceOver spot-check on encouragement-send interaction completes without blocking issues
    - Brief notes saved to `_cutover-evidence/phase02-5-a11y-notes.md` (replace placeholder content created in Step 4)

    The section header notes that Eric can adapt `_cutover-evidence/capture-axe-evidence.mjs` from prior phases — Phase 2.5 routes are the same set since no new UI shipped.

11. **`## 9. Production deploy plan`** — five checkbox items per spec:
    - Verify Railway frontend Dockerfile exposes both `VITE_USE_BACKEND_FRIENDS` and `VITE_USE_BACKEND_SOCIAL` via `ARG` + `ENV` before `RUN pnpm build` (mirror the `VITE_API_BASE_URL` pattern from Phase 1 and the `VITE_USE_BACKEND_ACTIVITY` pattern from Spec 2.9 cutover). If missing, add a one-line Dockerfile patch for each.
    - Add `VITE_USE_BACKEND_FRIENDS=true` to Railway frontend service env vars (Variables tab → "+ New Variable")
    - Add `VITE_USE_BACKEND_SOCIAL=true` to Railway frontend service env vars
    - Trigger a Railway deploy (push to `main`, or manual deploy)
    - After deploy completes: smoke test ONE friend request flow in prod between two real accounts (verify backend received the row via `railway connect postgres`)
    - If prod smoke test fails: see Rollback plan below

12. **`## 10. Rollback plan (if prod surfaces issues)`** — nine numbered steps per spec:
    1. Set `VITE_USE_BACKEND_FRIENDS=false` AND `VITE_USE_BACKEND_SOCIAL=false` in Railway frontend service env vars
    2. Trigger a redeploy (or wait for next auto-deploy)
    3. Verify in DevTools that subsequent friend mutations no longer fire backend POSTs/PATCHes/DELETEs
    4. Verify subsequent social interactions no longer fire backend POSTs
    5. localStorage data is untouched; users see no difference
    6. Backend rows already written remain (shadow data, no consumer)
    7. Backend-side milestone emissions continue (gated by Phase 2's flag, not Phase 2.5's) — this is by design and not a rollback concern
    8. Open a followup entry in `_plans/post-1.10-followups.md` describing what failed, including any `X-Request-Id` headers captured
    9. Do NOT revert the spec's commit. The flag-flips stay; the env var override is the rollback mechanism.

13. **`## 11. Sign-off`** — bracketed placeholder fields for Cutover date, Eric's sign-off, Local smoke test passed, Production smoke test passed, A11y evidence committed, Phase 2.5 status (final state: "PARTIAL — 6/8 specs complete (2.5.1–2.5.5). Remaining: 2.5.6 (Block UX), 2.5.7 (Mute UX).").

**Tone & voice:**

- Operator-internal voice (matches phase02 checklist). Direct, factual, blameless.
- No exclamation points anywhere.
- Where the checklist mentions user-facing behavior (e.g., the rollback paragraph noting "users see no difference"), the language remains warm and accurate — anti-pressure consistent.

**Guardrails (DO NOT):**

- Do NOT include automated test instructions in the checklist (per spec § "Tests Required").
- Do NOT instruct Eric to revert the cutover commit on rollback (per spec § "Architectural Decision #4" — the flag-flips stay; rollback is via Railway env var override).
- Do NOT skip Smokes 9–11 — they are the highest-signal smokes per spec § Notes for Eric.
- Do NOT mark Smokes 9 or 10 as hard-failures if seed data isn't at threshold-1; they are documented "best-effort" smokes that can defer to natural usage.
- Do NOT prescribe SQL DDL or DROP statements; all queries are read-only `SELECT`.
- Do NOT add JWT secrets, real Railway service IDs, or production user emails to the checklist body. Use placeholders and the dev seed emails (`dev1@worshiproom.com`, `dev2@worshiproom.com`).
- Do NOT include axe-core JSON inline in the markdown — the JSON lives in its own file (Step 3).
- Do NOT treat this checklist as a one-time scratchpad — it stays in Git as the audit trail for Phase 2.5 partial completion.
- Do NOT instruct Eric to flip rows in `_forums_master_plan/spec-tracker.md` from inside the checklist — tracker updates are Eric's separate responsibility per spec Guardrails.

**Verification:**

- [ ] File exists at `_plans/forums/phase02-5-cutover-checklist.md`.
- [ ] All 13 sections present and ordered as specified (Title/frontmatter, Overview, Pre-flight, Code change, Build verification, Friends mutations smoke, Milestone emission smoke, Backend verification queries, localStorage canonicality, A11y smoke, Production deploy plan, Rollback plan, Sign-off).
- [ ] All 8 friend/social mutation smokes (Send/Accept/Encourage/Nudge/Dismiss/Decline/Remove/Block) present with the right backend assertions.
- [ ] All 3 milestone emission smokes (LEVEL_UP, STREAK_MILESTONE, FRIEND_MILESTONE) present with "best-effort" framing for the first two.
- [ ] All 5 SQL queries + helper UUID lookup present in Section 6.
- [ ] All 9 rollback steps present in Section 10 (Step 9 explicitly says "Do NOT revert the spec's commit").
- [ ] Sign-off section uses bracketed placeholder fields (`____________`) per phase02 precedent.
- [ ] Phase 2.5 status line in sign-off reads "PARTIAL — 6/8 specs complete (2.5.1–2.5.5). Remaining: 2.5.6 (Block UX), 2.5.7 (Mute UX)."
- [ ] No exclamation points anywhere in the file.
- [ ] No real Railway service IDs, no production user emails, no real JWT secrets — only placeholders + dev seed emails.

**Test specifications:**

| Test | Type | Description |
|------|------|-------------|
| (none) | — | Documentation file, no tests. |

**Expected state after completion:**

- [ ] `_plans/forums/phase02-5-cutover-checklist.md` exists and matches spec § "Deliverable Structure" requirements.

---

### Step 3: Create `_cutover-evidence/phase02-5-a11y-smoke.json` placeholder

**Objective:** Scaffold a JSON file that Eric replaces with real axe-core scan output during the smoke-test execution. The placeholder self-identifies and references the existing capture script so future readers (or Eric on a different day) know what's needed.

**Files to create/modify:**

- `_cutover-evidence/phase02-5-a11y-smoke.json` — new file.

**Details:**

The placeholder is structured JSON, not an empty object. It self-identifies as needing replacement and embeds operator instructions in a `_placeholder` key that Eric removes when he writes the real output. Same shape as Spec 2.9's `phase2-a11y-smoke.json` placeholder, retargeted to Phase 2.5.

```json
{
  "_placeholder": "Replace this object with axe-core scan output captured by Eric during the Phase 2.5 cutover smoke test. See _cutover-evidence/capture-axe-evidence.mjs from prior phases for the canonical capture script — extend the ROUTES array to include /, /daily, /prayer-wall, and /profile/:userId. Phase 2.5 added no new routes (Recon I), so the route set matches Phase 2's.",
  "_authoredBy": "Eric Champlin",
  "_authoredDuring": "Phase 2.5 cutover smoke test (Spec 2.5.5)",
  "_expectedShape": {
    "captured": "ISO 8601 timestamp",
    "frontendUrl": "URL of the frontend that was scanned (local dev or deployed)",
    "results": "object keyed by route, each value is the @axe-core/playwright AxeResults shape"
  }
}
```

**Tone & voice:**

- Operator-internal. The `_placeholder` description tells the next reader exactly what to do.
- Use leading-underscore keys (`_placeholder`, `_authoredBy`, etc.) so an automated JSON consumer treats them as metadata, not as data.

**Guardrails (DO NOT):**

- Do NOT generate fake axe-core results. Per spec Guardrail "Do NOT generate the `_cutover-evidence/*` files with fake axe-core JSON output."
- Do NOT use `null` or `{}` — both are valid JSON but provide no instruction to the reader.
- Do NOT pre-fill the `captured`, `frontendUrl`, or `results` keys with stub data. The `_expectedShape` documentation key carries the structural contract; values are Eric's responsibility.

**Verification:**

- [ ] File exists at `_cutover-evidence/phase02-5-a11y-smoke.json`.
- [ ] File parses as valid JSON: `node -e "JSON.parse(require('fs').readFileSync('_cutover-evidence/phase02-5-a11y-smoke.json'))"` exits 0.
- [ ] File contains `_placeholder` key with text referencing `capture-axe-evidence.mjs`.
- [ ] File does NOT contain any `violations`, `passes`, `inapplicable`, or `incomplete` keys (those are real axe output keys; their absence proves no fake data was synthesized).
- [ ] File size is < 1 KB (real output is typically ~4 MB; this scaffold should be tiny).

**Test specifications:**

| Test | Type | Description |
|------|------|-------------|
| (none) | — | Placeholder evidence file, no tests. |

**Expected state after completion:**

- [ ] `_cutover-evidence/phase02-5-a11y-smoke.json` exists as a clearly-marked placeholder.

---

### Step 4: Create `_cutover-evidence/phase02-5-a11y-notes.md` placeholder

**Objective:** Scaffold a markdown file with the structure Eric will fill in during keyboard navigation and VoiceOver spot-check phases of the smoke test.

**Files to create/modify:**

- `_cutover-evidence/phase02-5-a11y-notes.md` — new file.

**Details:**

The placeholder presents the structure Eric fills in (one section per acceptance criterion in Universal Rule 17), leaving prompts and checkboxes that turn into evidence on completion. Mirrors `_cutover-evidence/phase2-a11y-notes.md` from Spec 2.9, retargeted to Phase 2.5's smoke flows (friends panel + encouragement-send instead of Daily Hub Pray + Prayer Wall pray-button).

```markdown
# Phase 2.5 Accessibility Notes — Cutover Smoke Test

**Status:** PLACEHOLDER — replace this file's body during the Phase 2.5 cutover smoke test execution. CC scaffolds the structure; Eric authors the real content.

**Cutover spec:** Spec 2.5.5 — Phase 2.5 Cutover (Friends + Social Dual-Write)
**Cutover date:** _____________ (Eric fills in)
**Authored by:** Eric Champlin
**Pairs with:** `_cutover-evidence/phase02-5-a11y-smoke.json` (axe-core JSON output)

---

## Scope (Universal Rule 17)

Phase 2.5 wired backend dual-write to existing Friends UI + existing social-interaction UI + existing milestone-feed UI; no new user-visible UI was added (Recon I). The accessibility smoke test verifies the routes touched by Phase 2.5 still scan clean — it is a regression check, not a net-new audit.

**Routes scanned:** `/`, `/daily`, `/prayer-wall`, `/profile/:userId` (and any friends-specific routes that exist).

---

## 1. axe-core automated scan

- [ ] Scan completed against the routes listed above
- [ ] Zero CRITICAL violations across all scanned routes
- [ ] JSON output committed to `_cutover-evidence/phase02-5-a11y-smoke.json`
- [ ] Any non-CRITICAL findings recorded as follow-up entries in `_plans/post-1.10-followups.md`

**Findings summary:** _____________ (Eric fills in: e.g., "0 CRITICAL, 0 SERIOUS — scan clean" or specific finding details)

---

## 2. Keyboard-only walkthrough

Primary flow: open friends panel → send friend request → accept request (across two browser sessions if dual-account testing).

- [ ] Walkthrough completed without dead-ends
- [ ] Skip-to-main-content link reachable from page load
- [ ] All interactive elements reachable via Tab
- [ ] Focus indicators visible at every step
- [ ] Friend-request and accept buttons announce role + label

**Notes:** _____________ (Eric fills in: any blocked-flow observations, focus management deviations, or "clean")

---

## 3. VoiceOver (macOS) spot-check

Target: encouragement-send interaction.

- [ ] Spot-check completed without blocking issues
- [ ] Encouragement preset chips announced with role and label
- [ ] State change after send announced
- [ ] No phantom announcements or stale regions

**Notes:** _____________ (Eric fills in: any deviations or "clean")

---

## Sign-off

- [ ] All three checks completed (or explicitly deferred with tracking note in `_plans/post-1.10-followups.md`)
- [ ] Phase 2.5 a11y smoke evidence committed
- [ ] Cutover sign-off date: _____________
```

**Tone & voice:**

- Operator-internal. Match the phase02 cutover notes tone (factual, blameless).
- Bracketed `_____________` placeholders match prior precedent.
- No exclamation points.

**Guardrails (DO NOT):**

- Do NOT pre-fill the "Findings summary" or "Notes" sections with synthesized observations.
- Do NOT pre-check any of the checkboxes — they're for Eric to mark.
- Do NOT remove the "PLACEHOLDER" status header until Eric replaces the body during execution.

**Verification:**

- [ ] File exists at `_cutover-evidence/phase02-5-a11y-notes.md`.
- [ ] File contains the "PLACEHOLDER" status header.
- [ ] File contains all three sections (axe-core scan, keyboard walkthrough, VoiceOver spot-check) with checkboxes.
- [ ] File contains a Sign-off section with bracketed placeholder for cutover date.
- [ ] No checkboxes are pre-checked.
- [ ] No exclamation points anywhere in the file.
- [ ] Section headers reference Phase 2.5 flows (friends panel + encouragement-send), not Phase 2's flows.

**Test specifications:**

| Test | Type | Description |
|------|------|-------------|
| (none) | — | Placeholder evidence file, no tests. |

**Expected state after completion:**

- [ ] `_cutover-evidence/phase02-5-a11y-notes.md` exists as a clearly-marked placeholder.

---

### Step 5: Build verification

**Objective:** Confirm the flag flips don't break the production build. The flips are two string changes in `.env.example`, but Vite reads `.env.example` only as a documentation file (not as runtime config). Verifying the build passes is belt-and-suspenders for Rule 10 (performance) and the spec's "Build still succeeds" acceptance criterion.

**Files to create/modify:** None (verification only).

**Details:**

Run from `frontend/`:

```
pnpm build
```

Expected: clean build with no new errors. Bundle size should be unchanged or trivially different (the flag flips are two single-string changes in `.env.example`, which Vite does not read for runtime config).

**Note on `.env.local`:** CC does NOT touch `.env.local` (gitignored). If Eric's `.env.local` does not have these flags set, his local dev build defaults to whatever Vite auto-loads at build time. This is fine for production build verification — `pnpm build` does not depend on `.env.local`.

**Guardrails (DO NOT):**

- Do NOT modify `.env.local` (gitignored; Eric controls his own).
- Do NOT clear caches with `rm -rf node_modules` or similar — full re-install is unnecessary and slow.
- Do NOT skip this step. Verifying a clean build catches surprises early.

**Verification:**

- [ ] `pnpm build` exits 0.
- [ ] No NEW TypeScript errors compared to the pre-flip baseline (any pre-existing errors are out of scope).
- [ ] No NEW build warnings flagged as errors.
- [ ] Dist output produced under `frontend/dist/`.

**Test specifications:**

| Test | Type | Description |
|------|------|-------------|
| (none) | — | Build is the verification, not a test. |

**Expected state after completion:**

- [ ] `pnpm build` clean.

---

### Step 6: Vitest baseline verification

**Objective:** Confirm Vitest passes at the documented post-2.5.4b baseline with both flags set to `true`. Existing helper tests in `frontend/src/lib/__tests__/env.test.ts` cover `isBackendFriendsEnabled` and `isBackendSocialEnabled` across all string-equality cases (including `'true'`, `'false'`, `''`, `undefined`, `'TRUE'`, `'1'`, `'yes'`) — they are flag-state-agnostic and continue to pass after the flips. Spec 2.5.4 and 2.5.4b dual-write tests run both flag states explicitly via `vi.stubEnv`, so they're also unaffected by the `.env.example` changes.

**Files to create/modify:** None (verification only).

**Details:**

Run from `frontend/`:

```
pnpm test
```

Expected: baseline matches post-2.5.4b count (CLAUDE.md "Build Health" section anchors at 8,811 pass / 11 known-fail; the post-2.5.4b count is higher — recon during execution should capture the actual current baseline). Per CLAUDE.md, "Any NEW failing file or fail count > 11 after a Forums Wave spec lands is a regression." Spec 2.9's plan observed 8973/14 mid-Phase-2; Phase 2.5.x specs may have moved the baseline further — Eric records the actual observed baseline in the Execution Log during execution.

**Guardrails (DO NOT):**

- Do NOT add new test files for the flag flips.
- Do NOT modify existing test files in this spec (per spec § "Files NOT to modify").
- Do NOT change the Vitest config to suppress known failures — they remain visible.

**Verification:**

- [ ] `pnpm test` completes without crashing.
- [ ] Pass count is at or above the post-2.5.4b baseline.
- [ ] No NEW failing files compared to the post-2.5.4b baseline.
- [ ] Specifically: `frontend/src/hooks/__tests__/useFriends.test.ts` (or wherever 2.5.4 tests live) and the 2.5.4b test files all pass at both flag states (these tests use `vi.stubEnv` and don't depend on `.env.example`).

**Test specifications:**

| Test | Type | Description |
|------|------|-------------|
| (none) | — | Test suite is the verification. |

**Expected state after completion:**

- [ ] Vitest passes at or above the post-2.5.4b baseline.
- [ ] All Phase 2.5 deliverables (Specs 2.5.1–2.5.4b) unchanged.
- [ ] All acceptance criteria from spec § "Acceptance Criteria" are met.

---

## Step Dependency Map

| Step | Depends On | Description |
|------|------------|-------------|
| 1 | — | Flip both `VITE_USE_BACKEND_FRIENDS` and `VITE_USE_BACKEND_SOCIAL` defaults atomically in `frontend/.env.example` |
| 2 | — | Author `_plans/forums/phase02-5-cutover-checklist.md` |
| 3 | — | Create `_cutover-evidence/phase02-5-a11y-smoke.json` placeholder |
| 4 | — | Create `_cutover-evidence/phase02-5-a11y-notes.md` placeholder |
| 5 | 1 | Build verification (`pnpm build`) — must run AFTER Step 1 because the flag flips are what we're verifying don't break the build |
| 6 | 1 | Vitest baseline verification (`pnpm test`) — must run AFTER Step 1 for the same reason |

Steps 1–4 have no dependencies on each other and could in theory run in any order; Step 1 is listed first because it is the spec's "actual code change" while 2–4 are documentation. Steps 5 and 6 are independent of each other (build and test both run from `frontend/` and don't share state) but per project convention, Step 5 (build) runs before Step 6 (test) so test errors aren't conflated with build errors.

---

## Execution Log

| Step | Title | Status | Completion Date | Notes / Actual Files |
|------|-------|--------|-----------------|----------------------|
| 1 | Flip both flag defaults atomically | [COMPLETE] | 2026-04-27 | Modified `frontend/.env.example` lines 32–39: both flags flipped to `true`, both comment blocks updated to "Activated by Spec 2.5.5 cutover on 2026-04-27." All 8 grep verification checks passed. |
| 2 | Author `_plans/forums/phase02-5-cutover-checklist.md` | [COMPLETE] | 2026-04-27 | Created 17.9 KB checklist with all 11 H2 sections, all 8 friend/social mutation smokes, all 3 milestone smokes, 5 SQL queries + helper, 9-step rollback (Step 9 explicitly "Do NOT revert"), sign-off with PARTIAL 6/8 status. Zero exclamation points. |
| 3 | Create `_cutover-evidence/phase02-5-a11y-smoke.json` placeholder | [COMPLETE] | 2026-04-27 | 733-byte JSON placeholder with `_placeholder` key referencing `capture-axe-evidence.mjs`. Zero real axe-output keys (no `violations`/`passes`/`inapplicable`/`incomplete`). Parses as valid JSON. |
| 4 | Create `_cutover-evidence/phase02-5-a11y-notes.md` placeholder | [COMPLETE] | 2026-04-27 | 2.5 KB notes.md placeholder with PLACEHOLDER status header, all 5 H2 sections (Scope + 3 numbered checks + Sign-off), zero pre-checked boxes, zero exclamation points, references the Phase 2.5 flows (friends panel + encouragement-send). |
| 5 | Build verification (`pnpm build`) | [COMPLETE] | 2026-04-27 | `pnpm build` exits 0. Built in 10.84s + 115ms (service worker). PWA precache: 341 entries (17,600 KiB). The chunk-size warning for large Bible JSON chunks (psalms, isaiah, jeremiah, ezekiel) is pre-existing and unrelated to this spec — flag flips do not affect bundle output. |
| 6 | Vitest baseline verification (`pnpm test`) | [COMPLETE] | 2026-04-27 | Observed baseline: 698 test files / 9,051 passing tests. Run 1 surfaced 1 flake in `src/hooks/__tests__/useNotifications.test.ts > "returns notifications sorted newest first (mock data order)"` — a 1ms `Date.now()` ordering race in mock data construction, structurally unrelated to env-flag changes. Run 2 (full re-run) passed 9,051 / 698 with zero failures. Failure count remains well below the 11-known-fail CLAUDE.md anchor. The 2.5.4 / 2.5.4b hook tests (`useFriends`, `useSocialInteractions`) all pass — those tests use `vi.stubEnv` and don't read `.env.example`. Not a regression. |
