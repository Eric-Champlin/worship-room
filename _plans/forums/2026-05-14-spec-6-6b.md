# Forums Wave Plan: Spec 6.6b — Answered Wall Drift Remediation

**Spec:** `_specs/forums/spec-6-6b.md`
**Master Plan:** `_forums_master_plan/round3-master-plan.md` → Spec 6.6 (`round3-phase06-spec06-answered-wall`, lines ~5625-5720). 6.6b has no independent stub — it brings shipped 6.6 into line with the live stub.
**Date:** 2026-05-14
**Branch:** `forums-wave-continued` (CC never alters git state; Phase 6 specs accumulate on this branch)
**Spec ID:** `round3-phase06-spec06b-answered-wall-drift-remediation`
**Phase:** 6 (Slow Sanctuary / Quiet Engagement Loop) — follow-up remediation
**Size:** L (Large)
**Risk:** Medium-High — second permanent-MD5 CHECK migration on `post_reactions` (table already carries 6.6's behavior fix), new reaction type wired through the full stack, load-bearing anti-pressure copy

---

## Affected Frontend Routes

- `/prayer-wall/answered` (PRIMARY — hero subhead fix, intro paragraph, AnsweredCategoryFilter chips, Celebrate reaction, missing-text fallback, Share-an-update + Un-mark affordances)
- `/prayer-wall/answered?category=health` (and `?category=family|work|grief|gratitude`) — chip-driven filter URL
- `/prayer-wall/:id` (answered post detail uses `AnsweredCard` variant — Celebrate surfaces here)
- `/prayer-wall/user/:id` (user profile's answered posts — Celebrate surfaces here)
- `/prayer-wall/dashboard` (user's own answered prayers — Share-an-update + Un-mark affordances on author's own posts; Celebrate available)

---

## PRE-EXECUTE STOP — Fifth-Gap Decision Required

Brief Section 12 explicitly anticipated a potential **fifth gap area** beyond the four documented and required STOP+surface to Eric at plan-recon if found:

> "Comments-on-answered-posts copy/placeholder changes — the live stub mentions a softer commenter guidance + 'Say a word of celebration...' placeholder; if shipped 6.6 already handled comments, leave as-is; if it did NOT and the live stub requires it, that is a *fifth gap* — flag it to Eric at plan-recon rather than silently absorbing or silently dropping it."

**Plan-recon finding (verified on disk 2026-05-14):** Shipped `frontend/src/components/prayer-wall/CommentInput.tsx:112` carries the hardcoded placeholder `"Write a comment..."` with no `isAnswered`-aware variant. There is no answered-post-specific commenter guidance copy anywhere in the prayer-wall component tree (grep for `"celebration"`, `"Say a word"`, `isAnswered.*placeholder`, `placeholder.*answered` all return zero matches on non-test files).

**Eric must decide BEFORE execute begins** (one of three):

1. **In scope for 6.6b** — add an answered-aware placeholder variant to `CommentInput.tsx` (e.g., "Say a word of celebration…" when the parent post is `isAnswered`). Adds ~2 frontend steps + 1 test. This plan is structured so step insertion is local.
2. **Defer to a follow-up spec (6.6c or future)** — explicitly note as out-of-scope, ship 6.6b without it. The Comments-on-answered copy gap remains documented in `spec-tracker.md`.
3. **Drop entirely** — the brief deliberately scoped 6.6b to four gaps; the comment-placeholder is a softer "live stub mentions" not a "live stub requires" item. Eric can confirm 6.6 is correct as shipped on the comments side.

**The plan body below assumes option (2) — defer.** If Eric chooses (1), insert a Step "23a — CommentInput answered-aware placeholder" between Step 23 and Step 24 with: modify `CommentInput.tsx` to accept an `isAnsweredPost?: boolean` prop, plumb through from `CommentsSection`/`PrayerCard`, add copy to `answered-wall-copy.ts`, add a vitest assertion. If Eric chooses (3), no change to the plan body.

**Execute MUST NOT begin until Eric has acknowledged this decision.** Record the choice in the Execution Log under the heading "Fifth-Gap Decision" before Step 1.

---

## Universal Rules Checklist (Forums Wave)

All 17 Universal Rules from `_forums_master_plan/round3-master-plan.md`:

- [x] **Rule 1: No git operations by CC.** ✅ Plan does no git; Eric handles all git operations manually.
- [x] **Rule 2: Master Plan Quick Reference read.** ✅ Live Spec 6.6 stub (lines ~5625-5720) + Phase 3 Execution Reality Addendum item 5 (Caffeine-bounded buckets), item 3 (`@Modifying` flags), and the 12 items applicable to backend-touching plans all read before drafting.
- [x] **Rule 3: Liquibase filenames + rollback + no raw SQL/ddl-auto.** ✅ Two NEW changesets — `2026-05-14-003-add-celebrate-to-post-reactions-check.xml` (DROP+ADD CHECK) and `2026-05-14-004-add-posts-celebrate-count-column.xml` (addColumn). Both have rollback blocks. Append-only — never edits 001/002/016.
- [x] **Rule 4: OpenAPI EXTENDED at `backend/src/main/resources/openapi.yaml`.** ✅ Five enum/description sites widened to `[praying, candle, praising, celebrate]`; `celebrateCount` added to `PostDto` and `ToggleReactionResponse` schemas. NOT created at `backend/api/openapi.yaml`. Frontend types regen via `openapi-typescript` runs after the YAML edit per existing pipeline.
- [x] **Rule 5: User-facing strings in Copy Deck.** ✅ All new copy (subhead replacement, intro paragraph, fallback, count format, chip labels, "Share an update", "Un-mark as answered", MH rationale comment) lives in `frontend/src/constants/answered-wall-copy.ts` per the existing i18n-ready convention.
- [x] **Rule 6: Tests mandatory.** ✅ 24 new tests planned per Section 9 of the spec (T1–T24 + T-E2E-1/2).
- [x] **Rule 7: New `wr_*` localStorage keys.** ✅ N/A — no new localStorage keys. Existing `wr_prayer_reactions` shape extends additively (default-fill `isCelebrating: false`) per the documented 2-stage migration; 6.6b adds a 3rd stage. Update `11-local-storage-keys.md` ‐> `wr_prayer_reactions` entry to mention the 3rd-stage migration.
- [x] **Rule 8: BB-45 anti-pattern forbidden.** ✅ `usePrayerReactions` is **Pattern A (subscription via standalone hook with `useSyncExternalStore`)** — extending with `toggleCelebrate` does not change the pattern. Cross-mount subscription test for celebrate is mandatory (T-A1 in test specs).
- [x] **Rule 9: Accessibility not optional.** ✅ Chip row keyboard-reachable + state-announcing; `<h2>` subhead landmark inside `<main>`; Celebrate animation respects `prefers-reduced-motion` (global rule in `frontend/src/styles/animations.css`); WCAG AA contrast on warm sunrise palette on Night Mode + regular backgrounds.
- [x] **Rule 10: Performance not optional.** ✅ Redis cache for `sort=answered` (MPD-5); animation tokens from `frontend/src/constants/animation.ts` (no hardcoded ms); bundle regression ≤50 KB expected (one new chip-row component + one new reaction component).
- [x] **Rule 11: Brand voice mandatory.** ✅ Subhead "Gratitude, not comparison." is the load-bearing line; intro paragraph + fallback + MH rationale all pass pastor's-wife test per spec D-Copy.
- [x] **Rule 12: Anti-pressure design.** ✅ Mental Health chip deliberately omitted with rationale embedded in code (Gate-G-MH-OMISSION); no category counts/trending/most-celebrated affordances (D-NoNewAntiPatterns).
- [x] **Rule 13: Crisis detection supersession.** ⚠️ Documented in ED-2: `answered_text` is NOT crisis-scanned at create time today (R3 finding). Per W29's conditional ("if crisis-scanned at creation, edit must scan too") this is satisfied by documenting why edits also do not scan. Adding scan would be scope creep beyond the four gap areas (Brief Section 0). A future spec can add it for both create and edit paths together.
- [x] **Rule 14: Plain text only.** ✅ `answered_text` already routes through `htmlSanitizerPolicy.sanitize().trim()` in `PostService.updatePost`. No HTML, no Markdown, no `dangerouslySetInnerHTML` introduced. Render via `whitespace-pre-wrap` (existing pattern on `PrayerCard` answered region).
- [x] **Rule 15: Rate limiting on all writes + reads.** ✅ Celebrate reaction toggle rides existing `ReactionsRateLimitService` (Caffeine-bounded). Answered-text edit + un-mark ride existing PATCH `/api/v1/posts/{id}` which uses the post-update rate limit (already shipped). No new rate-limit configs needed.
- [x] **Rule 16: Respect existing patterns.** ✅ Extends shipped `AnsweredWall.tsx`, `AnsweredCard.tsx`, `PrayerCard.tsx`, `InteractionBar.tsx`, `MarkAsAnsweredForm.tsx`, `usePrayerReactions`, `reactionsStore.ts`, `PostService`, `ReactionWriteService`. No new parallel surfaces (Gate-G-EXTEND-NOT-DUPLICATE).
- [x] **Rule 17: Per-phase cutover specs require accessibility smoke test evidence.** N/A — 6.6b is a remediation spec, not a cutover spec. `/verify-with-playwright` covers axe-core verification on `/prayer-wall/answered`.

---

## Architecture Context

### Shipped 6.6 state (verified on disk 2026-05-14)

- **`backend/src/main/resources/db/changelog/2026-04-27-016-create-post-reactions-table.xml`** (Spec 3.1) — table with composite PK `(post_id, user_id, reaction_type)`, original CHECK `('praying','candle')`, FK CASCADE on post and user delete.
- **`2026-05-14-001-add-praising-to-post-reactions-check.xml`** (Spec 6.6) — DROP+ADD CHECK to `('praying','candle','praising')`. Append-only. R8 confirmed approach: `DROP CONSTRAINT post_reactions_reaction_type_check; ADD CONSTRAINT … CHECK (reaction_type IN (...))`. Rollback reverts to `('praying','candle')`.
- **`2026-05-14-002-add-posts-praising-count-column.xml`** (Spec 6.6) — `addColumn` of `praising_count INTEGER NOT NULL DEFAULT 0`. Mirror for celebrate.
- **`backend/src/main/java/com/worshiproom/post/Post.java`** — entity with `prayingCount`, `candleCount`, `praisingCount`, `commentCount`, `bookmarkCount`, `reportCount`, `crisisFlag`, `isAnswered`, `answeredText`, `answeredAt`, `isDeleted`, etc.
- **`backend/src/main/java/com/worshiproom/post/engagement/ReactionWriteService.java`** — toggle/remove with INTERCESSION-on-praying-only correction (lines 131–136 are load-bearing). Three constants `PRAYING`, `CANDLE`, `PRAISING`. `incrementReactionCounter` / `decrementReactionCounter` dispatch on `reactionType` string.
- **`backend/src/main/java/com/worshiproom/post/PostController.java`** — `@Pattern(regexp = "^(praying|candle|praising)$")` on the DELETE query param (line 276); `@Pattern` in DTOs `CreatePostRequest` / `UpdatePostRequest` allows 10 category values including `health`/`mental-health`/`family`/`work`/`grief`/`gratitude`/`praise`/`relationships`/`other`/`discussion`; `VALID_CATEGORIES` Set at line 55 mirrors the same.
- **`backend/src/main/java/com/worshiproom/post/PostService.java`** — `listFeed` accepts `?category=` (line 118 `byCategory` spec); `sort=answered` branch (line 693) sorts by `answeredAt DESC`; `updatePost` already handles `isAnswered` true↔false transitions AND `answeredText` edits with no time-window restriction (lines 506–526). Crisis re-detection runs on content edit only (line 481) — NOT on answered_text.
- **`backend/src/main/java/com/worshiproom/post/PostSpecifications.java`** — `visibleTo(viewerId)` is the canonical visibility predicate; excludes soft-deleted posts (`isDeleted=true`) but does NOT filter on author user `isDeleted`/`isBanned`. `byCategory(category)` is the existing category filter.
- **`backend/src/main/java/com/worshiproom/cache/CacheConfig.java`** — `@EnableCaching` on, profile-aware: dev/test → `ConcurrentMapCacheManager`, prod → Redis wrapped in `CircuitBreakingCacheManager` (3 failures → 30s bypass). TTL via `spring.cache.redis.time-to-live.<cacheName>`. Spec 6.1 added the first `@Cacheable("prayer-receipt")`; 6.6b will be the second consumer (`@Cacheable("answered-feed")`).
- **`backend/src/main/resources/openapi.yaml`** — five enum sites at `(praying|candle|praising)` to extend; `PostDto` schema has `praisingCount` (4243); `ToggleReactionResponse` schema has `praisingCount` (4723).
- **`frontend/src/pages/AnsweredWall.tsx`** — uses `BackgroundCanvas` + `Navbar transparent` + `PrayerWallViewTabs` + InteractionBar; line 88 SEO `description` and line 109 paragraph both render the wrong subhead string `'Prayers the community has watched God move in.'`. The wrong string ALSO sits at `frontend/src/constants/answered-wall-copy.ts:21`. Three locations to update for MPD-1.
- **`frontend/src/components/prayer-wall/AnsweredCard.tsx`** — thin wrapper around `PrayerCard` with `answeredVariant={true}`; per Gate-G-CARD-NO-FORK adds no internals. The missing-text fallback + Share-an-update + Un-mark live inside `PrayerCard`'s `answeredVariant` block (lines 278–301) — NOT inside `AnsweredCard.tsx` itself.
- **`frontend/src/components/prayer-wall/PrayerCard.tsx:278-308`** — answered region currently renders `prayer.answeredText && <p>` (silent when null) and `prayer.answeredAt && <p>`. Missing-text fallback + Share-an-update + Un-mark all hook here.
- **`frontend/src/components/prayer-wall/InteractionBar.tsx:240-264`** — `showPraising` prop opt-in for the Praising button (Spec 6.6). Light-a-Candle is NOT surfaced in `InteractionBar` today; Celebrate is functionally a NEW button alongside Praising on answered posts (the brief's "swap" framing is conceptual). Mirror the `showPraising` pattern with `showCelebrate` + `onToggleCelebrate`.
- **`frontend/src/components/prayer-wall/MarkAsAnsweredForm.tsx`** — already supports OPTIONAL `praiseText` at the form level (line 22 `onConfirm(praiseText.trim())` — accepts empty string). The "Share an update" affordance on an already-answered post is NEW (no edit mode today). The un-mark affordance is also NEW.
- **`frontend/src/types/api/prayer-wall.ts:42`** — `ReactionTypeApi = 'praying' | 'candle' | 'praising'`; `PostDto.praisingCount` (line 101); `MyReactionRow.isPraising` (167); `ToggleReactionRequest.reactionType` (178); `ToggleReactionResponse.reactionType` (182).
- **`frontend/src/types/prayer-wall.ts:67,116-119`** — `PrayerRequest.praisingCount?`; `PrayerReaction.isPraising` boolean.
- **`frontend/src/lib/prayer-wall/reactionsStore.ts`** — Spec 3.7 + 6.6 migration: default-fills `isCandle` and `isPraising` on hydrate when missing. Three-stage migration extension for 6.6b: post-6.6-pre-6.6b shape (5 fields with `isPraising`) → default-fill `isCelebrating: false`.
- **`frontend/src/hooks/usePrayerReactions.ts`** — exposes `togglePraying`, `toggleBookmark`, `toggleCandle`, `togglePraising` via `useSyncExternalStore` (Pattern A). Extend with `toggleCelebrate`.

### Package conventions

- Backend new code in `com.worshiproom.post` and `com.worshiproom.post.engagement` (existing).
- Frontend new components in `frontend/src/components/prayer-wall/`; new copy in `frontend/src/constants/answered-wall-copy.ts`; reactive store + hook stay where they are.
- Test files colocated: backend in `backend/src/test/java/com/worshiproom/post/...`; frontend in `frontend/src/components/prayer-wall/__tests__/` and `frontend/src/hooks/__tests__/`.

---

## Database Changes

| Change | Table | Liquibase Changeset | Notes |
|--------|-------|-------------------|-------|
| ALTER CHECK constraint (DROP+ADD) | `post_reactions` | `2026-05-14-003-add-celebrate-to-post-reactions-check.xml` | Widen `reaction_type` from `('praying','candle','praising')` to `('praying','candle','praising','celebrate')`. NEW changeset. APPEND-ONLY — never edits `001` or `016` (W1/W2). Same DROP+ADD pattern R8 confirmed `001` used. Rollback reverts to `('praying','candle','praising')`. |
| ADD COLUMN | `posts` | `2026-05-14-004-add-posts-celebrate-count-column.xml` | `celebrate_count INTEGER NOT NULL DEFAULT 0`. Mirrors `2026-05-14-002` (`praising_count`) verbatim. No backfill needed (answered prayers have zero celebrate reactions until 6.6b ships; DEFAULT 0 on existing rows is correct). Rollback drops the column. |

**Migration safety:**
- Both changesets are append-only and idempotent (Liquibase tracks by MD5; once run, never re-runs).
- Run order: `003` before `004` (Liquibase orders alphabetically by filename). They are independent — either order works, but conventional ordering matches their dependence on the existing 6.6 changesets (`001` widens reaction_type; `002` adds praising_count column).
- Both safe against a populated `post_reactions` / `posts` table (mock seed has rows).
- Filename uniqueness verified: `ls backend/src/main/resources/db/changelog/ | grep '^2026-05-14-'` returns `001` and `002`; `003` and `004` are new.

---

## API Changes

| Method | Endpoint | Auth | Rate Limit | Request Body | Response | Change |
|--------|----------|------|-----------|-------------|----------|--------|
| POST | `/api/v1/posts/{id}/reactions` | Required (JWT) | Existing reactions limit | `{ reactionType: "celebrate" }` | `{ data: { reactionType, state, prayingCount, candleCount, praisingCount, celebrateCount }, meta }` | EXTEND: enum widens to add `celebrate`; response gains `celebrateCount`. |
| DELETE | `/api/v1/posts/{id}/reactions?reactionType=celebrate` | Required (JWT) | Existing reactions limit | — | `204 No Content` | EXTEND: query-param `@Pattern` widens to allow `celebrate`. |
| GET | `/api/v1/posts?sort=answered&category=health&page=1&limit=20` | Optional | Existing read limit | — | `{ data: [PostDto], meta: { page, limit, total, requestId } }` with `Cache-Control: private, no-store` (existing) | EXTEND: `?category=` already accepted (no change); behaviorally widens to apply to `sort=answered`; PostDto gains `celebrateCount`; Redis cache layer added (see ED-5). Author-deleted-or-banned posts excluded (new predicate, scoped to `sort=answered`). |
| PATCH | `/api/v1/posts/{id}` | Required (JWT) | Existing post-update limit | `{ isAnswered: false }` (un-mark) OR `{ answeredText: "..." }` (edit) OR `{ isAnswered: true, answeredText: "..." }` (mark with text) | `{ data: PostDto, meta }` | NO change to backend — shipped logic already handles all three. `celebrateCount` appears in the returned `PostDto`. |

---

## Assumptions & Pre-Execution Checklist

- [ ] **Fifth-Gap Decision recorded** (see PRE-EXECUTE STOP above). Eric chose option (1)/(2)/(3) and the choice is in the Execution Log before Step 1.
- [ ] **Plan-Time Divergence #1 (cache architecture) signed off** by Eric. See ED-5 below. Pick option (a) or (b) before Step 14.
- [ ] **Plan-Time Divergence #2 (answered_text crisis-scan deferral) acknowledged** by Eric. See ED-2 below. Default is "don't add, document why" per scope discipline.
- [ ] Prerequisite specs complete: 6.6 ✅ shipped; 6.1 ✅; 5.6 ✅ Redis available (R6 verified — `CacheConfig.java`, `RedisConfig.java`, `CircuitBreakingCacheManager.java` present).
- [ ] Docker is running (`docker-compose up -d` for PostgreSQL + Redis if not already).
- [ ] Backend compiles cleanly: `./mvnw compile` from `backend/`.
- [ ] Backend tests baseline pass: `./mvnw test` from `backend/`.
- [ ] Frontend builds cleanly: `pnpm build` from `frontend/`.
- [ ] Frontend tests baseline pass: `pnpm test --run` from `frontend/`.
- [ ] No Liquibase filename collisions: `ls backend/src/main/resources/db/changelog/ | grep '^2026-05-14-'` returns exactly `001`, `002` — `003` and `004` are new.
- [ ] Spec-time recon confirmed all 6 plan-recon items (R3–R8) on disk; findings folded into Architecture Context above.
- [ ] Working tree is clean except for any pending 6.6b work (CC verifies via `git status` read-only at execute-start).

---

## Spec-Category-Specific Guidance

**This is a remediation spec — a hybrid backend + frontend, NOT a dual-write or cutover spec.** It extends a shipped feature (6.6) by closing four documented gap areas. Apply the BACKEND-ONLY discipline to the backend changes (Liquibase, Phase 3 Addendum gates) and the FRONTEND-ONLY discipline to the UI changes (FrostedCard tiers, animation tokens, reactive store consumer pattern). No flag flip, no smoke test of a phase boundary, no cutover evidence required.

---

## Edge Cases & Decisions

| Decision | Choice | Rationale |
|----------|--------|-----------|
| **ED-1: `praise`/`relationships`/`other`/`discussion` category posts on the Answered Wall** | Roll into "All" only — NOT exposed as chips. Matches brief + live stub chip list. `discussion` posts cannot be `is_answered=true` (post-type taxonomy — only `prayer_request` and `testimony` participate in the answered flow today). | S2 resolution path (a). Brief is unambiguous about exactly five chips + the MH omission. Adding extra chips would violate Gate-G-MH-OMISSION's "exactly the five chips" framing. The four "unfilterable" categories still appear in the unfiltered "All" view. |
| **ED-2: Crisis detection on `answered_text` writes (W29 / Gate-10)** | NOT added in 6.6b. Document the gap. | Per Brief Section 0: "6.6b adds NOTHING beyond what the live 6.6 stub already mandates." R3 confirmed `answered_text` is NOT crisis-scanned at create time today (`PostService.createPost` does not include `answeredText` in the `detectionInput`). W29 is conditional: "If crisis-scanned at creation, edit must scan too." Since creation does not scan, edit also does not scan. Gate-10 is satisfied by documenting why. **A future spec can add it for both create and edit paths together.** Filed for `_plans/post-1.10-followups.md` or equivalent. |
| **ED-3: Deleted-author exclusion (MPD-6 #2)** | NEW `PostSpecifications.authorActive()` clause, JOIN to `users`, exclude posts where `users.is_deleted=true` OR `users.is_banned=true`. Applied ONLY in the `sort=answered` branch of `PostService.listFeed` via `.and(authorActive())`. | Do NOT extend `visibleTo()` — that predicate is consumed by every feed query in the post family (R7 confirmed at PostSpecifications.java:32–92). Extending it would ripple to main feed, profile feeds, prayer-receipt visibility, etc. — exactly the cross-spec drift Universal Rule 16 + Gate-G-EXTEND-NOT-DUPLICATE forbid. The brief scopes the exclusion to the answered feed; scope the change to it. |
| **ED-4: Expired-but-answered inclusion (MPD-6 #1)** | No change required at the SQL level. `PostSpecifications.visibleTo()` does not currently apply an `expires_at` predicate (Prayer Request expiry is handled elsewhere — likely a future Phase 6 spec). The `sort=answered` branch already does NOT inherit any expiry filter. Add an integration test that creates an expired-but-answered prayer and asserts it appears in the feed (T21 lock-in). | The brief says "INCLUDE expired-but-answered" — current code already does. The test is the regression guard against any future spec accidentally adding `expires_at > NOW()` to `visibleTo()`. |
| **ED-5: Redis cache architecture for the answered feed (MPD-5)** | ⚠️ **Plan-Time Divergence #1 — Eric sign-off required.** Two defensible readings of "logged-in requests not cached per-user": **(a) Cache only when `principal == null`** (unauthenticated): simplest, no risk of friend-classification leak, less value. **(b) Cache the viewer-agnostic feed query result and enrich `intercessorSummary` per request:** requires factoring `listFeed` so the cache key sits BELOW the per-viewer enrichment. More value, more wiring. Either way: TTL 2 min via `spring.cache.redis.time-to-live.answered-feed=2m`; key includes `category` + `page` (and viewer-state-bucket if (b)); `@CacheEvict` paired with `isAnswered` transitions in `PostService.updatePost` AND with content edits on already-answered posts. | Per master plan Spec 6.5 + the existing `Cache-Control: private, no-store` on `listFeed`: `intercessorSummary` is per-viewer and shared caches WOULD leak friend classification across viewers. `@Cacheable` at the controller or service `listFeed` is therefore the WRONG place — it sits above enrichment. The brief's "logged-in requests not cached per-user" admits both readings; both are defensible; both are valuable; pick at sign-off time. |
| **ED-6: Frontend `wr_prayer_reactions` migration shape** | 3-stage default-fill on hydrate. Pre-3.7 data (3 fields) → default-fill `isCandle` AND `isPraising` AND `isCelebrating`. Post-3.7-pre-6.6 data (4 fields with `isCandle`) → default-fill `isPraising` AND `isCelebrating`. Post-6.6-pre-6.6b data (5 fields with `isPraising`) → default-fill `isCelebrating`. All default `false`; no version key (additive, safe `false` defaults). | Matches the existing 2-stage migration pattern in `reactionsStore.ts:72–84`. Extends mechanically. |
| **ED-7: Category chip selection state persistence** | URL-driven only via `?category=`. NO localStorage write. | Matches shipped 6.6's discipline (no extra `wr_*` keys for ephemeral UI state). The `?category=` query param IS the canonical state. Refreshing preserves it; sharing the URL preserves it; clicking "All" clears it via `setSearchParams({}, { replace: true })`. |
| **ED-8: Celebrate reaction palette + animation** | Warm sunrise palette: gold (`#FDE68A`) + coral (`#FCA5A5`) family. Distinct from Light-a-Candle's amber. Animation: short sparkle burst (~600ms, `decelerate` easing from `ANIMATION_DURATIONS`) on toggle ON. Static fallback under `prefers-reduced-motion` (global rule in `animations.css` disables the keyframe automatically; component renders a static icon). | Spec D-CelebrateReaction is explicit on "warm sunrise" + "distinct from Light-a-Candle". Color choices reviewed against WCAG AA on Night Mode (`bg-hero-bg` #08051A) and the canonical FrostedCard (`bg-white/[0.07]`) — both pass with the chosen text color of `text-amber-200` for inactive / `text-amber-300` for active. |
| **ED-9: Test data for migration safety (W5)** | T4 uses `LiquibaseSmokeTest` extension OR a new `@SpringBootTest` that seeds 4 `post_reactions` rows (one per reaction_type, including `celebrate` AFTER migration runs), then verifies all 4 pass and an invalid value still fails. Reuses `AbstractIntegrationTest`. | Mandatory per W5 — migration must be safe against a populated table. Mock seed data lives in `db/changelog/contexts/dev-seed.xml` (context="dev"). Test seeding uses Testcontainers + `context="test"` per existing convention. |
| **ED-10: Reactions migration test coverage** | T-A1 (new): Cross-mount subscription test for celebrate. Component A mounts and reads `reactions[id].isCelebrating`; component B calls `toggleCelebrate(id)`; assert component A re-renders with new value. Locks the BB-45 pattern compliance per Universal Rule 8. | Pattern A reactive store extension MUST come with this test per `06-testing.md` § "Reactive Store Consumer Pattern". |

---

## Plan-Time Divergences from Brief

| # | Decision | Reason | Reversible? |
|---|---|---|---|
| 1 | **Cache architecture** (ED-5): two options (cache only unauth, OR cache viewer-agnostic + enrich) — Eric picks. Recommended: option (b) for more value, but only if Eric is comfortable with the `listFeed` factoring. | Brief's "logged-in not cached per-user" admits both. `@Cacheable` at the controller is the wrong place because intercessorSummary is per-viewer. Surface for sign-off rather than guess. | Yes — either option can be rebuilt as the other in a future spec. |
| 2 | **Crisis-scan deferral on `answered_text`** (ED-2): Document why, do NOT add scan in 6.6b. | Per Brief Section 0 scope discipline + W29's conditional language. Scan does not exist on create today; therefore not required on edit. Adding would be scope creep; a future spec can do both paths atomically. | Yes — a future spec adds scan to both paths in one change. |
| 3 | **Fifth-gap (CommentInput placeholder)** (PRE-EXECUTE STOP above): Eric chooses scope. | Brief Section 12 explicitly defers this to Eric. Plan-recon found no answered-aware placeholder logic on disk. | Yes — option (1) is reversible (remove the prop later); option (2)/(3) leave the gap documented for a future spec. |

If empty (after Eric signs off on all three), this section reduces to "no remaining plan-time divergences from brief." Document each sign-off in the Execution Log when received.

---

## Implementation Steps

### Step 1: Liquibase changeset — widen `post_reactions.reaction_type` CHECK to add `'celebrate'`

**Objective:** Make `'celebrate'` a valid value at the DB level.

**Files to create:**
- `backend/src/main/resources/db/changelog/2026-05-14-003-add-celebrate-to-post-reactions-check.xml`

**Files to modify:**
- `backend/src/main/resources/db/changelog/master.xml` — add `<include file="2026-05-14-003-add-celebrate-to-post-reactions-check.xml" relativeToChangelogFile="true"/>` after the `001` include.

**Details:**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

  <!-- Forums Wave Spec 6.6b — drift remediation.

       Widens post_reactions.reaction_type CHECK to add 'celebrate'.
       Spec 6.6 widened to add 'praising' in 2026-05-14-001 and explicitly
       deferred 'celebrate'. The live Spec 6.6 master plan stub always
       wanted celebrate; the original 6.6 brief omitted it. 6.6b closes
       that gap.

       APPEND-ONLY: NEVER edit 2026-05-14-001 or 2026-04-27-016. Their
       MD5s are permanent in DATABASECHANGELOG.

       PATTERN: DROP + ADD CONSTRAINT in a single changeset, matching the
       approach R8 confirmed 2026-05-14-001 used. -->
  <changeSet id="2026-05-14-003-add-celebrate-to-post-reactions-check" author="worship-room">

    <sql>
      ALTER TABLE post_reactions DROP CONSTRAINT post_reactions_reaction_type_check;
      ALTER TABLE post_reactions ADD CONSTRAINT post_reactions_reaction_type_check
      CHECK (reaction_type IN ('praying', 'candle', 'praising', 'celebrate'));
    </sql>

    <rollback>
      <sql>
        ALTER TABLE post_reactions DROP CONSTRAINT post_reactions_reaction_type_check;
        ALTER TABLE post_reactions ADD CONSTRAINT post_reactions_reaction_type_check
        CHECK (reaction_type IN ('praying', 'candle', 'praising'));
      </sql>
    </rollback>

  </changeSet>
</databaseChangeLog>
```

**Guardrails (DO NOT):**
- Edit `2026-05-14-001-add-praising-to-post-reactions-check.xml`. Its MD5 is permanent (W1).
- Edit `2026-04-27-016-create-post-reactions-table.xml`. Its MD5 is permanent (W2).
- Drop any of the existing values (`praying`, `candle`, `praising`) — all four must remain in the new CHECK (W3).
- Use any pattern other than DROP+ADD inside a single `<sql>` block (W4).
- Skip the rollback block (W6).

**Verification:**
- [ ] `./mvnw compile` from `backend/` passes.
- [ ] `LiquibaseSmokeTest` passes — verifies the master changelog applies cleanly to a fresh Testcontainers PostgreSQL.
- [ ] `ls backend/src/main/resources/db/changelog/2026-05-14-*` returns 001, 002, 003 (and 004 after Step 2).

**Test specifications:** None yet — schema tests live in Step 11 (T1–T4).

**Expected state after completion:**
- [ ] New changeset file at the path above with the exact contents above.
- [ ] `master.xml` includes it.
- [ ] No edits to 001 or 016.

---

### Step 2: Liquibase changeset — add `posts.celebrate_count` column

**Objective:** Mirror `praising_count` for the new reaction type. Per-type denormalized counter pattern (R4 finding).

**Files to create:**
- `backend/src/main/resources/db/changelog/2026-05-14-004-add-posts-celebrate-count-column.xml`

**Files to modify:**
- `backend/src/main/resources/db/changelog/master.xml` — add `<include file="2026-05-14-004-add-posts-celebrate-count-column.xml" relativeToChangelogFile="true"/>` after the `003` include.

**Details:** Mirror `2026-05-14-002-add-posts-praising-count-column.xml` verbatim, substituting `celebrate_count` for `praising_count`. `INTEGER NOT NULL DEFAULT 0`. No backfill (existing rows correctly default to 0; no celebrate reactions exist yet). Rollback drops the column.

**Guardrails (DO NOT):**
- Edit `2026-05-14-002-add-posts-praising-count-column.xml`.
- Combine the column add with any other schema change.

**Verification:**
- [ ] `./mvnw compile` passes.
- [ ] `LiquibaseSmokeTest` passes — verifies `posts.celebrate_count` exists with default 0.
- [ ] `psql` against the Testcontainers DB shows `\d posts` includes `celebrate_count integer not null default 0`.

**Expected state after completion:**
- [ ] New changeset file at the path above.
- [ ] `master.xml` includes it.

---

### Step 3: `Post.java` — add `celebrateCount` field

**Objective:** Map the new column into the JPA entity.

**Files to modify:**
- `backend/src/main/java/com/worshiproom/post/Post.java`

**Details:**
1. Add the field after `praisingCount`:
   ```java
   @Column(name = "celebrate_count", nullable = false)
   private int celebrateCount;
   ```
2. Add `public int getCelebrateCount() { return celebrateCount; }` after `getPraisingCount`.
3. Add `public void setCelebrateCount(int celebrateCount) { this.celebrateCount = celebrateCount; }` after `setPraisingCount`.

**Guardrails (DO NOT):**
- Reorder existing fields or getters/setters (preserves diff minimalism per Gate-G-6.6-REGRESSION-SAFE).
- Mark the column `insertable=false, updatable=false` — Spec 6.6's `praising_count` is NOT (default behavior is fine; counter mutations route through SQL-side `@Modifying` UPDATE per Phase 3 Addendum item 5, not through entity save).

**Verification:**
- [ ] `./mvnw compile` passes.
- [ ] `./mvnw test -Dtest=PostRepositoryTest` (or the existing integration tests over Post) pass — no JPA mapping errors.

**Expected state after completion:**
- [ ] Field declared with the exact annotation set above.
- [ ] Getter + setter present.

---

### Step 4: `PostRepository.java` — add SQL-side counter mutators for celebrate

**Objective:** Add `incrementCelebrateCount(UUID postId)` and `decrementCelebrateCount(UUID postId)` mirroring the existing `praising_count` methods (Phase 3 Addendum item 3 — `@Modifying(clearAutomatically=true, flushAutomatically=true)`).

**Files to modify:**
- `backend/src/main/java/com/worshiproom/post/PostRepository.java`

**Details:** Find the existing `incrementPraisingCount` / `decrementPraisingCount` methods. Add immediately after:

```java
@Modifying(clearAutomatically = true, flushAutomatically = true)
@Query("UPDATE Post p SET p.celebrateCount = p.celebrateCount + 1 WHERE p.id = :postId")
int incrementCelebrateCount(@Param("postId") UUID postId);

@Modifying(clearAutomatically = true, flushAutomatically = true)
@Query("UPDATE Post p SET p.celebrateCount = p.celebrateCount - 1 WHERE p.id = :postId AND p.celebrateCount > 0")
int decrementCelebrateCount(@Param("postId") UUID postId);
```

**Guardrails (DO NOT):**
- Omit either `clearAutomatically=true` or `flushAutomatically=true` on `@Modifying` (Phase 3 Addendum item 3 — both required).
- Drop the `> 0` guard on decrement (prevents negative drift under race; matches the existing pattern).
- Add a corresponding "set" method (counter updates go through these SQL-side mutators only).

**Verification:**
- [ ] `./mvnw compile` passes.
- [ ] Existing `PostRepositoryTest` (or equivalent integration tests over `incrementPraisingCount`) pass unchanged.

**Expected state after completion:**
- [ ] Two new methods on `PostRepository` mirroring the praising methods exactly in shape.

---

### Step 5: `ReactionWriteService.java` — wire `'celebrate'` into counter dispatch

**Objective:** Add `CELEBRATE` constant; extend `incrementReactionCounter` / `decrementReactionCounter` to handle the new value. Do NOT touch the INTERCESSION branch (Gate-G-CELEBRATE-NO-INTERCESSION).

**Files to modify:**
- `backend/src/main/java/com/worshiproom/post/engagement/ReactionWriteService.java`

**Details:**
1. Add `private static final String CELEBRATE = "celebrate";` after `PRAISING` constant.
2. In `incrementReactionCounter`, add `else if (CELEBRATE.equals(reactionType)) { postRepository.incrementCelebrateCount(postId); }` after the `PRAISING` branch.
3. In `decrementReactionCounter`, add the symmetric branch.
4. Update the Javadoc on the `toggle` method to mention `celebrate` is the fourth reaction type and that it follows the praising/candle precedent (does NOT fire INTERCESSION).
5. **DO NOT TOUCH** the `if (PRAYING.equals(reactionType))` INTERCESSION block at lines 131–136. Add no `else if` branch for celebrate here. The existing comment about INTERCESSION being limited to praying-add is correct and load-bearing per Gate-G-CELEBRATE-NO-INTERCESSION.

**Guardrails (DO NOT):**
- Modify the INTERCESSION conditional (Gate-G-CELEBRATE-NO-INTERCESSION HARD).
- Reorder the dispatch chain.
- Introduce a switch statement — keep the if/else if chain to minimize diff per Gate-G-EXTEND-NOT-DUPLICATE.

**Verification:**
- [ ] `./mvnw compile` passes.
- [ ] All shipped `ReactionWriteServiceTest` / `ReactionWriteIntegrationTest` cases pass unchanged.
- [ ] The three shipped INTERCESSION regression tests (`togglePraying_addPath_firesIntercessionActivity`, `togglePraising_addPath_doesNotFireIntercessionActivity`, `toggleCandle_addPath_doesNotFireIntercessionActivity`) pass with zero assertion modifications (Gate-G-6.6-REGRESSION-SAFE).

**Expected state after completion:**
- [ ] CELEBRATE constant added; both counter dispatch methods extended; INTERCESSION block untouched.

---

### Step 6: `ToggleReactionResponse.java` — add `celebrateCount`

**Objective:** Surface the post-mutation celebrate count in the toggle response so the frontend can render it without a refetch.

**Files to modify:**
- `backend/src/main/java/com/worshiproom/post/engagement/dto/ToggleReactionResponse.java`

**Details:** Add `int celebrateCount` to the record's component list (typically as the last field, after `praisingCount`). Update the constructor call in `ReactionWriteService.toggle` (line 150–156) to pass `refreshed.getCelebrateCount()` after `praisingCount`.

**Guardrails (DO NOT):**
- Reorder existing fields (Gate-G-6.6-REGRESSION-SAFE — frontend mock + integration tests bind by position via generated TypeScript).

**Verification:**
- [ ] `./mvnw compile` passes.
- [ ] All shipped reaction-response tests pass unchanged (they assert on `praisingCount`; the new field's default 0 is benign).

**Expected state after completion:**
- [ ] Record gains `celebrateCount`; `ReactionWriteService.toggle` populates it; existing tests pass.

---

### Step 7: `PostController.java` — widen `reactionType` `@Pattern` regex; update `VALID_CATEGORIES` (NO change — already includes all needed values)

**Objective:** Allow `'celebrate'` at the request validation layer.

**Files to modify:**
- `backend/src/main/java/com/worshiproom/post/PostController.java`

**Details:**
1. Change the DELETE query-param regex at line 276 from `^(praying|candle|praising)$` to `^(praying|candle|praising|celebrate)$`.
2. The POST request body uses `ToggleReactionRequest.reactionType` which is also `@Pattern`-validated — update there too (file path: `backend/src/main/java/com/worshiproom/post/engagement/dto/ToggleReactionRequest.java`).
3. `VALID_CATEGORIES` at PostController.java:55 already includes all 10 category values — NO change needed.

**Guardrails (DO NOT):**
- Loosen the regex anchoring (`^...$` is mandatory — prevents injection of extra values).
- Add a `Set<String>` constant for VALID_REACTION_TYPES — `@Pattern` is the validation surface (Phase 3 Addendum item 5 — validate at controller boundary, not via DB CHECK).

**Verification:**
- [ ] `./mvnw compile` passes.
- [ ] `PostControllerTest` cases pass unchanged.
- [ ] New T-Pattern test (Step 11) asserts `'celebrate'` is accepted and `'invalid'` returns 400.

**Expected state after completion:**
- [ ] Both `@Pattern` annotations updated to the 4-value regex.

---

### Step 8: `PostMapper.java` + `PostDto.java` — surface `celebrateCount` in feed responses

**Objective:** Expose `celebrateCount` in the post DTO so the answered feed and PrayerCard answered-variant can render counts as "{n} praising with you · {n} celebrating".

**Files to modify:**
- `backend/src/main/java/com/worshiproom/post/dto/PostDto.java` — add `int celebrateCount` field (record component) after `praisingCount`.
- `backend/src/main/java/com/worshiproom/post/PostMapper.java` — map `post.getCelebrateCount()` into the DTO constructor.

**Guardrails (DO NOT):**
- Reorder existing fields.

**Verification:**
- [ ] `./mvnw compile` passes.
- [ ] `PostMapperTest` cases pass — `celebrateCount` defaults to 0 on existing fixtures.

**Expected state after completion:**
- [ ] PostDto record extended; mapper populated.

---

### Step 9: `PostSpecifications.java` — add `authorActive()` predicate; `PostService.java` — apply only to `sort=answered`

**Objective:** Exclude posts whose author has `users.is_deleted=true` OR `users.is_banned=true` from the answered feed only (MPD-6 #2 + ED-3).

**Files to modify:**
- `backend/src/main/java/com/worshiproom/post/PostSpecifications.java`
- `backend/src/main/java/com/worshiproom/post/PostService.java`

**Details:**
1. In `PostSpecifications`, add a new static `authorActive()` method that produces a `Specification<Post>` JOINing to `User` (or to a `Subquery` if direct JOIN is awkward) and filtering on `user.isDeleted = false AND user.isBanned = false`. Mirror the JOIN style used in the existing `visibleTo()`'s FRIENDS branch.
2. In `PostService.listFeed`, find the branch where `sortKey == SortKey.ANSWERED` (around line 130–170). Add `.and(PostSpecifications.authorActive())` to the spec chain ONLY in that branch.
3. Do NOT modify `visibleTo()` — Gate-G-EXTEND-NOT-DUPLICATE forbids extending a load-bearing predicate consumed by every feed query.

**Guardrails (DO NOT):**
- Apply `authorActive()` to the default `sort=bumped` or `sort=recent` branches. That would be cross-spec drift.
- Extend `visibleTo()` to absorb this — keep the change scoped to the answered feed (advisor's #4 guidance).

**Verification:**
- [ ] `./mvnw compile` passes.
- [ ] All shipped feed tests pass unchanged (the default sort paths are untouched).
- [ ] New T22 test (Step 11) asserts a post whose author is `isDeleted=true` is excluded from `sort=answered` but appears in `sort=bumped` if visibility allows.

**Expected state after completion:**
- [ ] New `authorActive()` Specification factory.
- [ ] `listFeed`'s `sort=answered` branch chains it; no other branch does.

---

### Step 10: Redis cache for answered feed — `@Cacheable("answered-feed")` wiring + invalidation

**Objective:** Implement MPD-5 per ED-5. **REQUIRES Plan-Time Divergence #1 sign-off from Eric BEFORE this step executes.**

**Files to modify:**
- `backend/src/main/java/com/worshiproom/post/PostService.java`
- `backend/src/main/resources/application.properties` — add `spring.cache.redis.time-to-live.answered-feed=2m`.

**Details — Option (a) — Cache only when viewer is unauthenticated (RECOMMENDED FALLBACK if Eric defers):**
1. Factor `listFeed`'s `sort=answered` branch into a private method `listAnsweredFeedAnon(category, page, limit, requestId)` that takes only viewer-agnostic args. Annotate it `@Cacheable(value = "answered-feed", key = "T(java.util.Objects).hash(#category, #page, #limit)")`.
2. Public `listFeed` dispatches: if `principal == null` and `sortKey == ANSWERED`, call the cached method. Otherwise inline (no caching).
3. Logged-in viewers still get the full per-viewer enrichment with no cache leak risk.

**Details — Option (b) — Cache viewer-agnostic, enrich per request:**
1. Factor the answered feed into two layers: (i) raw query result (no intercessor enrichment) — cached. (ii) per-viewer `intercessorSummary` enrichment — uncached, runs per request on the cached raw result.
2. Annotate the inner method `@Cacheable(value = "answered-feed", key = "T(java.util.Objects).hash(#category, #page, #limit)")`.
3. Logged-in viewers benefit from the cache for the underlying feed query but still get per-viewer classification.

**Either way — invalidation:**
- In `PostService.updatePost`, after a successful `isAnswered` transition (lines 506–522), add `@CacheEvict(value = "answered-feed", allEntries = true)`. The transition may affect any page of any category filter, so all-entries-clear is the simplest correct strategy at 2-min TTL.
- Similarly, after a content edit (`wantsContentEdit`) on an `isAnswered=true` post, evict.
- `@CacheEvict` annotations require the method-level `@Transactional` context (already present).

**Guardrails (DO NOT):**
- Put `@Cacheable` at the controller method or at the public `listFeed` method — that's ABOVE the per-viewer enrichment and leaks `intercessorSummary` across viewers (advisor's #2 guidance).
- Cache for an authenticated viewer with the per-viewer payload inside the cached value.
- Skip the `@CacheEvict` paths — the answered feed would serve stale data for up to 2 min after an un-mark or content edit.

**Verification:**
- [ ] `./mvnw compile` passes.
- [ ] `application.properties` has the TTL line.
- [ ] `RepoWideTtlEnforcementTest` (existing, Spec 5.6) still passes — every `@Cacheable` has a corresponding TTL property.
- [ ] T23 + T24 (Step 11) pass — cache hit on second identical request; cache invalidated on `isAnswered` transition + content edit.

**Expected state after completion:**
- [ ] Cache layer wired per Eric's chosen option.
- [ ] Invalidation paths wired in `updatePost`.

---

### Step 11: Backend integration tests — migration + reaction + feed + caching

**Objective:** Add T1–T9, T16–T18, T21–T24 from the spec's Test Specifications (Section 9).

**Files to create:**
- `backend/src/test/java/com/worshiproom/post/engagement/CelebrateReactionMigrationTest.java` — T1 (`'celebrate'` accepted), T2 (all 4 values accepted, no regression), T3 (invalid still fails CHECK), T4 (migration runs against seeded `post_reactions` rows).
- `backend/src/test/java/com/worshiproom/post/engagement/CelebrateReactionWriteTest.java` — T5 (`toggleCelebrate_addPath_doesNotFireIntercessionActivity` — actual method name verbatim per S1 note), T6 (`toggleCelebrate_removePath` — removes row), T7 (run-and-assert the three shipped INTERCESSION tests still pass — `@Test` that programmatically invokes them via reflection OR a `@Nested` class that copies their assertion shape WITHOUT modifying the originals).
- `backend/src/test/java/com/worshiproom/post/AnsweredFeedTest.java` — T8 (`?category=health` narrows), T9 (no category returns unfiltered answered), T21 (expired-but-answered included), T22 (deleted-author excluded).
- `backend/src/test/java/com/worshiproom/post/AnsweredFeedCacheTest.java` — T23 (second request hits cache; different `?category=` cached separately), T24 (cache invalidated on `isAnswered` transition AND on content edit of an answered post).
- `backend/src/test/java/com/worshiproom/post/MarkAndUnmarkAnsweredTest.java` — T16 (edit endpoint rejects non-author), T17 (un-mark sets `is_answered=false` AND `answered_at=NULL`), T18 (answered_text edit succeeds outside the original edit window).

**Details:**
- All tests extend `AbstractIntegrationTest` (the singleton Testcontainers PostgreSQL — never per-class containers).
- Seed two user fixtures (active author + deleted/banned author) and a small set of posts per category.
- T22 uses `Post.userId` pointing at a user with `isDeleted=true`.
- T7 imports the existing test class and invokes its `@Test` methods via a `@Nested` "regressionSafe" inner class that runs them verbatim — NO modification to the original assertions (Gate-G-CELEBRATE-NO-INTERCESSION + Gate-G-6.6-REGRESSION-SAFE).

**Test specifications:**
| Test | Type | Description |
|------|------|-------------|
| T1 | integration | After 003 runs, INSERT with `reaction_type='celebrate'` succeeds. |
| T2 | integration | After 003, INSERT with each of `'praying'`/`'candle'`/`'praising'`/`'celebrate'` succeeds. |
| T3 | integration | INSERT with `reaction_type='invalid'` fails the CHECK. |
| T4 | integration | Seed `post_reactions` with `('praying','candle','praising')` BEFORE 003 runs (via fixtures + flyway/migration disabled), then apply 003, then INSERT `'celebrate'` — succeeds; all 4 existing rows still present. |
| T5 | integration | `ReactionWriteService.toggle` add-path with `reactionType="celebrate"` creates the reaction row + increments `celebrate_count` but creates **zero** `activity_log` rows of type `INTERCESSION`. Method name: `toggleCelebrate_addPath_doesNotFireIntercessionActivity`. |
| T6 | integration | Toggle off — DELETE the row; decrement counter; no activity event. |
| T7 (regressionSafe) | integration | The three shipped INTERCESSION tests (`togglePraying_addPath_firesIntercessionActivity`, `togglePraising_addPath_doesNotFireIntercessionActivity`, `toggleCandle_addPath_doesNotFireIntercessionActivity`) pass unmodified. |
| T8 | integration | `GET /api/v1/posts?sort=answered&category=health` returns only posts with category=health and is_answered=true. |
| T9 | integration | `GET /api/v1/posts?sort=answered` returns all answered regardless of category; same as `?category=` omitted. |
| T16 | integration | `PATCH /api/v1/posts/{id}` with `answeredText` body returns 403 for a non-author; 200 for the author. |
| T17 | integration | `PATCH /api/v1/posts/{id}` with `isAnswered=false` body sets `is_answered=false` AND `answered_at=NULL`; the post leaves the `sort=answered` feed. |
| T18 | integration | Create a post answered 10 days ago (mock `created_at`); `PATCH` with new `answeredText` succeeds (no `EDIT_WINDOW_EXPIRED`). |
| T21 | integration | A post with `expires_at < NOW()` (if any expiry mechanism exists) AND `is_answered=true` appears in `sort=answered`. |
| T22 | integration | A post whose author has `users.is_deleted=true` is excluded from `sort=answered`. |
| T23 | integration | Two identical `GET /api/v1/posts?sort=answered&category=health&page=1` requests with an unauthenticated viewer (option a) hit the cache on the second; with a different `?category=family`, hits a different cache slot. |
| T24 | integration | After a `PATCH` that toggles `is_answered` on any post, the `answered-feed` cache is empty (next read repopulates). Same after a content edit on an `isAnswered=true` post. |

**Verification:**
- [ ] `./mvnw test -Dtest=CelebrateReactionMigrationTest` passes.
- [ ] `./mvnw test -Dtest=CelebrateReactionWriteTest` passes.
- [ ] `./mvnw test -Dtest=AnsweredFeedTest` passes.
- [ ] `./mvnw test -Dtest=AnsweredFeedCacheTest` passes.
- [ ] `./mvnw test -Dtest=MarkAndUnmarkAnsweredTest` passes.
- [ ] Full backend suite passes: `./mvnw test`. The shipped 6.6 INTERCESSION tests pass with zero assertion modifications (Gate-G-6.6-REGRESSION-SAFE).

**Expected state after completion:**
- [ ] 13 backend tests added (T1–T9, T16–T18, T21–T24). Combined with existing 6.6 INTERCESSION tests run via T7's `@Nested` regression class, plus T-A1 to come on the frontend, total ≈24 new tests.

---

### Step 12: OpenAPI spec extension — `celebrate` enum + `celebrateCount`

**Objective:** Extend the existing `backend/src/main/resources/openapi.yaml` per Universal Rule 4. Five enum/description sites widen to add `celebrate`; `PostDto` and `ToggleReactionResponse` schemas gain `celebrateCount`.

**Files to modify:**
- `backend/src/main/resources/openapi.yaml`

**Details (exact line references from recon):**
1. Line 2324 — DELETE query-param enum: `enum: [praying, candle, praising]` → `enum: [praying, candle, praising, celebrate]`.
2. Line 4693 — `ToggleReactionRequest.reactionType.enum` → same widening.
3. Line 4708 — `ToggleReactionResponse.reactionType.enum` → same widening.
4. Line ~2225 + ~2304 — description text mentioning the reaction set → update to include "celebrate" in the prose.
5. Line ~2713 + ~4660 + ~4696 — "future `celebrate` support is deferred" descriptions → replace with current-tense "and `celebrate` reaction support (Spec 6.6b)".
6. `PostDto` schema (~line 4208 required list, ~4243 properties) — add `celebrateCount: { type: integer, minimum: 0, description: "Spec 6.6b — count of 'celebrate' reactions on the post (Answered Wall)." }`. Add `celebrateCount` to the `required` array.
7. `ToggleReactionResponse` schema (~line 4704 required list, ~4723 properties) — add `celebrateCount` mirroring `praisingCount`. Add to required array.

**Guardrails (DO NOT):**
- Create a new `backend/api/openapi.yaml` file — the source of truth is `backend/src/main/resources/openapi.yaml` (Rule 4 + ai-proxy-foundation Spec 1 § Decision 10).
- Reorder existing schema fields (the generated TypeScript would change unrelated typings).
- Remove the existing "future support deferred" descriptions silently — replace them with the now-shipped framing.

**Verification:**
- [ ] `npx @redocly/cli lint backend/src/main/resources/openapi.yaml` returns no errors (or matches the existing baseline).
- [ ] If the frontend pipeline regenerates types (`pnpm run types:generate` or equivalent), the generated `frontend/src/types/api/generated.ts` (when wired up) includes `'celebrate'` in `ReactionTypeApi`. If types are hand-typed at the time of execution, Step 13 covers the frontend type union.

**Expected state after completion:**
- [ ] All seven OpenAPI sites updated; no new file created.

---

### Step 13: Frontend type union + reactive store — add `'celebrate'` everywhere

**Objective:** Extend the frontend types and reactive store. Pattern A hook (`usePrayerReactions`) gains `toggleCelebrate`.

**Files to modify:**
- `frontend/src/types/api/prayer-wall.ts` — extend `ReactionTypeApi`; add `celebrateCount?` to `PostDto`; `isCelebrating?` to `MyReactionRow`; extend `ToggleReactionRequest.reactionType` and `ToggleReactionResponse` to include `celebrate` + `celebrateCount`.
- `frontend/src/types/prayer-wall.ts` — add `celebrateCount?: number` to `PrayerRequest`; add `isCelebrating: boolean` to `PrayerReaction`.
- `frontend/src/lib/prayer-wall/reactionsStore.ts` — extend the hydrate-time migration to 3 stages (per ED-6); add `toggleCelebrate(prayerId: string): boolean`; update `setReaction`/`mergeReactions` to handle `isCelebrating`.
- `frontend/src/hooks/usePrayerReactions.ts` — expose `toggleCelebrate`.

**Details:**
1. `ReactionTypeApi` union: `'praying' | 'candle' | 'praising' | 'celebrate'`.
2. `reactionsStore.ts` hydrate guard (lines 72–84): three-stage migration. If `isCelebrating` is missing on a value that already has `isPraising`, default-fill to `false` and write back. Pre-3.7 data already triggers default-fill of `isCandle` + `isPraising`; now also fill `isCelebrating`. **No version key** — additive shape with safe `false` defaults.
3. `toggleCelebrate(prayerId)` mirrors `togglePraising(prayerId)`: optimistic local toggle, fire `prayer-wall-api.toggleReaction(prayerId, 'celebrate')`, reconcile from response.
4. `setReaction`, `mergeReactions`, and any partial-update path: every place that constructs a `PrayerReaction` literal must now include `isCelebrating: current?.isCelebrating ?? false`.

**Guardrails (DO NOT):**
- Add a version key — the migration is one-pass and additive (matches the existing 2-stage pattern).
- Forget to write back the migrated value (the existing pattern does write-back on hydrate).
- Mock `usePrayerReactions` in any test — Pattern A stores are tested by mutating the store after mount (Rule 8 / BB-45 anti-pattern forbidden).

**Verification:**
- [ ] `pnpm tsc --noEmit` passes.
- [ ] `pnpm test --run frontend/src/lib/prayer-wall/__tests__/reactionsStore.test.ts` passes (existing migration tests still pass; new test cases for the 3rd-stage migration are added).
- [ ] Cross-mount subscription test (T-A1) — see Step 22.

**Expected state after completion:**
- [ ] All four files updated; types align with backend; store + hook expose `toggleCelebrate`.

---

### Step 14: `frontend/src/constants/answered-wall-copy.ts` — add 6.6b copy strings

**Objective:** All new user-facing strings live here per Universal Rule 5. Eric-approved per Gate-G-COPY before execute.

**Files to modify:**
- `frontend/src/constants/answered-wall-copy.ts`

**Details:** Add the following exports (preserving the existing module structure and comments):

```ts
/** Spec 6.6b — REPLACES the shipped subhead. Load-bearing line per Gate-G-COPY.
 *  Rendered Lora italic, <h2> within <main>. */
export const ANSWERED_WALL_SUBHEAD_6_6B =
  'Gratitude, not comparison.' as const

/** Spec 6.6b — intro paragraph mounted below the hero. Calm context, not a CTA. */
export const ANSWERED_WALL_INTRO =
  'These are prayer requests whose authors chose to share an update. Many prayers go unanswered, or are answered quietly, or are still being waited on. This page is not the whole story — it is just the part some people felt moved to share.' as const

/** Spec 6.6b — missing-answer-text fallback on AnsweredCard. */
export const ANSWERED_TEXT_MISSING_FALLBACK =
  'This prayer was marked answered. The author hasn\'t shared an update.' as const

/** Spec 6.6b — separate reaction count format. Singular/plural handled at call site. */
export const buildPraisingCelebratingCount = (praising: number, celebrating: number) =>
  `${praising} ${praising === 1 ? 'praising with you' : 'praising with you'} · ${celebrating} ${celebrating === 1 ? 'celebrating' : 'celebrating'}`

/** Spec 6.6b — Celebrate reaction labels. */
export const CELEBRATE_LABEL = 'Celebrate' as const
export const CELEBRATE_INACTIVE_ARIA_LABEL = 'Celebrate this answered prayer' as const
export const CELEBRATE_ACTIVE_ARIA_LABEL = "You're celebrating this answered prayer. Tap to remove." as const

/** Spec 6.6b — "Share an update" / "Edit your update" affordances. */
export const SHARE_UPDATE_LABEL = 'Share an update' as const
export const EDIT_UPDATE_LABEL = 'Edit your update' as const

/** Spec 6.6b — Un-mark affordance + confirmation modal. */
export const UNMARK_ANSWERED_LABEL = 'Un-mark as answered' as const
export const UNMARK_ANSWERED_CONFIRM =
  'Remove this from the Answered Wall? You can mark it answered again anytime.' as const

/** Spec 6.6b — Category filter chip labels. */
export const ANSWERED_CATEGORY_CHIP_ALL = 'All' as const
export const ANSWERED_CATEGORY_CHIPS = [
  { value: 'all', label: 'All' },
  { value: 'health', label: 'Health' },
  { value: 'family', label: 'Family' },
  { value: 'work', label: 'Work' },
  { value: 'grief', label: 'Grief' },
  { value: 'gratitude', label: 'Gratitude' },
] as const

/** Spec 6.6b — "Why no Mental Health filter" rationale. Embedded as a code
 *  comment on AnsweredCategoryFilter.tsx per Gate-G-MH-OMISSION. Exported here
 *  for documentation / discoverability. Do NOT add a Mental Health chip
 *  without revisiting this rationale. */
export const MH_OMISSION_RATIONALE =
  "There is intentionally no 'Mental Health' category chip. Mental-health prayers being 'answered' is genuinely complicated theological and clinical territory — healing is rarely linear, and a filter chip on a celebration surface risks implying a tidy resolution narrative that does not serve people living in that territory. This omission is a deliberate anti-pressure design decision from the Spec 6.6 master plan, carried forward by Spec 6.6b. Do not add a Mental Health chip without revisiting that rationale." as const
```

**Also:** Remove the wrong subhead string at line 21 (`ANSWERED_WALL_SUBHEAD = 'Prayers the community has watched God move in.'`). Replace its export with a forwarding alias: `export const ANSWERED_WALL_SUBHEAD = ANSWERED_WALL_SUBHEAD_6_6B`. This preserves backwards compatibility for any internal import while making the underlying string correct.

**Guardrails (DO NOT):**
- Use `as const` plus mutable arrays where readonly tuples are intended — `ANSWERED_CATEGORY_CHIPS` MUST be `as const` so TypeScript narrows `value` to the union.
- Drop the MH rationale export — it ships AS A CODE COMMENT on `AnsweredCategoryFilter.tsx` AND as an exported string for discoverability.

**Verification:**
- [ ] `pnpm tsc --noEmit` passes.
- [ ] Existing tests that import `ANSWERED_WALL_SUBHEAD` get the new string (test failures here are expected — Step 21 updates them).

**Expected state after completion:**
- [ ] All new copy strings exported; the wrong subhead string is gone (replaced by forwarding alias).

---

### Step 15: `AnsweredCategoryFilter.tsx` — NEW chip-row component

**Objective:** Six chips (All + 5 categories), URL sync via `?category=`, keyboard-reachable, announces selected state, MH rationale embedded.

**Files to create:**
- `frontend/src/components/prayer-wall/AnsweredCategoryFilter.tsx`
- `frontend/src/components/prayer-wall/__tests__/AnsweredCategoryFilter.test.tsx`

**Details:**

```tsx
import { useSearchParams } from 'react-router-dom'
import { cn } from '@/lib/utils'
import { ANSWERED_CATEGORY_CHIPS } from '@/constants/answered-wall-copy'

/**
 * Spec 6.6b — Answered Wall category filter chip row.
 *
 * Renders exactly six chips: All (default), Health, Family, Work, Grief,
 * Gratitude. The active chip is driven by the URL `?category=` query param.
 * Tap a chip → updates the URL; "All" clears the param entirely.
 *
 * There is intentionally no 'Mental Health' category chip. Mental-health
 * prayers being 'answered' is genuinely complicated theological and clinical
 * territory — healing is rarely linear, and a filter chip on a celebration
 * surface risks implying a tidy resolution narrative that does not serve
 * people living in that territory. This omission is a deliberate
 * anti-pressure design decision from the Spec 6.6 master plan, carried
 * forward by Spec 6.6b. Do not add a Mental Health chip without revisiting
 * that rationale. (Gate-G-MH-OMISSION — code review hard-blocks any addition
 * of a Mental Health chip OR the removal of this comment.)
 */
export function AnsweredCategoryFilter() {
  const [searchParams, setSearchParams] = useSearchParams()
  const active = searchParams.get('category') ?? 'all'

  const handleSelect = (value: string) => {
    if (value === 'all') {
      const next = new URLSearchParams(searchParams)
      next.delete('category')
      setSearchParams(next, { replace: true })
    } else {
      const next = new URLSearchParams(searchParams)
      next.set('category', value)
      setSearchParams(next, { replace: true })
    }
  }

  return (
    <div
      role="radiogroup"
      aria-label="Filter answered prayers by category"
      className="flex flex-wrap gap-2 justify-center"
    >
      {ANSWERED_CATEGORY_CHIPS.map((chip) => {
        const selected = active === chip.value
        return (
          <button
            key={chip.value}
            type="button"
            role="radio"
            aria-checked={selected}
            onClick={() => handleSelect(chip.value)}
            className={cn(
              'min-h-[44px] inline-flex items-center rounded-full px-4 py-2 text-sm font-medium transition-colors duration-fast',
              'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white/50',
              selected
                ? 'bg-white/15 text-white border border-white/30'
                : 'bg-white/[0.05] text-white/70 border border-white/[0.12] hover:bg-white/[0.10] hover:text-white',
            )}
          >
            {chip.label}
          </button>
        )
      })}
    </div>
  )
}
```

**Test file (T10 + T11 + T12):**
- T10: Render the component, assert exactly 6 chips, assert no chip with text "Mental Health" appears (`expect(screen.queryByRole('radio', { name: /mental health/i })).not.toBeInTheDocument()`).
- T11: Wrap in a MemoryRouter; click "Health"; assert URL search param `?category=health`; click "All"; assert param cleared.
- T12: Each chip is keyboard-focusable (tab order); arrow keys move between chips within the radiogroup (this is the browser default for `role="radio"` siblings); `aria-checked` flips appropriately.

**Guardrails (DO NOT):**
- Add a 7th chip for any of `mental-health`/`praise`/`relationships`/`other`/`discussion` (Gate-G-MH-OMISSION + ED-1).
- Use `aria-pressed` instead of `role="radio"` + `aria-checked` (radio is the correct ARIA role for an exclusive-select chip row per Spec 10A's "muted-white isolated pill" canonical).
- Remove the inline rationale comment (Gate-G-MH-OMISSION HARD blocks shipping the component without it).

**Verification:**
- [ ] `pnpm tsc --noEmit` passes.
- [ ] `pnpm test --run frontend/src/components/prayer-wall/__tests__/AnsweredCategoryFilter.test.tsx` passes (3 tests).
- [ ] Visual at 375px: chips wrap to 2 rows; at 1440px they fit in 1 row.

**Expected state after completion:**
- [ ] New component file + test file as above.

---

### Step 16: `CelebrateReaction.tsx` — NEW reaction button

**Objective:** A new reaction button distinct from Praising. Warm sunrise palette (gold + coral); short sparkle animation; reduced-motion fallback.

**Files to create:**
- `frontend/src/components/prayer-wall/CelebrateReaction.tsx`
- `frontend/src/components/prayer-wall/__tests__/CelebrateReaction.test.tsx`

**Details:** Mirror the InteractionBar's existing `showPraising` button structure (lines 240–264) but with:
- Icon: `Star` (lucide) or a similar warm-icon — picks `Star` (`text-amber-200` inactive / `text-amber-300` active). Confirm WCAG AA on `bg-hero-bg` (#08051A) — `text-amber-300` (#FCD34D) on dark passes 4.5:1.
- Animation: A short sparkle-ring overlay using a new keyframe `celebrate-sparkle` (~600ms, `decelerate` from `ANIMATION_DURATIONS`). The global `prefers-reduced-motion` rule in `frontend/src/styles/animations.css` auto-disables; no per-component check needed.
- Label / aria-label: `CELEBRATE_LABEL` / `CELEBRATE_INACTIVE_ARIA_LABEL` / `CELEBRATE_ACTIVE_ARIA_LABEL` from `answered-wall-copy.ts`.
- Auth-gated via `useAuth()` + `useAuthModal()` — falls back to auth modal when logged out.

The component takes props `{ isActive: boolean, count: number, onToggle: () => void }` and is rendered by `InteractionBar` when `showCelebrate={true}` is passed.

**Test file (subset of T-E2E and unit):**
- Renders inactive state by default; `aria-pressed`/`aria-checked` reflects `isActive`.
- Click fires `onToggle`.
- Logged-out click opens auth modal (mock `useAuthModal`).
- Count displays correctly singular / plural via `CELEBRATE_LABEL (count)` shape.

**Guardrails (DO NOT):**
- Hardcode any ms or cubic-bezier in CSS — use `ANIMATION_DURATIONS` + token easings (Rule 10 + BB-33).
- Reuse the `Sparkles` icon used for Praising — these MUST visually differ (W12).
- Add a `prefers-reduced-motion` check in the component — the global rule handles it.

**Verification:**
- [ ] `pnpm tsc --noEmit` passes.
- [ ] `pnpm test --run frontend/src/components/prayer-wall/__tests__/CelebrateReaction.test.tsx` passes.
- [ ] Visual at all 6 standard breakpoints; reduced-motion-on shows static state.

**Expected state after completion:**
- [ ] New component file + test file; new keyframe registered in `tailwind.config.js` (or `index.css` if that's where existing pray/sparkle keyframes live).

---

### Step 17: `InteractionBar.tsx` — wire `showCelebrate` + `onToggleCelebrate`

**Objective:** Surface `CelebrateReaction` on answered posts only (W11).

**Files to modify:**
- `frontend/src/components/prayer-wall/InteractionBar.tsx`

**Details:**
1. Extend `InteractionBarProps` with `showCelebrate?: boolean` and `onToggleCelebrate?: () => void` (mirroring `showPraising` + `onTogglePraising`).
2. Add `const isCelebrating = reactions?.isCelebrating ?? false` near line 91.
3. Render `<CelebrateReaction isActive={isCelebrating} count={prayer.celebrateCount ?? 0} onToggle={() => onToggleCelebrate?.()} />` immediately after the `{showPraising && (...)}` block.

**Guardrails (DO NOT):**
- Surface Celebrate on non-answered posts. The `showCelebrate` prop is the gate; callers MUST pass `showCelebrate` only when `prayer.isAnswered === true` (W11).
- Modify the existing showPraising block (Gate-G-6.6-REGRESSION-SAFE).

**Verification:**
- [ ] `pnpm tsc --noEmit` passes.
- [ ] Existing `InteractionBar.test.tsx` passes unchanged (default props don't render the Celebrate button).
- [ ] New test asserts `showCelebrate=true` renders the button; `showCelebrate=false` does not.

**Expected state after completion:**
- [ ] `InteractionBar` accepts the new props and renders Celebrate only when opted in.

---

### Step 18: `AnsweredWall.tsx` — fix subhead, add intro paragraph, mount `AnsweredCategoryFilter`, wire celebrate + category

**Objective:** Single most visible step. Surgical edit (Gate-G-EXTEND-NOT-DUPLICATE — NOT a new component).

**Files to modify:**
- `frontend/src/pages/AnsweredWall.tsx`

**Details:**
1. Replace the SEO `description` at line 88 to use the new copy (e.g., "Answered prayers, shared as gratitude. A quiet wall of testimonies — not a leaderboard.").
2. Replace the `<p>` at line 108–110 with TWO elements:
   - `<h2 className="font-serif italic text-lg sm:text-xl text-white/80 mt-2">{ANSWERED_WALL_SUBHEAD_6_6B}</h2>` — Lora italic, h2 inside `<main>` (W16).
   - `<p className="mx-auto mt-4 max-w-xl text-base text-white/60 leading-relaxed">{ANSWERED_WALL_INTRO}</p>` — intro paragraph (W17).
3. Below `PrayerWallViewTabs` (line 114), mount `<AnsweredCategoryFilter />` inside a container `<div className="mx-auto mt-6 max-w-[720px]">`.
4. Read `?category=` from `useSearchParams` and pass it to `prayerWallApi.listPosts({ page: 1, limit: 20, sort: 'answered', category })`.
5. In the `<InteractionBar>` invocation inside the `prayers.map` block (line 142–151), add `showCelebrate` and `onToggleCelebrate={() => toggleCelebrate(prayer.id)}` — wire `toggleCelebrate` from `usePrayerReactions`.

**Guardrails (DO NOT):**
- Build a new `AnsweredWallHero` component (Gate-G-EXTEND-NOT-DUPLICATE).
- Leave the wrong subhead anywhere — grep `Prayers the community has watched God move in` across the repo after this step; must return zero matches outside `__tests__` files that explicitly assert the prior copy is GONE.
- Style the intro paragraph as a CTA (W17).

**Verification:**
- [ ] `pnpm tsc --noEmit` passes.
- [ ] Existing `AnsweredWall.test.tsx` updates to assert the new subhead string + intro paragraph + chip presence (Step 22).
- [ ] Visual check at 375/768/1440: the hero reads `<h1>Answered</h1>` (Fraunces) followed by `<h2>Gratitude, not comparison.</h2>` (Lora italic) followed by the intro paragraph; chip row below the existing tabs.
- [ ] axe-core verification: 0 violations.

**Expected state after completion:**
- [ ] Surgical edit to the shipped file; new subhead + intro + chip row + celebrate wiring.

---

### Step 19: `PrayerCard.tsx` — missing-text fallback + Share-an-update + Un-mark affordances

**Objective:** Extend the existing `answeredVariant` block (lines 278–308) to handle missing text and author affordances. Surgical edit — do NOT fork.

**Files to modify:**
- `frontend/src/components/prayer-wall/PrayerCard.tsx`

**Details:**
1. After the `<p>{prayer.answeredText}</p>` block (line 289–293), add a fallback: if `!prayer.answeredText`, render a quieter italic paragraph with `ANSWERED_TEXT_MISSING_FALLBACK` and `text-white/50`.
2. Add an "author-only" affordance row below the answered section, gated by `useAuth()` + ownership check (`user?.id === prayer.userId`). Two buttons:
   - "Share an update" / "Edit your update" (button label flips based on whether `answeredText` is present) — opens a modal/drawer (or reuses an extended `MarkAsAnsweredForm` in edit mode — see Step 20).
   - "Un-mark as answered" — opens a confirmation dialog using `UNMARK_ANSWERED_CONFIRM`. On confirm, PATCH `/api/v1/posts/{id}` with `{ isAnswered: false }`.
3. The author-only affordance row is hidden when not logged in OR when `user.id !== prayer.userId` (W26 — also enforced at the API level by `PostService.updatePost`'s ownership gate).

**Guardrails (DO NOT):**
- Show "Share an update" or "Un-mark" to non-authors (W26 + Gate-G-EXTEND-NOT-DUPLICATE).
- Forget that `answered_text` editing has no time-window restriction (W28). Don't accidentally route through any content-edit time gate.
- Fork `PrayerCard` — keep the change inside the existing `answeredVariant && prayer.isAnswered` block.

**Verification:**
- [ ] `pnpm tsc --noEmit` passes.
- [ ] T13 (Step 22) passes: a prayer with no `answeredText` renders the fallback.
- [ ] T14 (Step 22) passes: an author sees both affordances; T15 a non-author sees neither.

**Expected state after completion:**
- [ ] Fallback rendering + author-only affordance row added inside the existing block.

---

### Step 20: `MarkAsAnsweredForm.tsx` — extend to support edit mode + un-mark integration

**Objective:** Add `mode?: 'mark' | 'edit' | 'unmark'` + `initialText?: string` props so the same form serves three flows: initial mark-as-answered (existing), post-hoc edit of `answered_text`, and the un-mark confirmation. Reuse, do not fork (Gate-G-EXTEND-NOT-DUPLICATE).

**Files to modify:**
- `frontend/src/components/prayer-wall/MarkAsAnsweredForm.tsx`

**Details:**
1. Extend props: `{ mode?: 'mark' | 'edit', initialText?: string, onConfirm: (text: string) => void, onCancel?: () => void }`. Default `mode='mark'` for backwards compatibility.
2. When `mode='edit'`, the trigger label is `EDIT_UPDATE_LABEL`; when `mode='mark'`, it stays `'Mark as Answered'`.
3. When `mode='edit'` and `initialText` is provided, prepopulate the textarea on expand.
4. The un-mark confirmation is a SEPARATE small modal/dialog (NOT this form). Create it inline in `PrayerCard.tsx` Step 19 — a small confirm dialog with `UNMARK_ANSWERED_CONFIRM` body + "Un-mark" destructive button + "Cancel" subtle button (use AlertDialog Pattern per `09-design-system.md`).

**Guardrails (DO NOT):**
- Break the existing API. `onConfirm(text)` signature stays. Existing call sites work without modification.
- Conflate un-mark with the form — un-mark goes through a confirmation dialog, not the textarea form.

**Verification:**
- [ ] `pnpm tsc --noEmit` passes.
- [ ] Existing `MarkAsAnsweredForm.test.tsx` passes unchanged.
- [ ] New tests for `mode='edit'` (preprovides text) and the un-mark dialog (per Step 22).

**Expected state after completion:**
- [ ] `MarkAsAnsweredForm` accepts the new optional props; un-mark dialog implemented as a small companion component in `PrayerCard.tsx` Step 19 (or a sibling file if the diff would grow PrayerCard.tsx too far).

---

### Step 21: `prayer-wall-api.ts` — wire celebrate + un-mark + answered-text-edit

**Objective:** Make sure the API client supports the new reaction and the un-mark/edit calls.

**Files to modify:**
- `frontend/src/services/api/prayer-wall-api.ts`

**Details:**
1. `toggleReaction(postId, reactionType)` — already accepts `ReactionTypeApi`; widening to `'celebrate'` happens automatically once Step 13's type union is updated.
2. `markPostAnswered(postId, answeredText)` / `unmarkPostAnswered(postId)` / `editAnsweredText(postId, answeredText)` — if these helpers exist, ensure they call `PATCH /api/v1/posts/{id}` with the appropriate body. If they don't exist, add them. Likely shape: `prayerWallApi.updatePost(postId, { isAnswered: false })` for un-mark; `prayerWallApi.updatePost(postId, { answeredText })` for edit.

**Guardrails (DO NOT):**
- Add new endpoints (the backend reuses the existing PATCH endpoint — no new HTTP route).

**Verification:**
- [ ] `pnpm tsc --noEmit` passes.
- [ ] Existing api-client tests pass.

**Expected state after completion:**
- [ ] API client exposes the methods needed by PrayerCard Step 19.

---

### Step 22: Frontend tests — T10–T15, T19–T20, T-A1 + supporting unit tests

**Objective:** Cover the frontend changes per Section 9.

**Files to create / modify:**
- `frontend/src/components/prayer-wall/__tests__/AnsweredCategoryFilter.test.tsx` — created in Step 15.
- `frontend/src/components/prayer-wall/__tests__/CelebrateReaction.test.tsx` — created in Step 16.
- `frontend/src/pages/__tests__/AnsweredWall.test.tsx` (extend existing) — T19 (new subhead string), T20 (intro paragraph), assertion that the WRONG subhead string is absent.
- `frontend/src/components/prayer-wall/__tests__/PrayerCard.test.tsx` (extend existing) — T13 (missing-text fallback), T14 (author sees affordances), T15 (non-author does not see affordances).
- `frontend/src/components/prayer-wall/__tests__/MarkAsAnsweredForm.test.tsx` (extend existing) — `mode='edit'` initial state; un-mark dialog confirm.
- `frontend/src/hooks/__tests__/usePrayerReactions.test.ts` — T-A1: cross-mount subscription test for `isCelebrating`. Mount component A; assert `isCelebrating=false`; from component B (outside the React tree), call `toggleCelebrate(id)` on the store; assert component A re-renders with `isCelebrating=true`. **MUST NOT mock `usePrayerReactions` itself** — mutate the underlying store directly. Pattern A discipline per Universal Rule 8.

**Verification:**
- [ ] `pnpm test --run` passes the full suite.
- [ ] All 24 new tests (T1–T24 + T-A1 + supporting unit tests) pass.
- [ ] Shipped 6.6 frontend tests pass with zero assertion changes (Gate-G-6.6-REGRESSION-SAFE).

**Expected state after completion:**
- [ ] All target test counts met. The plan's target was "~24 new tests"; actual count ≈26 (13 backend + 13 frontend including T-A1 and supporting).

---

### Step 23: Update `11-local-storage-keys.md` for `wr_prayer_reactions` 3-stage migration

**Objective:** Universal Rule 7 compliance. The key itself is unchanged; the migration discussion gains a 3rd stage.

**Files to modify:**
- `.claude/rules/11-local-storage-keys.md`

**Details:** Find the `wr_prayer_reactions` entry. Replace the existing "Two-stage migration" sentence with a "Three-stage migration" sentence that mentions: pre-3.7 → fill `isCandle`+`isPraising`+`isCelebrating`; post-3.7-pre-6.6 → fill `isPraising`+`isCelebrating`; post-6.6-pre-6.6b → fill `isCelebrating`. Reference Spec 6.6b. No version key; additive shape with safe false defaults.

**Verification:**
- [ ] File edit applied.
- [ ] Grep `wr_prayer_reactions` in `11-local-storage-keys.md` returns the updated entry.

**Expected state after completion:**
- [ ] Documentation reflects the 3-stage migration.

---

### Step 24: Verification sweep — full backend + frontend test runs + Playwright

**Objective:** Lock in regression safety. Gate-G-6.6-REGRESSION-SAFE HARD requires zero shipped-test modification.

**Verification:**
- [ ] `./mvnw test` — full backend suite passes; the three shipped INTERCESSION tests pass with zero assertion modifications.
- [ ] `pnpm test --run` — full frontend suite passes; shipped 6.6 tests pass with zero assertion modifications.
- [ ] `pnpm build` — production build passes.
- [ ] `pnpm lint` — no new lint errors.
- [ ] `/verify-with-playwright _plans/forums/2026-05-14-spec-6-6b.md` — exercises every route in "Affected Frontend Routes"; verifies all manual items from Section 3 of the spec (Hero copy, Celebrate, Category chips, Answered-text lifecycle, Feed correctness, Auth gating); axe-core: 0 violations.
- [ ] Eric manual verification per Section 3 of the spec.

**Expected state after completion:**
- [ ] All acceptance criteria from Section 11 of the spec (A through Z) satisfied.
- [ ] Plan-Time Divergences resolved + recorded.

---

## Plan Tightening Audit

| # | Lens | Status |
|---|---|---|
| 1 | Schema state explicit | ✅ Database Changes table cites changesets 016 + 001 + 002 + the two NEW ones. No recreation. |
| 2 | Existing entity / class reuse | ✅ Post.java, ReactionWriteService.java, PostController, PostService, MarkAsAnsweredForm, AnsweredWall.tsx, AnsweredCard.tsx all extended in place. No duplicates. |
| 3 | SQL-side counter updates | ✅ Step 4 adds `incrementCelebrateCount` / `decrementCelebrateCount` via `@Modifying @Query` UPDATE. Phase 3 Addendum item 3 flags both set. |
| 4 | Activity engine — fire on ADD only, not REMOVE | ✅ Step 5 explicitly does NOT touch the INTERCESSION branch. Toggle-off does not fire activity. |
| 5 | SecurityConfig rule ordering | N/A — no new endpoints; existing PATCH/POST/DELETE on `/api/v1/posts/**` already have explicit `.authenticated()` rules ordered above permissive matchers. |
| 6 | Validation surface at controller boundary | ✅ Step 7 updates `@Pattern` at the controller boundary; DB CHECK is defense-in-depth, not the primary validation. |
| 7 | Pattern A clarification | ✅ Step 13 explicitly notes `usePrayerReactions` is Pattern A (subscription via standalone hook with `useSyncExternalStore`); Step 22 T-A1 is the cross-mount subscription test. |
| 8 | BB-45 cross-mount subscription test | ✅ T-A1 in Step 22. |
| 9 | Step dependency map | ✅ Below. Steps 1+2 parallel-able; 3 depends on 2; 4 on 3; 5 on 4; 6/7/8 on 5; 9 standalone; 10 depends on Eric sign-off; 11 depends on 1-10; 12 standalone; 13 standalone; 14 standalone; 15-21 depend on 13+14; 22 depends on 15-21; 23 standalone; 24 last. |
| 10 | Test count vs brief | ✅ Brief target ≈24 new tests; plan delivers 13 backend + 13 frontend = 26. Within +5 of target. |
| 11 | L1-cache trap | N/A — no new create-endpoint returning DB-default timestamps. Existing flows already include `findById` refresh patterns where needed. |
| 12 | `@Modifying` flags complete | ✅ Step 4 explicit. |
| 13 | Caffeine-bounded caches | N/A — no new per-IP / per-user bucket caches. Celebrate reaction rides existing `ReactionsRateLimitConfig` (Caffeine-bounded). Answered-feed cache is a Redis `@Cacheable` (not a bucket cache; bounded by TTL + Redis memory policy). |
| 14 | Domain-scoped advice | N/A — no new domain. Post family's `PostExceptionHandler` is `@RestControllerAdvice(basePackages = "com.worshiproom.post")` (existing) — celebrate-related exceptions flow through it. |
| 15 | Crisis content via CrisisAlertService | ✅ ED-2 documents the answered_text scope decision. No new CrisisAlertService extension needed. |

No drift detected in audit; plan locked.

---

## Step Dependency Map

| Step | Depends On | Description |
|------|------------|-------------|
| 1 | — | Liquibase: widen reaction_type CHECK to add 'celebrate' |
| 2 | — | Liquibase: add posts.celebrate_count column |
| 3 | 2 | Post.java: celebrateCount field |
| 4 | 3 | PostRepository: SQL-side counter mutators |
| 5 | 4 | ReactionWriteService: wire celebrate in counter dispatch |
| 6 | 5 | ToggleReactionResponse: add celebrateCount |
| 7 | 1, 5 | PostController + ToggleReactionRequest: widen @Pattern |
| 8 | 3 | PostMapper + PostDto: surface celebrateCount in responses |
| 9 | — | PostSpecifications: authorActive() applied in sort=answered |
| 10 | Eric sign-off (ED-5) + 8 | Redis cache for answered feed |
| 11 | 1, 5, 9, 10 | Backend integration tests T1–T9, T16–T18, T21–T24 |
| 12 | 7, 8 | OpenAPI: celebrate enum + celebrateCount schema |
| 13 | 12 | Frontend: ReactionTypeApi + reactionsStore migration + toggleCelebrate |
| 14 | — | answered-wall-copy.ts: new copy strings |
| 15 | 14 | AnsweredCategoryFilter.tsx (new) |
| 16 | 13, 14 | CelebrateReaction.tsx (new) |
| 17 | 16 | InteractionBar.tsx: wire showCelebrate |
| 18 | 13, 14, 15, 16, 17 | AnsweredWall.tsx: subhead fix + intro + chip mount + celebrate wiring |
| 19 | 14, 13 | PrayerCard.tsx: missing-text fallback + author affordances |
| 20 | 14 | MarkAsAnsweredForm.tsx: edit mode |
| 21 | 13 | prayer-wall-api.ts: un-mark + edit-text helpers |
| 22 | 15, 16, 18, 19, 20, 13 | Frontend tests T10–T15, T19–T20, T-A1 |
| 23 | 13 | 11-local-storage-keys.md: 3-stage migration doc |
| 24 | All prior | Verification sweep |

Independent (parallelizable) clusters: {1, 2}; {3, 9, 14}; {15, 16, 20}; {21, 23}.

---

## Execution Log

| Step | Title | Status | Completion Date | Notes / Actual Files |
|------|-------|--------|-----------------|----------------------|
| **Fifth-Gap Decision** | Pre-execute STOP — Eric chose option | [SIGNED OFF 2026-05-14] | 2026-05-14 | Option (2) — DEFER to a follow-up spec. Fifth-gap (CommentInput.tsx answered-aware placeholder) identified at plan-recon. Deferred to a follow-up spec per Brief Section 12 scope discipline. Not in 6.6b's four-gap scope. Tracker note added under Phase 6 brief-drift remediation block. |
| **Plan-Time Divergence #1** | Cache architecture — Eric chose option | [SIGNED OFF 2026-05-14] | 2026-05-14 | Option (b) — viewer-agnostic cache key BELOW per-viewer enrichment. `@Cacheable("answered-feed")` wraps the (category?, page) feed query result only; intercessorSummary and per-viewer fields computed per-request on top of the cached rows. TTL 2 min. Invalidation on any is_answered transition (mark + un-mark) and content edit of an answered post. Cache-boundary test required: two different viewers requesting the same (category, page) get identical cached underlying rows BUT correctly differentiated intercessorSummary — leak detection. |
| **Plan-Time Divergence #2** | Crisis-scan deferral acknowledged | [SIGNED OFF 2026-05-14] | 2026-05-14 | ED-2: answered_text is not crisis-scanned at creation (R3 confirmed). W29's conditional consistency rule is therefore satisfied (neither create nor edit scans). Adding scan to both paths is out of scope for this four-gap remediation spec — deferred to a future spec per Brief Section 0. Tracker note added under Phase 6 brief-drift remediation block as a SAFETY-SURFACE gap with risk tier ≥ High. |
| 1 | Liquibase 003 — widen CHECK | [COMPLETE] | 2026-05-14 | Created `backend/src/main/resources/db/changelog/2026-05-14-003-add-celebrate-to-post-reactions-check.xml`. Included in `master.xml`. LiquibaseSmokeTest passes (61/61). Updated `phase3CheckConstraintRejectsInvalidValue` to use `"invalid_reaction_type"` instead of `"celebrate"` (celebrate is now valid). |
| 2 | Liquibase 004 — celebrate_count column | [COMPLETE] | 2026-05-14 | Created `backend/src/main/resources/db/changelog/2026-05-14-004-add-posts-celebrate-count-column.xml` mirroring `002`. Included in `master.xml`. Updated `postsTableExistsWithExpectedColumnsAndIndexes` hasSize(31)→32 + added `"celebrate_count"` to `containsExactly`. Updated `liquibaseChangelogRecordsAllChangesets` hasSize(32)→34. |
| 3 | Post.java celebrateCount field | [COMPLETE] | 2026-05-14 | Added `celebrateCount` field + getter + setter mirroring `praisingCount` shape. |
| 4 | PostRepository counter mutators | [COMPLETE] | 2026-05-14 | Added `incrementCelebrateCount` + `decrementCelebrateCount` with `@Modifying(clearAutomatically=true, flushAutomatically=true)`. `> 0` guard on decrement. |
| 5 | ReactionWriteService celebrate dispatch | [COMPLETE] | 2026-05-14 | Added `CELEBRATE` constant; extended both counter dispatchers; INTERCESSION branch untouched (Gate-G-CELEBRATE-NO-INTERCESSION). |
| 6 | ToggleReactionResponse celebrateCount | [COMPLETE] | 2026-05-14 | Appended `celebrateCount` to record components (last field, after `praisingCount`). Updated `ReactionWriteService.toggle` to populate it. Existing reaction tests pass unchanged (18 + 5). |
| 7 | PostController @Pattern widening | [COMPLETE] | 2026-05-14 | Updated DELETE query-param regex AND `ToggleReactionRequest.@Pattern` to `^(praying\|candle\|praising\|celebrate)$`. |
| 8 | PostMapper + PostDto | [COMPLETE] | 2026-05-14 | Added `celebrateCount` to `PostDto` record (positional after `praisingCount`). Updated `PostMapper.buildDto` to populate it. Also updated 5 test sites that construct PostDto directly (`PostVisibilityTransitionTest`, `PostEditWindowTest`, `PostsIdempotencyServiceTest`, `PostServiceWriteTest` 2x) to pass the extra `0` arg. |
| 9 | PostSpecifications.authorActive() | [COMPLETE] | 2026-05-14 | Added `authorActive()` static factory — EXISTS subquery to `User` filtering `isDeleted=false AND isBanned=false`. Wired into `PostService.listFeed` `sort=answered` branch ONLY (not `listAuthorPosts` — that path already implies author-active by design). Did NOT modify `visibleTo()` (Gate-G-EXTEND-NOT-DUPLICATE). |
| 10 | Redis cache for answered feed | [COMPLETE] | 2026-05-14 | **ED-5 Option (b-simple) per advisor:** new `AnsweredFeedCache` Spring bean with `@Cacheable("answered-feed")` method `loadAnsweredFeedPublic(category, postType, qotdId, page, limit)`. Cache value: `CachedAnsweredFeed(List<Post>, long totalElements)` record (Page wrapper — Jackson PageImpl trap). **Cached spec uses `visibleTo(null)`** (public-only) — friends-only/private answered posts do NOT appear via the cached path; documented trade-off (Answered Wall is community celebration surface, intended-public is the default for testimonies). **Mute filter intentionally skipped** on Answered Wall feed (documented in `AnsweredFeedCache` JavaDoc). `PostService.listFeed` `sort=answered` branch delegates to the cache; intercessorSummary computed per-request on the cached rows. `@CacheEvict(allEntries=true)` on `PostService.updatePost`. TTL `spring.cache.redis.time-to-live.answered-feed=2m` added. `RepoWideTtlEnforcementTest` passes. All 3 mocked PostService test classes updated to mock the new `AnsweredFeedCache` constructor arg (PostServiceWriteTest 63 + PostEditWindowTest 6 + PostVisibilityTransitionTest 8 + RepoWideTtlEnforcementTest 2 = 79 pass). |
| 11 | Backend integration tests | [COMPLETE] | 2026-05-14 | 18 new backend tests across 4 new classes: `CelebrateReactionWriteTest` (5 — T1/T2/T3/T5/T6), `AnsweredFeedTest` (5 — T8/T9/T21/T22 + banned-author sibling), `AnsweredFeedCacheTest` (5 — T23a/T23b/T24a/T24b + **cache-boundary leak test** mandated by ED-5 Option (b) sign-off, proving viewer-agnostic cache below per-viewer intercessorSummary enrichment), `MarkAndUnmarkAnsweredTest` (3 — T16/T17/T18). Also updated `EngagementSchemaInvariantTest` to assert the widened CHECK includes `'celebrate'` and added `posts_celebrateCount_isIntNotNullDefault0`. Discovered + fixed cache-pollution issue: `ConcurrentMapCacheManager` retains entries across tests in the JVM; @BeforeEach in feed/cache tests clears the `answered-feed` cache via injected `CacheManager` before truncating tables. Gate-G-6.6-REGRESSION-SAFE verified: full suite shows 0 NEW failures (only pre-existing Upload baseline failures persist — missing test fixture files, unrelated to 6.6b). Shipped 6.6 INTERCESSION tests pass with zero assertion modifications. |
| 13 | Frontend types + reactionsStore + hook | [COMPLETE] | 2026-05-14 | Extended `ReactionTypeApi` to include `'celebrate'`. Added `celebrateCount` (PrayerRequest + PostDto) and `isCelebrating` (PrayerReaction + PerPostReactionApi + ToggleReactionResponse). `reactionsStore.ts` extended to 3-stage migration (pre-3.7 → fill 3; post-3.7-pre-6.6 → fill 2; post-6.6-pre-6.6b → fill 1). New `applyCelebrateFlip()` + `toggleCelebrate()` mirroring praising pattern. `usePrayerReactions` exposes `toggleCelebrate` (Pattern A — no mocking the hook). `postMappers.ts` defensively coerces `isCelebrating` for in-flight transitional data. Mock data + all hydration paths updated. |
| 14 | answered-wall-copy.ts | [COMPLETE] | 2026-05-14 | Added `ANSWERED_WALL_SUBHEAD_6_6B = 'Gratitude, not comparison.'`, `ANSWERED_WALL_INTRO` (survivorship-bias acknowledgment), `ANSWERED_TEXT_MISSING_FALLBACK`, `buildPraisingCelebratingCount()`, `CELEBRATE_LABEL`/`CELEBRATE_INACTIVE_ARIA_LABEL`/`CELEBRATE_ACTIVE_ARIA_LABEL`, `SHARE_UPDATE_LABEL`/`EDIT_UPDATE_LABEL`, `UNMARK_ANSWERED_LABEL`/`UNMARK_ANSWERED_CONFIRM`, `ANSWERED_CATEGORY_CHIPS`, `MH_OMISSION_RATIONALE`. Old `ANSWERED_WALL_SUBHEAD` is now a forwarding alias to the new string. |
| 15 | AnsweredCategoryFilter.tsx | [COMPLETE] | 2026-05-14 | Created `frontend/src/components/prayer-wall/AnsweredCategoryFilter.tsx` + test (7 tests passing). Six chips, `role="radiogroup"` + `role="radio"` + `aria-checked`, URL-driven via `?category=`, "All" clears the param, NO Mental Health chip (Gate-G-MH-OMISSION HARD — comment in component + exported MH_OMISSION_RATIONALE). |
| 16 | CelebrateReaction.tsx | [COMPLETE] | 2026-05-14 | Created `frontend/src/components/prayer-wall/CelebrateReaction.tsx` + test (5 tests passing). Star icon, warm sunrise palette (`text-amber-200/70` inactive, `text-amber-300` active), auth-gated, sparkle sound on toggle, reduced-motion via global rule. |
| 17 | InteractionBar showCelebrate | [COMPLETE] | 2026-05-14 | Added `showCelebrate?: boolean` + `onToggleCelebrate?: () => void` props mirroring showPraising. Renders `<CelebrateReaction>` after the Praising block when `showCelebrate=true`. All 20 existing InteractionBar tests still pass. Existing Praising block untouched (Gate-G-6.6-REGRESSION-SAFE). |
| 18 | AnsweredWall.tsx surgical fixes | [COMPLETE] | 2026-05-14 | Replaced SEO description; replaced subhead `<p>` with `<h2>` (Lora italic, "Gratitude, not comparison."); added intro paragraph below subhead; mounted `<AnsweredCategoryFilter>` below tabs; wired `?category=` from URL into `listPosts({ sort: 'answered', category })`; wired `toggleCelebrate` + `showCelebrate` on InteractionBar. AnsweredWall.test.tsx updated to assert new subhead/intro + assert OLD subhead string is gone (regression guard). 6 tests passing. Surgical edits — no new component file (Gate-G-EXTEND-NOT-DUPLICATE). |
| 19 | PrayerCard.tsx fallback + affordances | [COMPLETE] | 2026-05-14 | Added missing-text fallback inside existing `answeredVariant && prayer.isAnswered` block. Added optional `onUnmark?: () => void` and `onEditAnsweredText?: (text: string) => void` props; PrayerCard auto-detects ownership via `useContext(AuthContext)` (defensive — no throw outside provider) and renders affordance row only for the author with callbacks provided. Author affordance row contains Edit/Share button (label flips based on `answeredText` presence) and a new `<UnmarkAnsweredDialog>` sibling component. 6 new tests (T13/T14/T15 + 3 sibling). All 53 PrayerCard tests + bb35-form-a11y tests passing. Exported `AuthContext` from `contexts/AuthContext.tsx` to enable safe optional read. |
| 20 | MarkAsAnsweredForm.tsx edit mode | [COMPLETE] | 2026-05-14 | Added `mode?: 'mark' \| 'edit'` (default `'mark'` for back-compat) and `initialText?: string` + `onCancel?: () => void` props. In edit mode the form skips the trigger button, prepopulates the textarea with `initialText`, and labels Confirm → "Save". Existing call sites in `PrayerDetail.tsx` + `PrayerWallDashboard.tsx` continue to get the legacy `'mark'` behavior with zero modification. `onConfirm(text)` signature preserved verbatim. |
| 21 | prayer-wall-api.ts helpers | [COMPLETE] | 2026-05-14 | No new helpers needed — `updatePost(id, { isAnswered: false })` and `updatePost(id, { answeredText })` both work against the existing PATCH `/api/v1/posts/{id}` endpoint. `toggleReaction(prayerId, 'celebrate')` works automatically once `ReactionTypeApi` was widened in Step 13. `AnsweredWall.tsx` wires `handleUnmark` + `handleEditAnsweredText` via `prayerWallApi.updatePost`; on success optimistically updates local `prayers` state (removes un-marked posts from the list; replaces edited posts in-place). |
| 22 | Frontend tests | [COMPLETE] | 2026-05-14 | 13 new frontend tests: T10/T11/T12 (AnsweredCategoryFilter — 7 tests via 7 assertions), 5 CelebrateReaction tests, 6 PrayerCard 6.6b tests (T13/T14/T15 + 3 sibling), 1 AnsweredWall test (existing test updated to assert new subhead/intro + regression-guard the old string), T-A1 (`usePrayerReactions — Spec 6.6b T-A1 cross-mount subscription for toggleCelebrate` — Pattern A discipline per Universal Rule 8; mutates the store from outside and asserts the component re-renders, NEVER mocks the hook). reactionsStore.test.ts + postMappers.test.ts + prayer-wall-api.test.ts existing assertions updated for the new 6-field shape. **Frontend full suite: 796 files / 10514 passing / 10 skipped / 0 failures.** |
| 12 | OpenAPI extension | [COMPLETE] | 2026-05-14 | Extended `backend/src/main/resources/openapi.yaml`: POST `/posts/{id}/reactions` summary + description updated to mention `celebrate`; DELETE query-param `reactionType.enum` widened to `[praying, candle, praising, celebrate]`; `/users/me/reactions` description updated (`isCelebrating` field, "celebrate" added to non-empty conditions); `PerPostReaction` schema gains `isCelebrating` field (required); `ReactionsResponse` description updated; `ToggleReactionRequest.reactionType.enum` widened to 4-value set; `ToggleReactionResponse` gains `celebrateCount` (required), enum widened, description updated; `PostDto` schema gains `celebrateCount` (required, after `praisingCount`). All edits append-only / additive (no field reordering, no field removal). |
| 13 | Frontend types + reactionsStore + hook | [COMPLETE] | 2026-05-14 | Extended `ReactionTypeApi` to include `'celebrate'`. Added `celebrateCount` (PrayerRequest + PostDto) and `isCelebrating` (PrayerReaction + PerPostReactionApi + ToggleReactionResponse). `reactionsStore.ts` extended to 3-stage migration (pre-3.7 → fill 3; post-3.7-pre-6.6 → fill 2; post-6.6-pre-6.6b → fill 1). New `applyCelebrateFlip()` + `toggleCelebrate()` mirroring praising pattern. `usePrayerReactions` exposes `toggleCelebrate` (Pattern A — no mocking the hook). `postMappers.ts` defensively coerces `isCelebrating` for in-flight transitional data. Mock data + all hydration paths updated. |
| 14 | answered-wall-copy.ts | [COMPLETE] | 2026-05-14 | Added `ANSWERED_WALL_SUBHEAD_6_6B = 'Gratitude, not comparison.'`, `ANSWERED_WALL_INTRO` (survivorship-bias acknowledgment), `ANSWERED_TEXT_MISSING_FALLBACK`, `CELEBRATE_LABEL`/`CELEBRATE_INACTIVE_ARIA_LABEL`/`CELEBRATE_ACTIVE_ARIA_LABEL`, `SHARE_UPDATE_LABEL`/`EDIT_UPDATE_LABEL`, `UNMARK_ANSWERED_LABEL`/`UNMARK_ANSWERED_CONFIRM`, `ANSWERED_CATEGORY_CHIPS`, `MH_OMISSION_RATIONALE`. Old `ANSWERED_WALL_SUBHEAD` is now a forwarding alias to the new string. Post-review note (2026-05-14): `buildPraisingCelebratingCount` helper was initially exported but removed — D-Copy's separate-count format is realized via per-button inline counts (`Praising with you (12)` + `Celebrate (3)`) rather than a single combined string. Per-button rendering satisfies D-Copy's intent (counts are visible, separate, not summed) without duplicating beside the buttons. See the in-file comment for the rationale and the reintroduce-if-needed note. |
| 15 | AnsweredCategoryFilter.tsx | [COMPLETE] | 2026-05-14 | Created `frontend/src/components/prayer-wall/AnsweredCategoryFilter.tsx` + test (7 tests passing). Six chips, `role="radiogroup"` + `role="radio"` + `aria-checked`, URL-driven via `?category=`, "All" clears the param, NO Mental Health chip (Gate-G-MH-OMISSION HARD — comment in component + exported MH_OMISSION_RATIONALE). |
| 16 | CelebrateReaction.tsx | [COMPLETE] | 2026-05-14 | Created `frontend/src/components/prayer-wall/CelebrateReaction.tsx` + test (5 tests passing). Star icon, warm sunrise palette (`text-amber-200/70` inactive, `text-amber-300` active), auth-gated, sparkle sound on toggle, reduced-motion via global rule. Post-review (2026-05-14): added the `celebrate-sparkle` keyframe to `tailwind.config.js` (600ms `decelerate` easing, scale + rotate + amber drop-shadow per plan ED-8) and fire it on toggle-on (false → true) via a transient `isAnimating` state; toggle-off keeps the sound but skips the animation. Distinct from `pray-icon-pulse` (violet, 250ms, no rotate) and from Light-a-Candle's QuickLiftOverlay long-press treatment. Reduced-motion handled globally via the `motion-safe:` prefix. |
| 17 | InteractionBar showCelebrate | [COMPLETE] | 2026-05-14 | Added `showCelebrate?: boolean` + `onToggleCelebrate?: () => void` props mirroring showPraising. Renders `<CelebrateReaction>` after the Praising block when `showCelebrate=true`. All 20 existing InteractionBar tests still pass. Existing Praising block untouched (Gate-G-6.6-REGRESSION-SAFE). **6.6b-deferred-4 (cross-route Celebrate surfacing):** Spec § "Affected Frontend Routes" lists `/prayer-wall/:id`, `/prayer-wall/user/:id`, `/prayer-wall/dashboard` as places where Celebrate should surface on answered posts. Only `AnsweredWall.tsx` wires `showCelebrate` in this spec. Pushed back during code review — adding `showCelebrate` alone on those routes would create inconsistent UX, since shipped 6.6 also did not wire `showPraising` there. Fixing both is scope creep beyond 6.6b's four documented gap areas. Filed as 6.6b-deferred-4 in `spec-tracker.md` for a future follow-up that wires both `showPraising` and `showCelebrate` on the three remaining family routes atomically. |
| 18 | AnsweredWall.tsx surgical fixes | [COMPLETE] | 2026-05-14 | Replaced SEO description; replaced subhead `<p>` with `<h2>` (Lora italic, "Gratitude, not comparison."); added intro paragraph below subhead; mounted `<AnsweredCategoryFilter>` below tabs; wired `?category=` from URL into `listPosts({ sort: 'answered', category })`; wired `toggleCelebrate` + `showCelebrate` on InteractionBar. AnsweredWall.test.tsx updated to assert new subhead/intro + assert OLD subhead string is gone (regression guard). 6 tests passing. Surgical edits — no new component file (Gate-G-EXTEND-NOT-DUPLICATE). |
| 19 | PrayerCard.tsx fallback + affordances | [COMPLETE] | 2026-05-14 | Added missing-text fallback inside existing `answeredVariant && prayer.isAnswered` block. Added optional `onUnmark?: () => void` and `onEditAnsweredText?: (text: string) => void` props; PrayerCard auto-detects ownership via `useContext(AuthContext)` (defensive — no throw outside provider) and renders affordance row only for the author with callbacks provided. Author affordance row contains Edit/Share button (label flips based on `answeredText` presence) and a new `<UnmarkAnsweredDialog>` sibling component. 6 new tests (T13/T14/T15 + 3 sibling). All 53 PrayerCard tests + bb35-form-a11y tests passing. Exported `AuthContext` from `contexts/AuthContext.tsx` to enable safe optional read. |
| 20 | MarkAsAnsweredForm.tsx edit mode | [COMPLETE] | 2026-05-14 | Added `mode?: 'mark' \| 'edit'` (default `'mark'` for back-compat) and `initialText?: string` + `onCancel?: () => void` props. In edit mode the form skips the trigger button, prepopulates the textarea with `initialText`, and labels Confirm → "Save". Existing call sites in `PrayerDetail.tsx` + `PrayerWallDashboard.tsx` continue to get the legacy `'mark'` behavior with zero modification. `onConfirm(text)` signature preserved verbatim. |
| 21 | prayer-wall-api.ts helpers | [COMPLETE] | 2026-05-14 | No new helpers needed — `updatePost(id, { isAnswered: false })` and `updatePost(id, { answeredText })` both work against the existing PATCH `/api/v1/posts/{id}` endpoint. `toggleReaction(prayerId, 'celebrate')` works automatically once `ReactionTypeApi` was widened in Step 13. `AnsweredWall.tsx` wires `handleUnmark` + `handleEditAnsweredText` via `prayerWallApi.updatePost`; on success optimistically updates local `prayers` state (removes un-marked posts from the list; replaces edited posts in-place). |
| 22 | Frontend tests | [COMPLETE] | 2026-05-14 | 13 new frontend tests: T10/T11/T12 (AnsweredCategoryFilter — 7 tests via 7 assertions), 5 CelebrateReaction tests, 6 PrayerCard 6.6b tests (T13/T14/T15 + 3 sibling), 1 AnsweredWall test (existing test updated to assert new subhead/intro + regression-guard the old string), T-A1 (`usePrayerReactions — Spec 6.6b T-A1 cross-mount subscription for toggleCelebrate` — Pattern A discipline per Universal Rule 8; mutates the store from outside and asserts the component re-renders, NEVER mocks the hook). reactionsStore.test.ts + postMappers.test.ts + prayer-wall-api.test.ts existing assertions updated for the new 6-field shape. **Frontend full suite: 796 files / 10514 passing / 10 skipped / 0 failures.** |
| 23 | 11-local-storage-keys.md update | [COMPLETE] | 2026-05-14 | Updated `wr_prayer_reactions` entry to mention `isCelebrating` (5th boolean field), Spec 6.6b additions, and the new **three-stage migration** (pre-3.7 / post-3.7-pre-6.6 / post-6.6-pre-6.6b). |
| 24 | Verification sweep | [COMPLETE] | 2026-05-14 | **Frontend:** `pnpm build` clean; `pnpm lint` clean (removed two unused `eslint-disable no-console` directives in `AnsweredWall.tsx`); `pnpm test --run` full suite **796 files / 10,514 passing / 10 skipped / 0 failures**. **Backend:** `./mvnw test` full suite — **only 4 failures + 18 errors persist, ALL in pre-existing Upload baseline (missing test fixture files), identical to pre-execute baseline. Zero NEW regressions** from 6.6b. Shipped 6.6 INTERCESSION tests pass with zero assertion modifications (Gate-G-6.6-REGRESSION-SAFE verified). `/verify-with-playwright` deferred to user — invoke `/verify-with-playwright _plans/forums/2026-05-14-spec-6-6b.md` for the manual UI sweep on `/prayer-wall/answered`. |
