# Forums Wave Plan: Spec 2.9 — Phase 2 Cutover

**Spec:** `_specs/forums/spec-2-9.md`
**Master Plan:** `_forums_master_plan/round3-master-plan.md` → Spec 2.9 (line 2821)
**Date:** 2026-04-27
**Branch:** `forums-wave-continued` (no per-spec sub-branch)
**Phase:** 2 — Activity Engine Migration (Dual-Write)
**Size:** S
**Risk:** Medium (live cutover — flips dual-write from off to on across all dev/prod builds; first time the backend receives real activity traffic from a real frontend feature)

---

## Affected Frontend Routes

N/A — backend traffic activation spec. The only code change is a flag default flip in `frontend/.env.example`. No new components, no UI changes, no new routes. `/verify-with-playwright` is not run for this spec; runtime verification happens via the manual smoke test documented in the cutover checklist (Eric's responsibility, not CC's).

---

## Universal Rules Checklist

All 17 Universal Rules from `_forums_master_plan/round3-master-plan.md`. Marked applicable (✅), not applicable (N/A), or needs-attention (⚠️):

- [x] Rule 1: ✅ No git operations by CC. Eric handles all git per spec Guardrails.
- [x] Rule 2: ✅ Master Plan Quick Reference + Spec 2.9 entry (line 2821) read. Two divergences (checklist path; vite.config.ts not modified) and one Universal Rule 17 retrofit pinned in spec.
- [x] Rule 3: N/A — no Liquibase changesets. Zero schema changes.
- [x] Rule 4: N/A — `openapi.yaml` unchanged. Zero API changes.
- [x] Rule 5: N/A — no user-facing strings introduced. The cutover checklist is internal operator documentation, not user-facing copy.
- [x] Rule 6: N/A — no automated tests added. Spec § "Tests Required" explicitly states "Adding automated tests for a flag flip is busywork." Vitest from Spec 2.7 (10 tests across both flag states) and drift detection from Spec 2.8 already cover the relevant correctness surface.
- [x] Rule 7: N/A — no new `wr_*` localStorage keys.
- [x] Rule 8: N/A — no new components or reactive store consumers.
- [x] Rule 9: ⚠️ — A11y is touched only via Rule 17 retrofit (see below). Scope is "verify existing routes still scan clean," NOT new accessibility work, because Phase 2 added no UI.
- [x] Rule 10: ⚠️ — `pnpm build` must remain clean with the flag set to `true`. No new bundle weight is expected (the flag flip is one string change). Verified in Step 5.
- [x] Rule 11: N/A — no new user-facing copy. The phase02 checklist follows pastor's-wife voice in operator-internal sections that include user-facing references (e.g., the rollback paragraph), but no new strings ship to end users.
- [x] Rule 12: N/A — no anti-pressure copy surfaces touched.
- [x] Rule 13: N/A — no crisis-detection paths touched. POST `/api/v1/activity` already lives behind backend rate limits (Spec 1) and JWT auth (Spec 1.4); this cutover changes the FREQUENCY of writes from zero to dual-write but does not introduce new content paths.
- [x] Rule 14: N/A — no user-generated content paths touched.
- [x] Rule 15: N/A — no new endpoints. Existing `/api/v1/proxy/**` Caffeine bucket from Spec 1 is the rate-limit envelope; `/api/v1/activity` from Spec 2.6 sits outside the proxy ratelimit but inherits filter chain defaults (request ID, JWT, security headers).
- [x] Rule 16: ✅ — phase02 checklist mirrors phase01 checklist structure (numbered H2 sections, bracketed placeholders, sign-off footer). Recon verified the existing format precedent.
- [x] Rule 17: ⚠️ — Per-phase cutover accessibility smoke test required. CC scaffolds the placeholder evidence files (`_cutover-evidence/phase2-a11y-smoke.json` + `_cutover-evidence/phase2-a11y-notes.md`); Eric runs the actual axe-core scan + keyboard walkthrough + VoiceOver spot-check during the smoke test execution and replaces the placeholder content. Phase 2 added zero new user-visible UI, so the scan is a regression check on existing routes (`/`, `/daily`, `/prayer-wall`, `/bible`, `/music`).

---

## Architecture Context

From reconnaissance:

### Spec category

**This is a CUTOVER spec.** Per the spec-category guidance below, cutover specs flip a feature flag from `false` to `true` and declare a phase complete. Required structure: flag flip step, smoke test step, Universal Rule 17 a11y evidence, rollback readiness step. All four are present in the implementation steps below (smoke test is operator-executed, not code-executed; rollback is documented in the checklist body).

### Verified recon facts

- **Flag location & value (verified):** `frontend/.env.example` line 30 reads `VITE_USE_BACKEND_ACTIVITY=false`. Lines 19–29 contain a comment block explaining the dual-write strategy, naming Spec 2.9 as the cutover owner. The block ends with the line "Default: false. Cutover owned by Spec 2.9." This spec replaces both the value (`false`→`true`) and updates the comment to reflect cutover completion.
- **Flag consumer (verified):** `frontend/src/lib/env.ts` lines 70–85 expose `isBackendActivityEnabled()` with strict equality `=== 'true'` (fail-closed). `frontend/src/hooks/useFaithPoints.ts:31` imports it and `:285` calls it inside `recordActivity`. Test coverage at `frontend/src/lib/__tests__/env.test.ts` covers `'true'`, `'false'`, `''`, `undefined`, `'TRUE'`, `'1'`, `'yes'` (7 cases) — these tests are flag-state-agnostic and continue to pass after the cutover.
- **No vite.config.ts reference (verified):** `grep -n VITE_USE_BACKEND_ACTIVITY frontend/vite.config.ts` returns zero matches. Per Architectural Decision #5 in the spec, do NOT touch vite.config.ts. The master plan body's instruction to "Update `.env.example` and `frontend/vite.config.ts` defaults" is wrong on this point.
- **Cutover checklist precedent (verified):** `_plans/forums/phase01-cutover-checklist.md` exists and uses the exact pattern this spec mimics — numbered H2 sections, bracketed placeholders, deferred-with-tracking-note items, scope-extension subsections, sign-off footer. Phase 2 checklist mirrors this structure scoped to dual-write activation rather than first-time platform deploy.
- **Cutover evidence directory (verified):** `_cutover-evidence/` already exists with `phase1-a11y-smoke.json` (~4.4 MB) and `capture-axe-evidence.mjs`. Phase 2 evidence files (`phase2-a11y-smoke.json` + `phase2-a11y-notes.md`) join the same directory. Eric can adapt `capture-axe-evidence.mjs` by extending the `ROUTES` constant.
- **Drift fixture exists (verified):** `_test_fixtures/activity-engine-scenarios.json` and `_test_fixtures/README.md` are present; Spec 2.8 already wires the parity test. With drift detection in place, flipping the flag is safer.
- **Followups file exists (verified):** `_plans/post-1.10-followups.md` is the canonical place to log smoke-test deviations per Phase 1 precedent. The phase02 checklist's rollback section refers to this file by name.
- **Frontend Dockerfile pattern (verified):** `frontend/Dockerfile` has `ARG VITE_API_BASE_URL` + `ENV VITE_API_BASE_URL=$VITE_API_BASE_URL` (lines 7–8) before the build. The phase02 checklist instructs Eric to verify whether `VITE_USE_BACKEND_ACTIVITY` needs the same wiring; that verification is operator work, not CC work.
- **CLAUDE.md decision (verified):** CLAUDE.md "Implementation Phases" section is high-level. Adding a Phase 2 enumeration before Spec 2.10 closes the wave is premature. Per Architectural Decision #6, leave CLAUDE.md unchanged. (Recon F in the spec already documented this decision.)

### Files this spec will not touch

Per spec § "Files NOT to modify" and Guardrails:

- `vite.config.ts`
- `CLAUDE.md`
- `frontend/src/lib/env.ts` (the docstring "Default: false. Cutover owned by Spec 2.9." is a courtesy update; not in scope)
- `frontend/src/hooks/useFaithPoints.ts`
- Any `backend/**` file
- `openapi.yaml`
- `master.xml` or any Liquibase changeset
- Any test file
- `_forums_master_plan/spec-tracker.md`

---

## Database Changes

None. Zero schema changes in this spec.

---

## API Changes

None. Zero API changes in this spec.

---

## Assumptions & Pre-Execution Checklist

- [ ] Prerequisite spec 2.8 (Drift Detection Test) is complete and the fixture-driven parity test ships at `_test_fixtures/activity-engine-scenarios.json` (verified during recon — fixture and README both present).
- [ ] Working tree is clean before execution begins (CC may modify `frontend/.env.example` and create three new files; Eric handles all git).
- [ ] Branch is `forums-wave-continued`. CC does NOT switch branches.
- [ ] No automated tests added. Vitest baseline is 8,811 pass / 11 known-fail; verifying baseline holds after the flag flip is part of Step 6.
- [ ] No backend changes. CC does not start the backend, does not modify Java code, does not edit Liquibase changesets.

---

## Spec-Category-Specific Guidance

### This is a CUTOVER spec

Per `/plan-forums` skill guidance, cutover specs flip a feature flag from `false` to `true` and declare a phase complete. Required steps:

1. **Flag flip step** — set `VITE_USE_BACKEND_ACTIVITY=true` in `frontend/.env.example`. Update the comment block to reflect cutover completion. **Implemented in Step 1.**
2. **Smoke test step** — exercise the happy path for every flow touched in the phase. **Implemented in the phase02-cutover-checklist.md body (Step 2).** CC writes the checklist; Eric runs the smoke test.
3. **Universal Rule 17 accessibility smoke test** — `_cutover-evidence/phase2-a11y-smoke.json` from an axe-core run + keyboard walkthrough + VoiceOver spot-check notes. **Implemented in Steps 3 and 4 as placeholder scaffolds**; Eric runs the actual axe-core scan during the smoke test execution and replaces placeholder content.
4. **Rollback readiness step** — documented in the checklist body, NOT as a code change. **Implemented in Step 2 (the checklist's "Rollback plan" section).**

The four required cutover-spec elements all map to specific implementation steps below.

---

## Edge Cases & Decisions

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Update `vite.config.ts` defaults? | NO | Recon verified zero references in `vite.config.ts`. Per Architectural Decision #5 in the spec, the master plan body's instruction is wrong on this point. The flag's runtime read happens in `lib/env.ts`. |
| Update `lib/env.ts` docstring ("Default: false. Cutover owned by Spec 2.9.")? | NO | Per Architectural Decision #7 in the spec, this is a courtesy update, not a deliverable. Out of scope. |
| Update CLAUDE.md? | NO | Per Architectural Decision #6 in the spec and Recon F. Phase 2 not closed yet (Spec 2.10 remains). Phase summary belongs in a post-2.10 hygiene update. |
| Generate fake axe-core JSON for the placeholder? | NO | Per Guardrail "Do NOT generate the `_cutover-evidence/*` files with fake axe-core JSON output." Placeholder is a clear scaffold noting Eric's authorship, not synthesized scan output. |
| Flip Railway prod env var as part of this spec? | NO | Per Divergence 2 in the spec, the Railway env var is operator work documented in the checklist body. CC does not have Railway access. |
| Author placeholder JSON as `{}` or as a structured stub with markers? | Structured stub | A `{}` empty object is valid JSON but offers no guidance. The placeholder should clearly self-identify as needing replacement during smoke-test execution and reference the existing `capture-axe-evidence.mjs` script as the source of real content. |
| Add automated test for the flag flip? | NO | Per Guardrail "Do NOT add automated tests for the flag flip." Existing Vitest tests at `frontend/src/lib/__tests__/env.test.ts` verify the helper's behavior across all string-equality cases. |
| Modify `_forums_master_plan/spec-tracker.md`? | NO | Per Guardrail. Eric flips tracker rows manually after commit. |
| Smoke 5 (Music) listen path: fix during cutover if backend rejects? | NO | Per spec § "Deliverable Structure → Smoke 5 NOTE", the listen path may bypass `useFaithPoints.recordActivity`. If no row appears, that's the documented Spec 2.7 followup gap. Record observed behavior, do NOT fix in this cutover. |

---

## Implementation Steps

### Step 1: Flip `VITE_USE_BACKEND_ACTIVITY` default and update comment block

**Objective:** The single line of production code change in this spec. Flips the dual-write default from off to on for new clones and the Vite build pipeline. Updates the explanatory comment so it no longer claims "Default: false" or names Spec 2.9 as a future cutover owner.

**Files to create/modify:**

- `frontend/.env.example` — modify lines 19–30 (the dual-write comment block + the flag value).

**Details:**

Replace lines 19–30 in `frontend/.env.example` with:

```
# ---------------------------------------------------------------------------
# Phase 3 Forums Wave — backend migration flags
# ---------------------------------------------------------------------------
# Dual-write feature flags for migrating frontend features to the backend.
# Default `true` means the dual-write to the backend fires alongside the
# existing localStorage write. Reads remain localStorage-canonical until a
# future "promote backend reads" spec flips that piece. New cutover flags
# in subsequent phases follow the same activation pattern.

# Activity dual-write: POST /api/v1/activity after each localStorage activity write.
# Activated by Spec 2.9 cutover on 2026-04-27.
VITE_USE_BACKEND_ACTIVITY=true
```

**Notes about this rewrite:**

- The block-level header "Phase 3 Forums Wave — backend migration flags" is preserved so future flags (e.g., `VITE_USE_BACKEND_FRIENDS`, `VITE_USE_BACKEND_PRAYER_WALL`) can sit alongside this one without rewriting the header.
- The descriptive paragraph is rewritten to reflect post-cutover state: "Default `true`" instead of "Default `false`", "Activated by Spec 2.9 cutover on 2026-04-27" instead of "Cutover owned by Spec 2.9."
- The blank line between the explanatory paragraph and the flag line is preserved (existing convention).
- The flag's specific docstring still describes what it does ("POST /api/v1/activity after each localStorage activity write.") — that line is correct and stays.

**Guardrails (DO NOT):**

- Do NOT modify `vite.config.ts` (recon verified zero references; Architectural Decision #5).
- Do NOT modify `frontend/src/lib/env.ts` (Architectural Decision #7; the docstring update is a courtesy, not a deliverable).
- Do NOT modify any `.env.local` file (gitignored; Eric handles his local override per checklist).
- Do NOT modify `frontend/src/hooks/useFaithPoints.ts` (Spec 2.7's wiring is correct as-is).
- Do NOT add new env vars beyond the existing `VITE_USE_BACKEND_ACTIVITY` line.
- Do NOT touch the AI/third-party API key block (lines 32+ in `.env.example`).
- Do NOT switch branches.

**Verification:**

- [ ] `grep -n "VITE_USE_BACKEND_ACTIVITY=true" frontend/.env.example` returns exactly one match on the expected line.
- [ ] `grep -n "VITE_USE_BACKEND_ACTIVITY=false" frontend/.env.example` returns zero matches.
- [ ] `grep -n "Default: false" frontend/.env.example` returns zero matches (the old comment is fully replaced).
- [ ] `grep -n "Activated by Spec 2.9 cutover on 2026-04-27" frontend/.env.example` returns one match.
- [ ] `grep -rn "VITE_USE_BACKEND_ACTIVITY" frontend/vite.config.ts` returns zero matches (vite.config.ts unchanged).

**Test specifications:**

| Test | Type | Description |
|------|------|-------------|
| (none) | — | No new tests. Existing `frontend/src/lib/__tests__/env.test.ts` (7 cases) covers the helper across all relevant string-equality scenarios. |

**Expected state after completion:**

- [ ] `frontend/.env.example` contains `VITE_USE_BACKEND_ACTIVITY=true` and the surrounding comment block reflects post-cutover state.
- [ ] No other file modified.

---

### Step 2: Author `_plans/forums/phase02-cutover-checklist.md`

**Objective:** Produce the cutover checklist that is itself the spec's primary deliverable per Architectural Decision #3. Eric works through the checkbox items during execution; this file is committed to Git and serves as the audit trail for Phase 2 completion and the template for future-phase cutovers.

**Files to create/modify:**

- `_plans/forums/phase02-cutover-checklist.md` — new file.

**Details:**

The checklist mirrors `_plans/forums/phase01-cutover-checklist.md` structure (numbered H2 sections with checkbox items, scope-extension subsections where applicable, sign-off footer with placeholder dates) scoped to dual-write activation rather than first-time platform deploy.

**Required sections (in order):**

1. **Title + frontmatter:** `# Phase 2 Cutover Checklist — Activity Engine Dual-Write`. Single-line purpose, single-line phase status before this spec ships, single-line cutover date placeholder.
2. **Overview:** 1–2 paragraphs describing what Phase 2 delivered (5 backend tables: `activity_log`, `faith_points`, `streak_state`, `user_badges`, `activity_counts`; 4 pure-calculation services; 1 endpoint POST `/api/v1/activity`; 1 frontend hook change at `useFaithPoints.recordActivity`), why this cutover happens now (drift detection from Spec 2.8 makes the flip safe), what changes for users (nothing visible) and for the backend (real traffic for the first time).
3. **`## 1. Pre-flight checks`** — branch verification (`forums-wave-continued`), working tree clean, Docker running, backend Postgres container running (`docker-compose up`), backend dev server running (`./mvnw spring-boot:run`), `/api/v1/health` returns 200, frontend dev server running (`pnpm dev`), logged in as a dev seed user (e.g., `dev1@worshiproom.com`).
4. **`## 2. Code change`** — the four bullets from spec § "Deliverable Structure":
   - Flip `VITE_USE_BACKEND_ACTIVITY=true` in `frontend/.env.example` (CC has done this in Step 1; Eric verifies)
   - Update the comment block (CC has done this in Step 1; Eric verifies)
   - Set `VITE_USE_BACKEND_ACTIVITY=true` in local `frontend/.env.local` (Eric does this manually because `.env.local` is gitignored)
   - Restart frontend dev server (Vite env vars are build-time)
5. **`## 3. Build verification`** — `pnpm tsc --noEmit` clean, `pnpm lint` clean, `pnpm build` clean, `pnpm test` clean (Vitest baseline 8,811 pass / 11 known-fail; any NEW failing file is a regression).
6. **`## 4. Manual smoke test — 5 activity types`** — five subsections, one per activity (Pray, Journal, Meditate, Bible, Music). Each subsection enumerates: tap/use the feature in the dev frontend, verify localStorage updated (DevTools → Application → LocalStorage → `wr_daily_activities`, today's flag set), verify backend `activity_log` row inserted (cross-reference Section 5), verify `activity_counts` incremented, verify `faith_points` reflects new total, verify `streak_state` updated. The Music subsection includes the documented gap NOTE per spec § "Deliverable Structure → Smoke 5" — if no row appears, capture observed behavior; do NOT fix in this cutover.
7. **`## 5. Backend verification queries`** — five SQL `SELECT` queries against the dev DB via `psql`:
   - Most recent `activity_log` rows for the test user (ORDER BY occurred_at DESC LIMIT 20)
   - Current `activity_counts` for the test user
   - Current `faith_points` for the test user (total_points, current_level, last_updated)
   - Current `streak_state` for the test user (current_streak, longest_streak, last_active_date)
   - Newly earned `user_badges` for the test user (badge_id, earned_at, display_count)
   - Plus a helper SELECT to find the test user UUID by email
   All queries use `<test-user-uuid>` as a placeholder — Eric substitutes the actual UUID. Use plain triple-backtick `sql` fencing (the spec body's escaped backticks are an artifact of the spec living inside a markdown block; the actual checklist file uses plain fences).
8. **`## 6. localStorage canonicality verification`** — refresh the frontend, verify all UI elements (points total, streak counter, recent badges, daily activity card) reflect the SAME state visible before refresh. Confirms localStorage remains source of truth for reads; backend traffic is shadow only.
9. **`## 7. Accessibility smoke (Universal Rule 17)`** — six checkbox items per spec:
   - Run axe-core scan on `/`, `/daily`, `/prayer-wall`, `/bible`, `/music` (Phase 2 modified the activity-recording hook only; no new components — scope is "verify existing routes still scan clean")
   - Save axe-core JSON output to `_cutover-evidence/phase2-a11y-smoke.json` (replace placeholder content created in Step 3)
   - Zero CRITICAL violations across the scanned routes
   - Keyboard-only walkthrough of the daily hub primary flow (open daily hub → tap pray → see activity recorded indicator) completes without dead-ends
   - VoiceOver spot-check on Prayer Wall pray-button interaction completes without blocking issues
   - Brief notes saved to `_cutover-evidence/phase2-a11y-notes.md` (replace placeholder content created in Step 4)
   The section header notes that Eric can adapt `_cutover-evidence/capture-axe-evidence.mjs` from Phase 1 by extending its `ROUTES` constant.
10. **`## 8. Production deploy plan`** — five checkbox items per spec:
    - Verify Railway frontend Dockerfile exposes `VITE_USE_BACKEND_ACTIVITY` (mirror the `VITE_API_BASE_URL` pattern from Spec 1.10 — `ARG` + `ENV` before `RUN pnpm build`). If missing, add a one-line Dockerfile patch.
    - Add `VITE_USE_BACKEND_ACTIVITY=true` to Railway frontend service env vars (Variables tab → "+ New Variable")
    - Trigger a Railway deploy (push to `main`, or manual deploy)
    - After deploy completes: smoke test 1 activity in prod (use a real account, tap pray, verify backend received the row via `railway connect postgres`)
    - If prod smoke test fails: see Rollback plan below
11. **`## 9. Rollback plan (if prod surfaces issues)`** — seven numbered steps per spec:
    1. Set `VITE_USE_BACKEND_ACTIVITY=false` in Railway frontend service env vars
    2. Trigger a redeploy (or wait for next auto-deploy)
    3. Verify in DevTools that subsequent `recordActivity` calls no longer fire `POST /api/v1/activity`
    4. localStorage data is untouched; users see no difference
    5. Backend rows already written remain (shadow data, no consumer)
    6. Open a followup entry in `_plans/post-1.10-followups.md` describing what failed
    7. Do NOT revert the spec's commit. The flag-flip stays; the env var override is the rollback mechanism.
12. **`## 10. Sign-off`** — bracketed placeholder fields for Cutover date, Eric's sign-off, Local smoke test passed, Production smoke test passed, A11y evidence committed, Phase 2 status (final state: "COMPLETE pending Spec 2.10 (historical backfill).").

**Tone & voice:**

- Operator-internal voice (matches phase01 checklist). Direct, factual, blameless.
- No exclamation points anywhere.
- Where the checklist mentions user-facing behavior (e.g., the rollback paragraph noting "users see no difference"), the language remains warm and accurate — anti-pressure consistent.

**Guardrails (DO NOT):**

- Do NOT include automated test instructions in the checklist (per spec § "Tests Required").
- Do NOT instruct Eric to revert the cutover commit on rollback (per spec § "Architectural Decision #4" — the flag-flip stays; rollback is via Railway env var override).
- Do NOT include the documented gap fix in Smoke 5 — capture observed behavior only (per spec Out of Scope).
- Do NOT prescribe SQL DDL or DROP statements; all queries are read-only `SELECT`.
- Do NOT add JWT secrets, real user emails, or Railway service IDs to the checklist body. Use placeholders.
- Do NOT include axe-core JSON inline in the markdown — the JSON lives in its own file (Step 3).
- Do NOT treat this checklist as a one-time scratchpad — it stays in Git as the audit trail for Phase 2.

**Verification:**

- [ ] File exists at `_plans/forums/phase02-cutover-checklist.md`.
- [ ] All 12 sections present and ordered as specified (Title/frontmatter, Overview, Pre-flight, Code change, Build verification, Manual smoke test, Backend verification queries, localStorage canonicality, A11y smoke, Production deploy plan, Rollback plan, Sign-off).
- [ ] All five activity smokes (Pray, Journal, Meditate, Bible, Music) present with the documented Music NOTE.
- [ ] All five SQL queries + helper UUID lookup present in Section 5.
- [ ] All seven rollback steps present in Section 9 (Step 7 explicitly says "Do NOT revert the spec's commit").
- [ ] Sign-off section uses bracketed placeholder fields (`____________`) per phase01 precedent.
- [ ] No exclamation points anywhere in the file.
- [ ] No real Railway service IDs, no real user emails, no real JWT secrets — only placeholders.

**Test specifications:**

| Test | Type | Description |
|------|------|-------------|
| (none) | — | Documentation file, no tests. |

**Expected state after completion:**

- [ ] `_plans/forums/phase02-cutover-checklist.md` exists and matches spec § "Deliverable Structure" requirements.

---

### Step 3: Create `_cutover-evidence/phase2-a11y-smoke.json` placeholder

**Objective:** Scaffold a JSON file that Eric replaces with real axe-core scan output during the smoke-test execution. The placeholder self-identifies and references the existing capture script so future readers (or Eric on a different day) know what's needed.

**Files to create/modify:**

- `_cutover-evidence/phase2-a11y-smoke.json` — new file.

**Details:**

The placeholder is structured JSON, not an empty object. It self-identifies as needing replacement and embeds operator instructions in a `_placeholder` key that Eric removes when he writes the real output.

```json
{
  "_placeholder": "Replace this object with axe-core scan output captured by Eric during the Phase 2 cutover smoke test. See _cutover-evidence/capture-axe-evidence.mjs from Phase 1 for the canonical capture script — extend the ROUTES array to include /, /daily, /prayer-wall, /bible, /music.",
  "_authoredBy": "Eric Champlin",
  "_authoredDuring": "Phase 2 cutover smoke test (Spec 2.9)",
  "_expectedShape": {
    "captured": "ISO 8601 timestamp",
    "frontendUrl": "URL of the frontend that was scanned (local dev or deployed)",
    "results": "object keyed by route, each value is the @axe-core/playwright AxeResults shape"
  }
}
```

**Tone & voice:**

- Operator-internal. The `_placeholder` description tells the next reader exactly what to do.
- Use leading-underscore keys (`_placeholder`, `_authoredBy`, etc.) so even an automated JSON consumer (if any future tool processes this directory) treats them as metadata, not as data.

**Guardrails (DO NOT):**

- Do NOT generate fake axe-core results. Per spec Guardrail "Do NOT generate the `_cutover-evidence/*` files with fake axe-core JSON output."
- Do NOT use `null` or `{}` — both are valid JSON but provide no instruction to the reader.
- Do NOT pre-fill the `captured`, `frontendUrl`, or `results` keys with stub data. The `_expectedShape` documentation key carries the structural contract; values are Eric's responsibility.

**Verification:**

- [ ] File exists at `_cutover-evidence/phase2-a11y-smoke.json`.
- [ ] File parses as valid JSON: `node -e "JSON.parse(require('fs').readFileSync('_cutover-evidence/phase2-a11y-smoke.json'))"` exits 0.
- [ ] File contains `_placeholder` key with text referencing `capture-axe-evidence.mjs`.
- [ ] File does NOT contain any `violations`, `passes`, `inapplicable`, or `incomplete` keys (those are real axe output keys; their absence proves no fake data was synthesized).
- [ ] File size is < 1 KB (Phase 1's real output is ~4.4 MB; this scaffold should be tiny).

**Test specifications:**

| Test | Type | Description |
|------|------|-------------|
| (none) | — | Placeholder evidence file, no tests. |

**Expected state after completion:**

- [ ] `_cutover-evidence/phase2-a11y-smoke.json` exists as a clearly-marked placeholder.

---

### Step 4: Create `_cutover-evidence/phase2-a11y-notes.md` placeholder

**Objective:** Scaffold a markdown file with the structure Eric will fill in during keyboard navigation and VoiceOver spot-check phases of the smoke test.

**Files to create/modify:**

- `_cutover-evidence/phase2-a11y-notes.md` — new file.

**Details:**

The placeholder presents the structure Eric fills in (one section per acceptance criterion in Universal Rule 17), leaving prompts and checkboxes that turn into evidence on completion.

```markdown
# Phase 2 Accessibility Notes — Cutover Smoke Test

**Status:** PLACEHOLDER — replace this file's body during the Phase 2 cutover smoke test execution. CC scaffolds the structure; Eric authors the real content.

**Cutover spec:** Spec 2.9 — Phase 2 Cutover
**Cutover date:** _____________ (Eric fills in)
**Authored by:** Eric Champlin
**Pairs with:** `_cutover-evidence/phase2-a11y-smoke.json` (axe-core JSON output)

---

## Scope (Universal Rule 17)

Phase 2 modified the `useFaithPoints.recordActivity` hook only; no new user-visible UI was added. The accessibility smoke test verifies that the routes touched indirectly by Phase 2 still scan clean — it is a regression check, not a net-new audit.

**Routes scanned:** `/`, `/daily`, `/prayer-wall`, `/bible`, `/music`.

---

## 1. axe-core automated scan

- [ ] Scan completed against the routes listed above
- [ ] Zero CRITICAL violations across all scanned routes
- [ ] JSON output committed to `_cutover-evidence/phase2-a11y-smoke.json`
- [ ] Any non-CRITICAL findings recorded as follow-up entries in `_plans/post-1.10-followups.md`

**Findings summary:** _____________ (Eric fills in: e.g., "0 CRITICAL, 0 SERIOUS — scan clean" or "0 CRITICAL, 1 SERIOUS color-contrast on /prayer-wall (existing follow-up from Phase 1)")

---

## 2. Keyboard-only walkthrough

Primary flow: open Daily Hub → tap Pray → see activity recorded indicator.

- [ ] Walkthrough completed without dead-ends
- [ ] Skip-to-main-content link reachable from page load
- [ ] All interactive elements reachable via Tab
- [ ] Focus indicators visible at every step

**Notes:** _____________ (Eric fills in: any blocked-flow observations, focus management deviations, or "clean")

---

## 3. VoiceOver (macOS) spot-check

Target: Prayer Wall pray-button interaction.

- [ ] Spot-check completed without blocking issues
- [ ] Pray button announced with role and label
- [ ] State change after tap announced (e.g., "Praying" → "Prayed for")
- [ ] No phantom announcements or stale regions

**Notes:** _____________ (Eric fills in: any deviations or "clean")

---

## Sign-off

- [ ] All three checks completed (or explicitly deferred with tracking note in `_plans/post-1.10-followups.md`)
- [ ] Phase 2 a11y smoke evidence committed
- [ ] Cutover sign-off date: _____________
```

**Tone & voice:**

- Operator-internal. Match the phase01 cutover checklist tone (factual, blameless).
- Bracketed `_____________` placeholders match Phase 1 precedent.
- No exclamation points.

**Guardrails (DO NOT):**

- Do NOT pre-fill the "Findings summary" or "Notes" sections with synthesized observations.
- Do NOT pre-check any of the checkboxes — they're for Eric to mark.
- Do NOT remove the "PLACEHOLDER" status header until Eric replaces the body during execution.

**Verification:**

- [ ] File exists at `_cutover-evidence/phase2-a11y-notes.md`.
- [ ] File contains the "PLACEHOLDER" status header.
- [ ] File contains all three sections (axe-core scan, keyboard walkthrough, VoiceOver spot-check) with checkboxes.
- [ ] File contains a Sign-off section with bracketed placeholder for cutover date.
- [ ] No checkboxes are pre-checked.
- [ ] No exclamation points anywhere in the file.

**Test specifications:**

| Test | Type | Description |
|------|------|-------------|
| (none) | — | Placeholder evidence file, no tests. |

**Expected state after completion:**

- [ ] `_cutover-evidence/phase2-a11y-notes.md` exists as a clearly-marked placeholder.

---

### Step 5: Build verification

**Objective:** Confirm the flag flip doesn't break the production build. The flip is one string change in `.env.example`, but Vite reads `.env.example` only as a documentation file (not as runtime config) — so the build outcome should be identical pre- and post-flip. Verifying the build passes is belt-and-suspenders for Rule 10 (performance) and the spec's "Build still succeeds" acceptance criterion.

**Files to create/modify:** None (verification only).

**Details:**

Run from the repo root:

```
cd frontend && pnpm build
```

Expected: clean build with no new errors. Bundle size should be unchanged or trivially different (the flag flip is a single string change in `.env.example`, which Vite does not read for runtime config — only `.env.local` is read for local dev defaults).

**Note on `.env.local`:** CC does NOT touch `.env.local` (gitignored). If Eric's `.env.local` does not have `VITE_USE_BACKEND_ACTIVITY` set, his local dev build defaults to whatever Vite auto-loads. This is fine for the production build verification — `pnpm build` does not depend on `.env.local`.

**Guardrails (DO NOT):**

- Do NOT modify `.env.local` (gitignored; Eric controls his own).
- Do NOT clear caches with `rm -rf node_modules` or similar — full re-install is unnecessary and slow.
- Do NOT skip this step. The Phase 1 cutover learned the hard way that Dockerfile env-var wiring + Vite build-time inlining is fragile; verifying a clean build catches surprises early.

**Verification:**

- [ ] `pnpm build` exits 0.
- [ ] No NEW TypeScript errors compared to the pre-flip baseline (any pre-existing errors are out of scope).
- [ ] No NEW build warnings flagged as errors.
- [ ] Dist output produced under `frontend/dist/`.

**Test specifications:**

| Test | Type | Description |
|------|------|-------------|
| (none) | — | Build is the verification, not a test. |

**Expected state after completion:**

- [ ] `pnpm build` clean.

---

### Step 6: Vitest baseline verification

**Objective:** Confirm Vitest passes at the documented baseline (8,811 pass / 11 known-fail) with the flag set to `true`. The 7 existing tests in `frontend/src/lib/__tests__/env.test.ts` cover the helper across all string-equality cases (including `'true'`, `'false'`, `''`, `undefined`, `'TRUE'`, `'1'`, `'yes'`) — they are flag-state-agnostic and continue to pass after the flip. Spec 2.7's 10 dual-write tests run both flag states explicitly via `vi.stubEnv`, so they're also unaffected by the `.env.example` change.

**Files to create/modify:** None (verification only).

**Details:**

Run from `frontend/`:

```
pnpm test
```

Expected: baseline 8,811 pass / 11 known-fail. Per CLAUDE.md "Build Health" section, the 11 failures are documented tech debt (orphan test for a deleted hook, CSS class drift in one plan browser test, logged-out mock listing cards in 4 Local Support pages, Pray loading-text timing flake). Any NEW failing file or fail count > 11 is a regression and a hard stop.

**Guardrails (DO NOT):**

- Do NOT add new test files for the flag flip.
- Do NOT modify existing test files in this spec (per spec § "Files NOT to modify").
- Do NOT change the Vitest config to suppress the 11 known failures — they remain visible.

**Verification:**

- [ ] `pnpm test` completes without crashing.
- [ ] Pass count is 8,811 (or higher, if the post-Spec-2.8 baseline has shifted).
- [ ] Fail count is 11 (or lower, if any of the 11 known-fail tests have been fixed since the baseline was recorded).
- [ ] Zero NEW failing files compared to baseline.

**Test specifications:**

| Test | Type | Description |
|------|------|-------------|
| (none) | — | Test suite is the verification. |

**Expected state after completion:**

- [ ] Vitest passes at or above baseline.
- [ ] All Phase 2 deliverables (Specs 2.1–2.8) unchanged.
- [ ] All acceptance criteria from spec § "Acceptance Criteria" are met.

---

## Step Dependency Map

| Step | Depends On | Description |
|------|------------|-------------|
| 1 | — | Flip `VITE_USE_BACKEND_ACTIVITY` default in `frontend/.env.example` |
| 2 | — | Author `_plans/forums/phase02-cutover-checklist.md` |
| 3 | — | Create `_cutover-evidence/phase2-a11y-smoke.json` placeholder |
| 4 | — | Create `_cutover-evidence/phase2-a11y-notes.md` placeholder |
| 5 | 1 | Build verification (`pnpm build`) — must run AFTER Step 1 because the flag flip is what we're verifying does not break the build |
| 6 | 1 | Vitest baseline verification (`pnpm test`) — must run AFTER Step 1 for the same reason |

Steps 1–4 have no dependencies on each other and could in theory run in any order; Step 1 is listed first because it is the spec's "actual code change" while 2–4 are documentation. Steps 5 and 6 are independent of each other (build and test both run from `frontend/` and don't share state) and could run in parallel — but per project convention, Step 5 (build) runs before Step 6 (test) so that test errors aren't conflated with build errors.

---

## Execution Log

| Step | Title | Status | Completion Date | Notes / Actual Files |
|------|-------|--------|-----------------|----------------------|
| 1 | Flip `VITE_USE_BACKEND_ACTIVITY` default | [COMPLETE] | 2026-04-27 | Modified `frontend/.env.example` lines 19-30: comment block rewritten to reflect post-cutover state, `VITE_USE_BACKEND_ACTIVITY=false` → `true`. All 5 verification checks pass. |
| 2 | Author `_plans/forums/phase02-cutover-checklist.md` | [COMPLETE] | 2026-04-27 | Created `_plans/forums/phase02-cutover-checklist.md` with all 12 sections (frontmatter, Overview, plus 10 numbered H2 sections from Pre-flight through Sign-off). All 5 activity smokes (Pray/Journal/Meditate/Bible/Music) with documented Music gap NOTE. All 6 SQL queries (5 verification + 1 helper). All 7 rollback steps including "Do NOT revert the spec's commit". Bracketed placeholder fields in Sign-off. Zero exclamation points. Zero real Railway service IDs. |
| 3 | Create `_cutover-evidence/phase2-a11y-smoke.json` placeholder | [COMPLETE] | 2026-04-27 | Created 639-byte structured JSON placeholder with `_placeholder`, `_authoredBy`, `_authoredDuring`, and `_expectedShape` keys. References `capture-axe-evidence.mjs`. No fake axe-core data (zero `violations`/`passes`/`inapplicable`/`incomplete` keys). Parses as valid JSON. |
| 4 | Create `_cutover-evidence/phase2-a11y-notes.md` placeholder | [COMPLETE] | 2026-04-27 | Created markdown placeholder with PLACEHOLDER status header, three section structure (axe-core scan, keyboard walkthrough, VoiceOver spot-check), Sign-off section with bracketed cutover date. Zero pre-checked checkboxes. Zero exclamation points. |
| 5 | Build verification (`pnpm build`) | [COMPLETE] | 2026-04-27 | `pnpm build` clean: app built in 7.80s, SW built in 84ms, PWA precache 335 entries. Pre-existing chunk-size warning (large Bible JSONs + recharts) unchanged from baseline — flag flip introduced no new bundle weight. |
| 6 | Vitest baseline verification (`pnpm test`) | [COMPLETE] | 2026-04-27 | Build commands clean (tsc, lint, build). Vitest reports 8973 pass / 14 fail. The 14 failures are all in `GrowthGarden.test.tsx` and are a pre-existing time-of-day flake unrelated to this spec's flag flip — see `_plans/post-1.10-followups.md` "GrowthGarden test time-of-day flake" entry for full diagnosis. Verified that `GrowthGarden.tsx` and `GrowthGarden.test.tsx` have zero references to `VITE_USE_BACKEND_ACTIVITY`; the flag flip cannot influence aria-label time-of-day suffixes by any mechanism. |
