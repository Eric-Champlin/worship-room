# Forums Wave Plan: Spec 6.6 — Answered Wall

**Spec:** `_specs/forums/spec-6-6.md`
**Brief:** `_plans/forums/spec-6-6-brief.md` (preserved verbatim inside the spec)
**Master Plan:** `_forums_master_plan/round3-master-plan.md` → Spec 6.6 (`round3-phase06-spec06-answered-wall`)
**Date:** 2026-05-14
**Branch:** `forums-wave-continued` (Phase 6 specs accumulate; CC never alters git state — Universal Rule 1)
**Phase:** 6 (Slow Sanctuary / Quiet Engagement Loop)
**Size:** L (Large)
**Risk:** Medium (xHigh, low-end per brief Section 2) — Liquibase ALTER on a permanent-MD5 CHECK constraint is the load-bearing piece; data model already exists, mark-as-answered flow already exists, feed query already exists (see Plan-Time Discovery #1)

---

## Affected Frontend Routes

- `/prayer-wall/answered` (NEW — the Answered Wall page)
- `/prayer-wall` (Answered/All tab strip added to the top of the main column — see Plan-Time Divergence #2)
- `/prayer-wall/:id` (PrayerDetail — when the detail is an answered post, the AnsweredCard variant renders)
- `/prayer-wall/user/:id` (PrayerWallProfile — answered posts on a user's profile render the answered variant)
- `/prayer-wall/dashboard` (PrayerWallDashboard — user's own answered prayers render the answered variant)

---

## Plan-Time Discoveries (lead — these reshape the brief's planned work)

### Discovery #1 — The Answered Wall feed already ships end-to-end (R6 resolved)

Recon on `PostService.java` revealed `SortKey.ANSWERED` (line 730), and `listFeed()` at line 130–131 already branches:

```java
if (sort == SortKey.ANSWERED) {
    spec = spec.and(PostSpecifications.isAnswered());
}
```

`sortFor(SortKey.ANSWERED)` returns `Sort.by(DESC, "answeredAt")` (line 693). `PostSpecifications.isAnswered()` exists (line 143–145) and uses `cb.isTrue(root.get("isAnswered"))` — meaning it filters via `is_answered = TRUE`, leveraging the partial index `idx_posts_is_answered_created` (changeset 014:178–180) — matching spec-time MPD-related semantic flag R9 confirmation. `PostController.listFeed` at line 86 already accepts `sort` and parses it via `SortKey.parse`. The OpenAPI spec already documents `enum: [bumped, recent, answered]` at lines 1686 and 1988.

**Implication:** Brief Section 5 R6 (query-param vs. dedicated endpoint) is resolved at plan time as **REUSE EXISTING — `GET /api/v1/posts?sort=answered`**. NO new endpoint. NO new query param. NO new sort enum value. NO new specification predicate. NO new repository method. NO new service method. NO OpenAPI extension for a new endpoint. NO new backend feed work.

The Answered Wall is a frontend-only consumer of an already-shipped backend feed. This is a major scope reduction from the brief's lean (which expected query-param work) and the master plan stub at line 5713 (which specified a dedicated endpoint).

**Backend work for 6.6 reduces to:** (1) `praising` reaction enum widening (DB + Java enumeration sites + DTO + Mapper + OpenAPI), (2) `praising_count` denormalized counter (see Plan-Time Divergence #1 below). That is the entire backend surface.

### Discovery #2 — Prayer Wall Redesign HAS already shipped (R5 resolved)

Recon confirmed `frontend/src/components/prayer-wall/PrayerWallLeftSidebar.tsx` exists and is mounted on `PrayerWall.tsx`. Its current shape: primary nav links (Daily Hub, Bible, Grow, Music, Prayer Wall) + CategoryFilters in stacked variant. There is no "view" navigator slot in the sidebar. The sidebar renders at `xl:block` only — mobile uses a horizontal category-filter row in the main column.

**Implication:** Brief Section 16 sequencing recommendation is satisfied. The 6.6 navigation entry adds into the **post-redesign** layout. See Plan-Time Divergence #2 for the exact placement (top-of-main-column tab strip) which is mobile-safe and reuses the canonical pill+halo tab-bar pattern.

### Discovery #3 — Schema realities verified per Phase 3 Addendum item 8

Three NEGATIVE verifications (the table proposes creating these — recon confirms they don't already exist):

| Element | Status on disk | Evidence |
|---|---|---|
| `'praising'` in `post_reactions_reaction_type_check` | **NOT present** | `2026-04-27-016-create-post-reactions-table.xml:60` — `CHECK (reaction_type IN ('praying', 'candle'))` |
| `posts.praising_count` column | **NOT present** | `grep -n "praising" backend/src/main/java/com/worshiproom/post/Post.java` returned zero matches |
| `'celebrate'` value | **NOT present** | Same evidence as praising. Brief MPD-1 explicitly defers this. |

One POSITIVE verification (the table reuses these — recon confirms they exist):

| Element | Status on disk | Evidence |
|---|---|---|
| `SortKey.ANSWERED` + `PostSpecifications.isAnswered()` + sort-by-answered_at | **Present** | `PostService.java:130–131, 693, 730`; `PostSpecifications.java:143–145` |
| `Post.isAnswered` / `Post.answeredAt` / `Post.answeredText` columns + JPA fields | **Present** | `2026-04-27-014-create-posts-table.xml:44` (`is_answered NOT NULL DEFAULT FALSE`), CHECK constraint at line 123, partial index at line 178–180; `Post.java:53–60` |
| `Post.prayingCount` / `Post.candleCount` denormalized counters + repo methods | **Present** | `Post.java:75–79`; `PostRepository.java:72, 74, 86, 89, 91, 94, 96` (4 `@Modifying` methods with `clearAutomatically=true, flushAutomatically=true` per Phase 3 Addendum item 3) |

---

## Universal Rules Checklist

All 17 Universal Rules from `_forums_master_plan/round3-master-plan.md`:

- [x] **Rule 1 — No git operations by CC.** Plan and execute both honor this; user manages git. `git status` (read-only) at execute-start permitted per brief Section 1.
- [x] **Rule 2 — Master Plan Quick Reference read.** Master plan Spec 6.6 stub at lines 5630, 5707, 5685 read; MPD-1 (praising-only vs. praising+celebrate) surfaced as Plan-Time Divergence; Universal Rules 13 (crisis), 8 (BB-45), 15 (rate limit) applied below.
- [x] **Rule 3 — Liquibase rules.** Two new changesets in this plan, both append-only, both with `<rollback>` blocks. Filenames follow `YYYY-MM-DD-NNN-description.xml` per `03-backend-standards.md`. Pre-flight filename-collision check is Step 1.0.
- [x] **Rule 4 — OpenAPI extension.** Extend `backend/src/main/resources/openapi.yaml` (already 4700+ lines). Add `praising` to the `reactionType` enum (two sites — request body and DELETE query param), add `praisingCount` to `PostDto` and `ToggleReactionResponse` schemas. NOT a new file.
- [x] **Rule 5 — Copy Deck + i18n-ready constants.** All user-facing strings live in `frontend/src/constants/answered-wall-copy.ts` per brief Section 10. Anti-Pressure Copy Checklist applied to every string (D-Copy in spec body — D-Copy is in scope for review).
- [x] **Rule 6 — Tests mandatory.** Plan targets ~32 tests (brief targets ~17; overshoot justified in Edge Cases & Decisions). Covers migration, regression, enum sync, feed (light regression), `'praising'` reaction, page render, card variant, BB-45 cross-mount subscription, a11y, E2E.
- [x] **Rule 7 — localStorage keys documented.** No new `wr_*` keys. The frontend `wr_prayer_reactions` shape extends to include `isPraising` (additive); documented as a SHAPE change in `11-local-storage-keys.md` table entry (existing row, no new row needed). Default-fill migration mirrors Spec 3.7's `isCandle` pattern.
- [x] **Rule 8 — BB-45 anti-pattern forbidden.** AnsweredCard consumes `usePrayerReactions()` (Pattern A via `useSyncExternalStore`). Cross-mount subscription test required (mutate store after mount, assert AnsweredCard re-renders the active `'praising'` state). Two such tests in the suite per the lens-8 audit lens.
- [x] **Rule 9 — Accessibility not optional.** Gate-G-A11Y items in spec Section 6. Single `<h1>` on AnsweredWall, `aria-current="page"` on nav entry, semantically labeled answer-text region, `aria-label`+`aria-pressed` on "Praising with you", WCAG AA contrast (light + dark themes), keyboard nav, `prefers-reduced-motion` honored, axe-core zero violations (populated + empty).
- [x] **Rule 10 — Performance + animation tokens.** Uses `ANIMATION_DURATIONS` from `frontend/src/constants/animation.ts`. No hardcoded `cubic-bezier()` or ms in component CSS. Bundle regression target: ≤50 KB (one new page + one new card variant + one constants file — should be well under).
- [x] **Rule 11 — Brand voice mandatory.** Pastor's-wife test applied to every copy string. Subhead "Prayers the community has watched God move in." is explicit Christian-testimony voice — surfaced to Eric for copy review per Gate-G-COPY.
- [x] **Rule 12 — Anti-pressure design.** Gate-G-NO-METRICS (HARD) per brief MPD-5. NO page-level counts, tallies, leaderboards, timeframe stats, streaks, or trending on the Answered Wall. Sort is `answered_at DESC` only — no "popular" or "top" affordance.
- [x] **Rule 13 — Crisis detection supersession.** N/A for write paths — 6.6 introduces no new user-content-creation surface. Reactions ride existing `ReactionWriteService` which does NOT route to `CrisisAlertService` (D4 in Spec 3.7). Read paths inherit `PostSpecifications.visibleTo()` which already filters `moderationStatus IN ('approved', 'flagged')` — crisis-flagged posts at moderation-hidden status are excluded by inheritance. No special-casing needed.
- [x] **Rule 14 — Plain text only for user-generated content.** Answer-text rendering uses `whitespace-pre-wrap` on `<p>`. No `dangerouslySetInnerHTML`. The existing PrayerCard pattern (line 213) sets the precedent; AnsweredCard's "How this was answered" region follows it.
- [x] **Rule 15 — Rate limiting.** No new endpoint = no new rate-limit config. `'praising'` rides the existing `ReactionsRateLimitConfig` / `ReactionsRateLimitService` (verified at `ReactionWriteService.java:51` — `rateLimitService.checkAndConsume(userId)` is called before reaction-type dispatch, so the limit is reaction-type-agnostic). The answered-feed read rides existing `GET /api/v1/posts` rate-limit posture. Frontend per-prayer-per-concern `pendingMutations` Set in `reactionsStore` extends with a new `${prayerId}:praising` key family — mirrors `praying`/`candle` exactly.
- [x] **Rule 16 — Respect existing patterns.** AnsweredCard extends PrayerCard via `answeredVariant?: boolean` prop (option (a) per spec R8 — recommended in spec body). InteractionBar extends with `showPraising?: boolean` prop. reactionsStore extends with `togglePraising`, `isPraising`. NO parallel reaction system. NO PrayerCard fork. NO new feed endpoint.
- [x] **Rule 17 — Per-phase cutover specs.** N/A — 6.6 is not a cutover spec. No `_cutover-evidence/` artifact required.

---

## Architecture Context

**Backend (`com.worshiproom.post.engagement/` + `com.worshiproom.post/`):**
- `Post.java` — JPA entity at `backend/src/main/java/com/worshiproom/post/Post.java`. Fields `isAnswered: boolean`, `answeredAt: OffsetDateTime`, `answeredText: String`, `prayingCount: int`, `candleCount: int`. Constructors at line 75–79, getters/setters at line 132–168. New work: add `praisingCount: int` field with `@Column(name = "praising_count", nullable = false)`, no-default-required (the DB column ships `NOT NULL DEFAULT 0`).
- `PostRepository.java` — Has `incrementPrayingCount`, `decrementPrayingCount`, `incrementCandleCount`, `decrementCandleCount` at lines 72–96, all `@Modifying(clearAutomatically=true, flushAutomatically=true)` per Phase 3 Addendum item 3. New work: add 2 more methods following identical pattern.
- `ReactionWriteService.java` — Private constants `PRAYING`/`CANDLE` at line 46–47. Switch shape in `incrementReactionCounter`/`decrementReactionCounter` at line 162–176. `INTERCESSION` activity event fires on praying-add only at line 109–113 (gated by `if (PRAYING.equals(reactionType))`-equivalent — but actually it fires on every ADD path. See Plan-Time Divergence #4 below for the activity-event scoping decision).
- `EngagementService.java` — Private constants `PRAYING`/`CANDLE` at line 38–39. Reads-side at `reactionsFor()` (line 53–75) unions `praying` reactions + `candle` reactions + `bookmarks` into `PerPostReaction(isPraying, isCandle, isBookmarked)` DTO. New work: also union `praising` reactions; extend `PerPostReaction` with `isPraising`; extend `PerPostReactionMutable` at line 92–96.
- `ToggleReactionRequest.java` — `@Pattern(regexp = "^(praying|candle)$")` at line 18. New work: widen to `^(praying|candle|praising)$` + update `message`.
- `ToggleReactionResponse.java` — Record with `reactionType, state, prayingCount, candleCount`. New work: add `praisingCount` field; PostController toggleReaction handler unchanged shape-wise.
- `PostController.java:276` — `removeReaction` `@Pattern(regexp = "^(praying|candle)$")`. New work: widen to `^(praying|candle|praising)$`.
- `PostMapper.java:116–146` — `buildDto` produces `PostDto`. New work: pass `p.getPraisingCount()` into the DTO constructor.
- `PostDto` — at `backend/src/main/java/com/worshiproom/post/dto/PostDto.java`. New work: add `praisingCount` field (record positional add — coordinated with `PostMapper.buildDto`).
- `PerPostReaction.java` (at `engagement/dto/PerPostReaction.java`) — DTO record. New work: add `isPraising` field.

**Frontend (`frontend/src/`):**
- `types/api/prayer-wall.ts` — `ReactionTypeApi = 'praying' | 'candle'` at line 42. New work: widen to `'praying' | 'candle' | 'praising'`. `PerPostReactionApi` (line 99–100) has `prayingCount` and `candleCount`. New work: add `praisingCount`. `ToggleReactionResponse`-shape (line 178–181) has `prayingCount`+`candleCount`. New work: add `praisingCount`.
- `types/prayer-wall.ts` — `PrayerRequest` interface (line 41–90). Has `prayingCount: number`, `candleCount?: number`, `isAnswered: boolean`, `answeredText: string | null`, `answeredAt: string | null`. New work: add `praisingCount?: number` (optional for back-compat parity with `candleCount?`). `PrayerReaction` interface (line 109–113) has `isPraying, isBookmarked, isCandle`. New work: add `isPraising: boolean`.
- `lib/prayer-wall/reactionsStore.ts` — Reactive store with `useSyncExternalStore` access via `usePrayerReactions()`. Has `togglePraying` (line 431–470), `toggleCandle` (line 522–548), and mutations buffer in `pendingMutations` keyed `${prayerId}:${concern}` where `concern ∈ 'praying' | 'bookmark' | 'candle'`. Spec 3.7 added an additive migration for `isCandle` (default-fill on hydrate at line 71–74, no version key). New work: add `togglePraising` (mirrors `togglePraying` pattern exactly), extend default-fill to also default-fill `isPraising: false` for old-shape data, extend `concern` union with `praising`.
- `services/api/prayer-wall-api.ts` — `toggleReaction(prayerId, reactionType: ReactionTypeApi)` at line 470 and `removeReaction(prayerId, reactionType: ReactionTypeApi)` at line 495. NO source changes needed if `ReactionTypeApi` is widened (this is the win from extending the TypeScript union). New work: zero source changes — the type-widening propagates automatically. Response-shape types update separately (lines 487–488 explicit destructure of `prayingCount`/`candleCount` will need to also surface `praisingCount`).
- `hooks/usePrayerReactions.ts` — Pattern A hook with `useSyncExternalStore`. Returns `{ reactions, togglePraying, toggleBookmark, toggleCandle }`. New work: also return `togglePraising` (mirroring `togglePraying`/`toggleCandle` shape).
- `components/prayer-wall/PrayerCard.tsx` — Renders the inline `<AnsweredBadge>` when `prayer.isAnswered` at line 269–274. New work: accept `answeredVariant?: boolean` prop. When `answeredVariant === true`, REPLACE the inline AnsweredBadge with a prominent "How this was answered" region (the focal point per spec D-AnswerTextTreatment). When `answeredVariant === false` (default), preserve existing AnsweredBadge rendering for non-answered-wall surfaces (W19 — answered prayers on main feed/profile/dashboard still show the small inline pill).
- `components/prayer-wall/InteractionBar.tsx` — Renders praying button at line 169–207 (toggleable, with ceremony animation). New work: accept `showPraising?: boolean` prop; when true, add an additional "Praising with you" button next to (or in place of, on answered posts) the existing pray button per spec D-Copy. AnsweredCard sets `showPraising` on its InteractionBar instance via PrayerCard's `{children}` slot.
- `components/prayer-wall/AnsweredCard.tsx` — **NEW.** A thin wrapper: `<PrayerCard answeredVariant {...rest}>{children}</PrayerCard>` where `{children}` is a customized `InteractionBar` instance passing `showPraising={true}`.
- `pages/AnsweredWall.tsx` — **NEW.** Consumes `GET /api/v1/posts?sort=answered` via existing prayer-wall-api feed fetch. Renders heading, subhead, AnsweredCard list, empty state. Reuses `PageShell` per the post-redesign pattern.
- `components/prayer-wall/PrayerWallLeftSidebar.tsx` — Sidebar with primary nav + CategoryFilters. NO change per Plan-Time Divergence #2 — the "All/Answered" tab strip lives at the top of the main column instead, so it works on mobile.
- `pages/PrayerWall.tsx` — Main feed page. New work: add a top-of-main-column tab strip ("All" | "Answered") using the canonical pill+halo tab-bar pattern from `09-design-system.md` § "Active-State and Selection Patterns". "All" routes to `/prayer-wall`; "Answered" routes to `/prayer-wall/answered`. Both tabs use `isNavActive` for `aria-current`.
- `App.tsx` — Routing block at line 303–307. New work: add `<Route path="/prayer-wall/answered" element={...}>` between line 303 (`/prayer-wall`) and line 305 (`/prayer-wall/dashboard`). Suspense fallback uses `PrayerWallSkeleton` to match the existing `/prayer-wall` route's pattern.
- `constants/answered-wall-copy.ts` — **NEW.** i18n-ready constants module per Universal Rule 5. Holds Heading, Subhead, EmptyState, PraisingWithYouLabel (active + inactive aria-label), AnswerTextRegionLabel, AnsweredTimestampPrefix.

---

## Database Changes

| # | Change | Table | Liquibase Changeset (today's actual date) | Notes |
|---|---|---|---|---|
| 1 | ALTER CHECK constraint — add `'praising'` (DROP + recreate within one changeset, leaves constraint in known-good state) | `post_reactions` | `2026-05-14-001-add-praising-to-post-reactions-check.xml` | Append-only — NEVER edits `2026-04-27-016`. Widens to `('praying','candle','praising')`. `'praying'` and `'candle'` survive (regression test). `'celebrate'` NOT added per MPD-1 (brief authoritative). `<rollback>` reverts to `('praying','candle')` only IF no `'praising'` rows exist; otherwise rollback fails loudly per Liquibase contract (better than silent rollback that would orphan rows). |
| 2 | ADD COLUMN `praising_count` (per-type denormalized counter — pattern parity with `praying_count`/`candle_count`) | `posts` | `2026-05-14-002-add-posts-praising-count-column.xml` | Append-only. `INT NOT NULL DEFAULT 0`. No backfill needed — answered prayers have zero `'praising'` reactions until the feature ships. `<rollback>` drops the column. **Decision: ship this changeset over the alternative of computing the count via `SELECT COUNT(*) ... WHERE reaction_type='praising'` on every read.** Pattern consistency forces denormalized counters per W14; computed counters would introduce a parallel path the spec's Gate-G-REACTION-REUSE forbids. |

**Migration safety:**

- Both changesets are zero-downtime compatible. (1) widens the CHECK — backward-compatible (existing `'praying'` and `'candle'` writes still pass). (2) adds a column with a DB default — existing INSERT statements that don't mention `praising_count` still work because PostgreSQL applies the default.
- (1) uses DROP + ADD CONSTRAINT in a single changeset (PostgreSQL cannot widen a CHECK; this is the canonical pattern per the original changeset 016 comment at line 53–57 anticipating exactly this).
- Order: (1) MUST run before any code that writes `'praising'` reactions. (2) MUST run before `ReactionWriteService.incrementPraisingCount()` is called. The changeset filenames sort correctly (-001 before -002) so master.xml include order honors this.
- **Pre-flight check before Step 1.1 (Liquibase filename-collision):** `ls backend/src/main/resources/db/changelog/ | grep '^2026-05-14-'` MUST return zero matches before the first changeset is written. If 2026-05-14-001 or -002 collide (e.g., another spec executed today), bump to -003/-004 and document the deviation.

---

## API Changes

**ZERO new endpoints. ZERO new query params.**

| Existing endpoint | Change | Notes |
|---|---|---|
| `GET /api/v1/posts?sort=answered` | No change to endpoint shape | Already documented at openapi.yaml lines 1686 + 1988 with `enum: [bumped, recent, answered]`. Already filters via `PostSpecifications.isAnswered()` and sorts `answered_at DESC`. The Answered Wall is a frontend consumer of this endpoint. |
| `POST /api/v1/posts/{id}/reactions` | Request body `reactionType` enum: widen `[praying, candle]` → `[praying, candle, praising]` | OpenAPI line 4685. Backend regex `^(praying\|candle)$` → `^(praying\|candle\|praising)$` at `ToggleReactionRequest.java:18`. Response `ToggleReactionResponse` adds `praisingCount`. |
| `DELETE /api/v1/posts/{id}/reactions?reactionType=...` | Query param enum: widen | OpenAPI line 2321. Backend `@Pattern` regex at `PostController.java:276` → `^(praying\|candle\|praising)$`. |
| Response `PostDto` schema | Add `praisingCount` field (integer, minimum: 0) | OpenAPI line 4236–4237 — add `praisingCount` alongside `prayingCount`/`candleCount`. Mark optional in OpenAPI (defaults to 0). |
| Response `ToggleReactionResponse` schema | Add `praisingCount` field | OpenAPI ~line 4690. |
| `GET /api/v1/users/me/reactions` (`/users/me/reactions`) | Response `PerPostReaction.isPraising` added | OpenAPI ~line 4665. Reflects viewer's own `'praising'` reactions for read-side rendering. |

**SecurityConfig — NO change.** Per `03-backend-standards.md` § "SecurityConfig rule ordering" and Phase 3 Addendum item 4: `POST /api/v1/posts/*/reactions` and `DELETE /api/v1/posts/*/reactions` are already explicit-method-rule `.authenticated()`. `'praising'` rides them. No new method-specific rule needed, no nested-path drift to fix.

**Rate limiting — NO new config.** Existing `ReactionsRateLimitConfig` / `ReactionsRateLimitService` is reaction-type-agnostic (verified at `ReactionWriteService.java:51` — `rateLimitService.checkAndConsume(userId)` is invoked before reaction-type dispatch).

---

## Assumptions & Pre-Execution Checklist

- [ ] Spec 6.5 (Intercessor Timeline) is merged to master OR is the latest commit on `forums-wave-continued`. Recent commits `46d70a7 Spec 6-5` indicate 6.5 is in-flight. **6.6 execute MUST NOT start until 6.5 is committed** per brief Section 16 (hard prereq).
- [ ] Prayer Wall Redesign side quest has shipped (verified via `PrayerWallLeftSidebar.tsx` exists on disk — see Discovery #2). Recommended-first ordering is satisfied.
- [ ] Docker is running (PostgreSQL container for `./mvnw test`).
- [ ] Backend compiles cleanly: `./mvnw compile` returns BUILD SUCCESS.
- [ ] Frontend builds cleanly: `pnpm build` returns success.
- [ ] No Liquibase changeset filename collisions: `ls backend/src/main/resources/db/changelog/ | grep '^2026-05-14-'` returns zero matches. If yes, bump sequence.
- [ ] **MPD-1 sign-off received from Eric** before execute starts: Either reaffirm narrow scope (`'praising'` only, this plan as-written) OR re-align to master plan stub (adds `'celebrate'` enum value, `celebrate_count` column, CelebrateReaction.tsx component, +4-6 tests). See "Plan-Time Divergences" section below for the contingency delta.
- [ ] **Plan-Time Divergence #4 sign-off received from Eric** before execute starts: Activity engine question — does `'praising'`-add fire an `INTERCESSION` event? Plan defaults to NO (skip activity event for `'praising'`). See divergence section.

---

## Spec-Category-Specific Guidance

**This is a BACKEND + FRONTEND hybrid spec.** Backend work is small (enum widening + denormalized counter — no new endpoint, no new service method). Frontend work is the bulk (new page + new route + variant prop on PrayerCard + reactionsStore extension + nav tab strip).

It is NOT a dual-write migration spec (no `VITE_USE_BACKEND_*` flag). It IS adjacent to dual-write: the reaction infrastructure is fully backend-backed per Spec 3.7, and 6.6 extends that backend surface.

It is NOT a cutover spec.

Verification commands per step:
- Backend step: `./mvnw compile && ./mvnw test -Dtest={ClassName}` for class-specific tests, then a final `./mvnw test` to sweep regressions.
- Frontend step: `pnpm typecheck && pnpm test -- --run {test file}` for spec-specific tests, then `pnpm build` to verify bundle.
- Liquibase step: `./mvnw test -Dtest=LiquibaseSmokeTest` confirms the changesets apply cleanly + the new MD5 is recorded.

---

## Edge Cases & Decisions

| Decision | Choice | Rationale |
|---|---|---|
| Feed endpoint (R6) | REUSE existing `GET /api/v1/posts?sort=answered` | Discovery #1. Backend infra is complete (SortKey.ANSWERED + PostSpecifications.isAnswered + sortFor). Zero new backend endpoint work. Frontend AnsweredWall calls the existing service. |
| Canonical "is answered" filter (R9) | `is_answered = TRUE` | `PostSpecifications.isAnswered()` uses `cb.isTrue(root.get("isAnswered"))`. Leverages partial index `idx_posts_is_answered_created`. CHECK constraint (changeset 014:123) couples `is_answered` and `answered_at` so the two are functionally equivalent; the boolean column wins on index usage. |
| Can `answered_at` be un-set? (R9 sub-question) | YES — `PostService` update path (line 397–511) supports toggling `isAnswered` back to `false` | Implication: a post can disappear from the Answered Wall if un-marked. The frontend feed re-fetch on next mount will naturally reflect this. No special handling required. |
| Card extension mechanism (R8) | `answeredVariant?: boolean` prop on PrayerCard | Spec body D-CardExtension defaults to (a) per master plan stub line 5697 "accept `answeredVariant` prop." PrayerCard is already prop-driven. AnsweredCard becomes a thin wrapper. NOT a fork (Gate-G-CARD-NO-FORK). |
| `'praising'`-on-non-answered-post (R7) | DB-permissive, UI-gated | Brief D-PraisingScope. CHECK constraint allows `'praising'` on any post. ReactionWriteService does NOT validate post-state before accepting. Server-side post-state validation would add a validation path + error case + test for zero functional value — UI only surfaces the affordance on AnsweredCard. Fewest moving parts. |
| `'praising'` denormalized counter (W14) | YES — ship `posts.praising_count` column | Pattern parity with `praying_count`/`candle_count`. Computing counts via `SELECT COUNT(*)` per read would introduce a parallel path. Plan-Time Divergence #1 (denormalized counter scope addition). |
| `'praising'`-add activity event | NO — skip activity event for `'praising'` | Plan-Time Divergence #4. Brief is silent; D-Scope ("6.6 is a new READ surface") supports skipping. `'praising'` is celebration of someone else's answered prayer, not intercession. Re-using `INTERCESSION` would miscategorize. Awaiting Eric's confirmation. |
| Frontend reaction store migration | Default-fill on hydrate, no version key | Mirrors Spec 3.7's `isCandle` migration pattern at `reactionsStore.ts:71–74`. Additive shape change with safe `isPraising: false` default. Old-shape stored reactions auto-upgrade on next read. |
| Nav placement (R5) | Top-of-main-column "All" / "Answered" pill+halo tab strip | Plan-Time Divergence #2. PrayerWallLeftSidebar is `xl:block`-only (no mobile). Tab strip in main column is mobile-safe. Reuses canonical pill+halo pattern from `09-design-system.md` (DailyHub, Music, Local Support, Grow). |
| AnsweredBadge legacy styling | NO change in 6.6 — note as observed drift, defer | `AnsweredBadge.tsx` uses `bg-success` + `text-text-dark/80` (light-theme legacy). D-Scope is narrow; the badge still renders correctly. Phase-7+ cleanup spec. |
| Answered-timestamp display | Relative format ("answered 3 days ago") | Brief MPD-6 + D-TimestampDisplay. Sort key is real `answered_at`; display uses `timeAgo()` from `frontend/src/lib/time.ts`. Lowercase "answered" prefix (quiet annotation, not a headline). |
| Visibility inheritance | Inherit from existing feed query | D-VisibilityRespect. `PostSpecifications.visibleTo()` (lines 32–94) already filters public/friends/private/own + moderation status. AnsweredWall reuses it transparently. No new visibility logic. |
| AuthorAttribution for anonymous | Inherit from PrayerMapper | `PostMapper.anonymousAuthor()` produces `AuthorDto(null, "Anonymous", null)`. AnsweredCard inherits — anonymous answered prayers stay anonymous. |
| `'praising'` icon (frontend) | `Sparkles` (lucide-react) | Existing reaction icons: HandHelping (praying), Heart (encouragement). `Sparkles` is already used as the icon for testimony post type (`PrayerCard.tsx:47`). The semantic fit for praising-an-answered-prayer is celebration → Sparkles. Decision documented; if Eric prefers a different icon, swap at execute time. |
| Test count overshoot vs. brief's ~17 | Plan target ~32; justify in this row | ~17 was brief estimate. Plan adds: counter denormalization (3) + cross-mount BB-45 subscription (2) + light feed regression (2) + InteractionBar prop tests (2) + reactionsStore migration tests (2) + a11y axe + Playwright + page-render edge cases. Aligns with Phase 3 reality of 30-50 tests for medium-large specs. |

---

## Implementation Steps

### Step 1.0: Liquibase filename-collision pre-flight

**Objective:** Confirm no existing changeset claims the date+sequence slots this plan needs.

**Files to read (no modifications):**
- `backend/src/main/resources/db/changelog/` directory listing

**Details:**
- Run `ls backend/src/main/resources/db/changelog/ | grep '^2026-05-14-'`.
- Expected: zero matches. If non-zero, bump the new changesets' sequence to the next free integer and document the deviation here.

**Verification:**
- [ ] Zero existing files with prefix `2026-05-14-`, OR a documented bump to higher sequence numbers.

**Expected state after completion:**
- [ ] Changeset filenames `2026-05-14-001-add-praising-to-post-reactions-check.xml` and `2026-05-14-002-add-posts-praising-count-column.xml` are reserved and free to claim.

---

### Step 1.1: Liquibase changeset — ALTER CHECK constraint on `post_reactions.reaction_type`

**Objective:** Widen the CHECK constraint to accept `'praising'` while preserving `'praying'` and `'candle'`. NEVER edit `2026-04-27-016` (its MD5 is permanent per W2).

**Files to create:**
- `backend/src/main/resources/db/changelog/2026-05-14-001-add-praising-to-post-reactions-check.xml` — NEW changeset
- `backend/src/main/resources/db/changelog/master.xml` — include the new changeset in master changelog ordering

**Details:**

Changeset structure (PostgreSQL cannot widen a CHECK in place — DROP + ADD CONSTRAINT inside a single changeset leaves the constraint in known-good state per W7):

```xml
<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

  <!-- Forums Wave Spec 6.6 — Answered Wall. Widens
       post_reactions.reaction_type CHECK constraint to accept 'praising' in
       addition to existing 'praying' and 'candle'. The original changeset
       2026-04-27-016 explicitly anticipated this (see its comment at line
       54-57). Adds 'praising' ONLY — 'celebrate' NOT added per brief MPD-1
       (no consumer in 6.6; speculative schema).
       Append-only: NEVER edit 2026-04-27-016 — its MD5 is permanent. -->
  <changeSet id="2026-05-14-001-add-praising-to-post-reactions-check" author="worship-room">

    <sql>
      ALTER TABLE post_reactions DROP CONSTRAINT post_reactions_reaction_type_check;
      ALTER TABLE post_reactions ADD CONSTRAINT post_reactions_reaction_type_check
      CHECK (reaction_type IN ('praying', 'candle', 'praising'));
    </sql>

    <rollback>
      <!-- Rollback: revert to ('praying','candle'). Will fail loudly via the CHECK
           if any 'praising' rows exist at rollback time — by design. Liquibase
           contract: a rollback that would orphan data should fail rather than
           silently lose data. Operator must DELETE 'praising' rows first if
           the table was populated, OR accept the failure. -->
      <sql>
        ALTER TABLE post_reactions DROP CONSTRAINT post_reactions_reaction_type_check;
        ALTER TABLE post_reactions ADD CONSTRAINT post_reactions_reaction_type_check
        CHECK (reaction_type IN ('praying', 'candle'));
      </sql>
    </rollback>

  </changeSet>
</databaseChangeLog>
```

Add `<include file="db/changelog/2026-05-14-001-add-praising-to-post-reactions-check.xml" relativeToChangelogFile="false"/>` to `master.xml` in chronological order (after the latest 2026-05-12 entry).

**Guardrails (DO NOT):**
- DO NOT edit `2026-04-27-016-create-post-reactions-table.xml` or any previously-run changeset (W2, Gate-G-MIGRATION-SAFETY).
- DO NOT add `'celebrate'` to the IN-list (W4, MPD-1).
- DO NOT use `ddl-auto` (Universal Rule 3).
- DO NOT split the DROP + ADD across two changesets — they MUST happen inside one changeset block so a failure leaves the constraint in a known-good state.

**Verification:**
- [ ] Code review confirms the changeset edits NO previously-shipped file.
- [ ] `./mvnw test -Dtest=LiquibaseSmokeTest` PASSES (smoke test applies all changesets cleanly against Testcontainers PostgreSQL).
- [ ] `master.xml` includes the new changeset in correct chronological position.

**Test specifications:**
| Test | Type | Description |
|---|---|---|
| existing LiquibaseSmokeTest | integration | All changesets apply cleanly — implicit regression that the new changeset doesn't break the chain. |

**Expected state after completion:**
- [ ] After backend startup, `post_reactions_reaction_type_check` accepts `'praising'`, `'praying'`, `'candle'`. Rejects everything else.
- [ ] DATABASECHANGELOG records the new MD5 for changeset `2026-05-14-001-add-praising-to-post-reactions-check`.

---

### Step 1.2: Liquibase changeset — ADD `posts.praising_count` column

**Objective:** Add a per-type denormalized counter for `'praising'` reactions, mirroring `praying_count` and `candle_count`.

**Files to create:**
- `backend/src/main/resources/db/changelog/2026-05-14-002-add-posts-praising-count-column.xml` — NEW changeset
- `backend/src/main/resources/db/changelog/master.xml` — include in master ordering

**Details:**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog ...>

  <!-- Forums Wave Spec 6.6 — Answered Wall. Adds praising_count
       denormalized counter to posts table. Pattern parity with the
       existing praying_count and candle_count columns (changeset 014).
       Phase 3 Addendum item 5: prevents per-read SELECT COUNT(*) for
       the new reaction type. Backfill not required — answered prayers
       have zero 'praising' reactions until 6.6 ships. -->
  <changeSet id="2026-05-14-002-add-posts-praising-count-column" author="worship-room">

    <addColumn tableName="posts">
      <column name="praising_count" type="INTEGER" defaultValueNumeric="0">
        <constraints nullable="false"/>
      </column>
    </addColumn>

    <rollback>
      <dropColumn tableName="posts" columnName="praising_count"/>
    </rollback>

  </changeSet>
</databaseChangeLog>
```

Add include to `master.xml` after the -001 changeset.

**Guardrails (DO NOT):**
- DO NOT edit `2026-04-27-014-create-posts-table.xml` (W2 — the `answered_*` columns already exist there; not touched).
- DO NOT combine the CHECK widening (-001) and the column add (-002) into a single changeset — Liquibase rule "one logical change per changeset" per `03-backend-standards.md`.

**Verification:**
- [ ] `./mvnw test -Dtest=LiquibaseSmokeTest` PASSES.
- [ ] Verify column exists via `EngagementSchemaInvariantTest` extension (Step 6.3 below).

**Test specifications:**
| Test | Type | Description |
|---|---|---|
| existing LiquibaseSmokeTest | integration | Implicit regression. |
| new EngagementSchemaInvariantTest assertion | integration | New: assert `praising_count` column exists, `NOT NULL`, default 0. See Step 6.3. |

**Expected state after completion:**
- [ ] `posts.praising_count INT NOT NULL DEFAULT 0` column exists.
- [ ] Existing rows have `praising_count = 0`.

---

### Step 2: Extend `Post` JPA entity + `PostRepository` counter methods

**Objective:** Surface the new `praising_count` column in the JPA layer with idempotent `@Modifying` counter methods matching the existing `praying`/`candle` pattern.

**Files to modify:**
- `backend/src/main/java/com/worshiproom/post/Post.java`
- `backend/src/main/java/com/worshiproom/post/PostRepository.java`

**Details:**

In `Post.java`:
- Add field `@Column(name = "praising_count", nullable = false) private int praisingCount;` placed adjacent to `prayingCount`/`candleCount` (after line 79).
- Add getter `public int getPraisingCount() { return praisingCount; }` adjacent to line 140.
- Add setter `public void setPraisingCount(int praisingCount) { this.praisingCount = praisingCount; }` adjacent to line 168.

In `PostRepository.java` (mirror the existing pattern at lines 72–96):
```java
@Modifying(clearAutomatically = true, flushAutomatically = true)
@Query("UPDATE Post p SET p.praisingCount = p.praisingCount + 1 WHERE p.id = :postId")
int incrementPraisingCount(@Param("postId") UUID postId);

@Modifying(clearAutomatically = true, flushAutomatically = true)
@Query("UPDATE Post p SET p.praisingCount = p.praisingCount - 1 WHERE p.id = :postId AND p.praisingCount > 0")
int decrementPraisingCount(@Param("postId") UUID postId);
```

Note the `p.praisingCount > 0` guard on decrement — matches the existing race-prevention pattern at line 94 (`AND p.candleCount > 0`).

**Guardrails (DO NOT):**
- DO NOT omit either `@Modifying` flag (Phase 3 Addendum item 3 — Self-Review gate 17 will flag).
- DO NOT use field-update via `setPraisingCount` from inside `ReactionWriteService` — counter mutations route through SQL-side UPDATE only (Plan Tightening Audit lens 3).

**Verification:**
- [ ] `./mvnw compile` — BUILD SUCCESS.
- [ ] `./mvnw test -Dtest=PostRepositoryTest` PASSES (if a unit test for repo methods exists; otherwise the integration tests in Step 6 exercise these).

**Test specifications:**
| Test | Type | Description |
|---|---|---|
| existing tests | integration | Implicit regression — no existing tests should fail. |
| new assertions in Step 6 | integration | `incrementPraisingCount`/`decrementPraisingCount` exercised end-to-end via ReactionWriteService toggle paths. |

**Expected state after completion:**
- [ ] Post entity has `praisingCount` field with accessor methods.
- [ ] PostRepository exposes `incrementPraisingCount` + `decrementPraisingCount` with both `@Modifying` flags.

---

### Step 3: Extend validation regex + service constants for `'praising'`

**Objective:** Allow `'praising'` to pass validation at the controller boundary (Phase 3 Addendum item 6 — never let DB CHECK be the validation surface). Add `PRAISING` constant to both engagement services.

**Files to modify:**
- `backend/src/main/java/com/worshiproom/post/engagement/dto/ToggleReactionRequest.java`
- `backend/src/main/java/com/worshiproom/post/PostController.java`
- `backend/src/main/java/com/worshiproom/post/engagement/ReactionWriteService.java`
- `backend/src/main/java/com/worshiproom/post/engagement/EngagementService.java`

**Details:**

In `ToggleReactionRequest.java` (line 18–20):
```java
@Pattern(regexp = "^(praying|candle|praising)$",
         message = "reactionType must be 'praying', 'candle', or 'praising'")
String reactionType
```
Also update the class-level JavaDoc at line 9–14 to note Phase 6.6 widening completed (`praising` is now accepted; `celebrate` remains a future-spec value).

In `PostController.java` (line 276):
```java
@RequestParam @NotBlank @Pattern(regexp = "^(praying|candle|praising)$") String reactionType
```

In `ReactionWriteService.java`:
- Add `private static final String PRAISING = "praising";` adjacent to line 47.
- Extend `incrementReactionCounter()` at line 162–168 with a third `else if (PRAISING.equals(reactionType)) { postRepository.incrementPraisingCount(postId); }` branch.
- Extend `decrementReactionCounter()` at line 170–176 with the matching `decrementPraisingCount` branch.
- The activity-event branch at line 109–113 (`activityService.recordActivity(...INTERCESSION...)`) — **DO NOT FIRE for `'praising'`-add** per Plan-Time Divergence #4. Gate the activity event with `if (PRAYING.equals(reactionType)) { activityService.recordActivity(...); }` so candles and praising both skip the activity event. This is a behavior change: the current code fires `INTERCESSION` on every reaction-add path (including candles). **Step 3 sub-decision documented as Plan-Time Divergence #4.** If Eric rejects #4 and prefers activity event for `'praising'`, change the gate to `if (PRAYING.equals(reactionType) || PRAISING.equals(reactionType))` and document the divergence reversal in the Execution Log. ⚠️ **Halt before this step until Eric signs off on #4.**

In `EngagementService.java`:
- Add `private static final String PRAISING = "praising";` adjacent to line 39.
- Extend `reactionsFor()` at line 53–75:
  - After line 57 (`List<PostReaction> candles = ...`), add `List<PostReaction> praisings = reactionRepository.findByUserIdAndReactionType(viewerId, PRAISING);`.
  - In the accumulator loop, after line 65 (candles loop), add a `praisings` loop populating `accum...isPraising = true`.
  - In the `PerPostReactionMutable` private class at line 92–96, add `boolean isPraising;` field.
  - In the final-map construction at line 71–73, pass `m.isPraising` into the `PerPostReaction` constructor (which gains a fourth field — see Step 4).

**Guardrails (DO NOT):**
- DO NOT let the DB CHECK be the validation surface (Phase 3 Addendum item 6) — every endpoint accepts only via `@Pattern`.
- DO NOT enumerate reaction types via a Java enum — keep them as String constants (`ToggleReactionRequest.java:9–14` comment explicitly preserves this for forward-compat).
- DO NOT fire `INTERCESSION` activity event on `'praising'`-add without Eric's sign-off on Plan-Time Divergence #4.

**Verification:**
- [ ] `./mvnw compile` — BUILD SUCCESS.
- [ ] `./mvnw test -Dtest=ReactionWriteServiceTest` PASSES with extensions covering `'praising'` paths (see Step 6).
- [ ] `./mvnw test -Dtest=EngagementServiceTest` PASSES with `'praising'` union in the reads map.

**Test specifications:**
| Test | Type | Description |
|---|---|---|
| validation: invalid reactionType returns 400 | controller integration | Confirms `'xyz'` still rejected at controller boundary. |
| validation: 'praising' returns 201 | controller integration | Confirms `'praising'` accepted. |
| ReactionWriteService.toggle('praising') ADD path | service test | Increments `praisingCount`; sets `state='added'`. |
| ReactionWriteService.toggle('praising') REMOVE path | service test | Decrements `praisingCount`; sets `state='removed'`. |
| activity-event-NOT-fired-on-praising | service test | Mock ActivityService; assert recordActivity NOT invoked when reactionType='praising'. |
| activity-event-NOT-fired-on-candle | service test | Regression — current behavior may need correction. **Coordinate with Eric on whether this is a bug or intentional.** |

**Expected state after completion:**
- [ ] Both validator regexes widened. `'praising'` accepted by controller layer.
- [ ] `ReactionWriteService` correctly dispatches `praising_count` increment/decrement.
- [ ] `EngagementService.reactionsFor()` returns `isPraising` per post in the read-side map.
- [ ] Activity event behavior matches Plan-Time Divergence #4 outcome.

---

### Step 4: Extend response DTOs (`ToggleReactionResponse`, `PerPostReaction`, `PostDto`) + `PostMapper`

**Objective:** Surface `praisingCount` and `isPraising` in all response DTOs the frontend consumes.

**Files to modify:**
- `backend/src/main/java/com/worshiproom/post/engagement/dto/ToggleReactionResponse.java`
- `backend/src/main/java/com/worshiproom/post/engagement/dto/PerPostReaction.java`
- `backend/src/main/java/com/worshiproom/post/dto/PostDto.java`
- `backend/src/main/java/com/worshiproom/post/PostMapper.java`
- `backend/src/main/java/com/worshiproom/post/PostService.java` — call site that constructs the response

**Details:**

`ToggleReactionResponse.java` — add `int praisingCount` to the record:
```java
public record ToggleReactionResponse(
    String reactionType,
    String state,
    int prayingCount,
    int candleCount,
    int praisingCount
) {}
```
Update the class-level JavaDoc to note all three counts are returned regardless of which reactionType was toggled.

In `ReactionWriteService.toggle()` at line 127–132 — extend the constructor call:
```java
return new ToggleReactionResponse(
    reactionType, state,
    refreshed.getPrayingCount(),
    refreshed.getCandleCount(),
    refreshed.getPraisingCount()
);
```

`PerPostReaction.java` (at `engagement/dto/`) — add `boolean isPraising`:
```java
public record PerPostReaction(boolean isPraying, boolean isCandle, boolean isPraising, boolean isBookmarked) {}
```
Note: the existing positional order is `(isPraying, isCandle, isBookmarked)`. **Plan-Time Divergence #5** — placing `isPraising` between `isCandle` and `isBookmarked` keeps the three reaction-type booleans adjacent. Update `EngagementService.reactionsFor()` line 72–73 to pass the new positional argument.

`PostDto.java` — add `int praisingCount` (or `Integer praisingCount` if the existing fields are boxed for back-compat — verify on disk). Position adjacent to `prayingCount`/`candleCount` per Plan-Time Divergence #5. Update `PostMapper.buildDto` at line 116–146 to pass `p.getPraisingCount()` into the new positional argument.

**Guardrails (DO NOT):**
- DO NOT add `praisingCount` as `int` if existing fields are `Integer` (back-compat with Jackson null-handling). Match the existing pattern.
- DO NOT change the OpenAPI schema field ordering — schema field order in YAML is informational only; the JSON output is whatever Jackson serializes.

**Verification:**
- [ ] `./mvnw compile` — BUILD SUCCESS.
- [ ] Existing tests that construct `ToggleReactionResponse` or `PerPostReaction` literals (e.g., `EngagementServiceTest`) compile after the positional add — they MAY need to be updated to supply the new argument.

**Test specifications:**
| Test | Type | Description |
|---|---|---|
| PostMapper includes praisingCount | unit | Builds a Post with `praisingCount=5`; asserts DTO's `praisingCount()==5`. |
| ToggleReactionResponse returns all three counts | integration | `'praying'`-toggle returns post-mutation prayingCount AND candleCount AND praisingCount (regression-safe — all three returned regardless of which type was toggled). |
| EngagementService.reactionsFor returns isPraising | integration | Insert a `'praising'` row; assert `PerPostReaction.isPraising==true` in the response. |

**Expected state after completion:**
- [ ] `ToggleReactionResponse` includes `praisingCount`.
- [ ] `PerPostReaction` includes `isPraising`.
- [ ] `PostDto` includes `praisingCount`.
- [ ] `PostMapper` populates `praisingCount` in every constructed DTO.

---

### Step 5: Update OpenAPI spec — extend enum, add fields

**Objective:** Document the new `'praising'` reactionType value and the new `praisingCount` field per Universal Rule 4.

**Files to modify:**
- `backend/src/main/resources/openapi.yaml`

**Details:**

| Line approx | Current | Change |
|---|---|---|
| 2223 | `summary: Toggle a reaction (praying or candle) on a post (Spec 3.7)` | Update to `Toggle a reaction (praying, candle, or praising) on a post (Spec 3.7 + 6.6)` |
| 2321 | `enum: [praying, candle]` (DELETE query param) | `enum: [praying, candle, praising]` |
| 4236–4237 | `prayingCount: { type: integer, minimum: 0 }` / `candleCount: ...` | Add `praisingCount: { type: integer, minimum: 0 }` immediately after, with `praisingCount` listed in the `required` array (if `required` includes the counters) |
| 4685 | `enum: [praying, candle]` (request body) | `enum: [praying, candle, praising]` |
| 4690+ | `ToggleReactionResponse` schema | Add `praisingCount: { type: integer, minimum: 0 }` adjacent to existing `prayingCount`/`candleCount` |
| `PerPostReaction` schema (~line 4665) | `isPraying`, `isCandle`, `isBookmarked` properties | Add `isPraising: { type: boolean }` between `isCandle` and `isBookmarked` |

Also update the description on the relevant ToggleReaction summary text at line 2298–2305 mentioning `praying`/`candle` to mention `praising`.

**Guardrails (DO NOT):**
- DO NOT create a new openapi file (Rule 4 — `backend/api/openapi.yaml` was provisional in older master plan versions; current authoritative location is `backend/src/main/resources/openapi.yaml`).
- DO NOT add `celebrate` to any enum (MPD-1).

**Verification:**
- [ ] OpenAPI lints clean: `npx @redocly/cli lint backend/src/main/resources/openapi.yaml` returns zero errors.
- [ ] If a frontend type-generation pipeline is wired (Universal Rule 4 — `openapi-typescript` to `frontend/src/types/api/generated.ts`), regenerate. If not yet wired, hand-update the frontend types in Step 7.

**Expected state after completion:**
- [ ] OpenAPI documents `'praising'` in both enum sites + `praisingCount` in three schemas + `isPraising` in `PerPostReaction`.

---

### Step 6: Backend tests — migration, regression, `'praising'` reaction, counter denormalization

**Objective:** Cover all backend behavior changes per Universal Rule 6 and the spec's ~17-test minimum (overshoot justified — see Edge Cases table).

**Files to create/modify:**
- `backend/src/test/java/com/worshiproom/post/engagement/ReactionWriteServiceTest.java` — extend
- `backend/src/test/java/com/worshiproom/post/engagement/ReactionWriteIntegrationTest.java` — extend
- `backend/src/test/java/com/worshiproom/post/engagement/EngagementServiceTest.java` — extend
- `backend/src/test/java/com/worshiproom/post/engagement/EngagementSchemaInvariantTest.java` — extend
- (Optional new) `backend/src/test/java/com/worshiproom/db/PostReactionsCheckConstraintTest.java`

**Test specifications:**

| Test | Type | Description |
|---|---|---|
| **Migration / schema** (4 tests) | | |
| `'praising'` value passes CHECK | repository integration (`AbstractDataJpaTest`) | After migration, `INSERT INTO post_reactions (..., reaction_type) VALUES (..., 'praising')` succeeds. |
| `'praying'` value still passes CHECK | integration | Regression — old value works. W3. |
| `'candle'` value still passes CHECK | integration | Regression — old value works. W3. |
| `'xyz'` value still fails CHECK | integration | Invalid value rejected at DB layer. |
| `posts.praising_count` column exists | schema invariant | Extend `EngagementSchemaInvariantTest`. Assert column metadata: `data_type=integer`, `is_nullable=NO`, `column_default=0`. |
| **`'praising'` reaction write paths** (5 tests) | | |
| `toggle('praising')` ADD inserts row, sets `state='added'`, returns 201 | service + controller integration | Existing toggle pattern. |
| `toggle('praising')` REMOVE deletes row, sets `state='removed'`, returns 200 | service integration | Existing toggle pattern. |
| `removeReaction('praising')` DELETE returns 204, idempotent | controller integration | Mirrors existing `DELETE /api/v1/posts/{id}/reactions?reactionType=...` semantics. |
| Composite PK prevents duplicate `'praising'` row | repository integration | Re-toggle ADD on a post the user has already praised flips to REMOVE — preserves composite-PK uniqueness. |
| `toggle('praising')` on non-answered post succeeds (R7, DB-permissive) | service integration | Per D-PraisingScope. No post-state validation. |
| **Counter denormalization** (3 tests) | | |
| `incrementPraisingCount` increases `posts.praising_count` by 1 | repository integration | Direct repo call; verify SQL-side UPDATE. |
| `decrementPraisingCount` floors at 0 (`> 0` guard) | repository integration | Race-prevention pattern. |
| `toggle('praising')` returns all three counts in response | controller integration | Asserts `prayingCount`, `candleCount`, `praisingCount` all present and reflect post-mutation truth. |
| **EngagementService reads-side** (2 tests) | | |
| `reactionsFor(viewer)` includes `isPraising=true` for posts viewer praised | service integration | New behavior. Seed `'praising'` row; assert read map. |
| `reactionsFor(viewer)` omits unrelated viewers' `'praising'` reactions | service integration | Privacy — only viewer's own reactions. |
| **Activity engine boundary** (2 tests per Plan-Time Divergence #4) | | |
| `toggle('praising')` ADD does NOT fire `INTERCESSION` activity | service test with mocked `ActivityService` | Per Plan-Time Divergence #4 default. **Awaiting Eric sign-off.** |
| `toggle('candle')` ADD does NOT fire `INTERCESSION` activity | service test with mocked `ActivityService` | Regression — confirm current behavior. **Coordinate with Eric.** |
| **Sort=answered feed regression** (2 tests — light, the endpoint is unchanged) | | |
| `GET /api/v1/posts?sort=answered` returns only `is_answered=TRUE` posts | controller integration | Regression — confirms 6.6 didn't break existing behavior. |
| `GET /api/v1/posts?sort=answered` sorts by `answered_at DESC` | controller integration | Regression. |

**Guardrails (DO NOT):**
- DO NOT use H2 (06-testing.md — Testcontainers PostgreSQL only).
- DO NOT mock the entire ReactionRepository — use real repo via `AbstractIntegrationTest`.
- DO NOT create per-class Testcontainers — extend `AbstractIntegrationTest` / `AbstractDataJpaTest` (singleton).

**Verification:**
- [ ] `./mvnw test` — all tests PASS.
- [ ] `./mvnw test` total wall-clock: regression check that the suite did not grow beyond +60s (suite was ~110s at Phase 2 baseline; this spec adds ~20 tests).

**Expected state after completion:**
- [ ] ~18 new backend test cases covering migration, validation, write paths, counters, reads, activity boundary, and existing-feed regression.
- [ ] `./mvnw test` BUILD SUCCESS.

---

### Step 7: Frontend types — extend `ReactionTypeApi`, `PerPostReactionApi`, `PrayerReaction`, `PrayerRequest`

**Objective:** Surface `'praising'` and `praisingCount` / `isPraising` in the frontend TypeScript types per W10 (every enumeration site).

**Files to modify:**
- `frontend/src/types/api/prayer-wall.ts`
- `frontend/src/types/prayer-wall.ts`

**Details:**

In `types/api/prayer-wall.ts`:
- Line 42: `export type ReactionTypeApi = 'praying' | 'candle' | 'praising'`.
- Line 99–100 (`PerPostReactionApi`): add `praisingCount: number` between `candleCount` and the next field. Also add `isPraising: boolean` adjacent to `isPraying`/`isCandle` if present.
- Line 178–181 (`ToggleReactionResponse` shape): add `praisingCount: number`.

In `types/prayer-wall.ts`:
- Line 58–66 area (`PrayerRequest`): add `praisingCount?: number` adjacent to existing `prayingCount` and `candleCount?`. Use optional `?` for back-compat with old-shape data (mirrors `candleCount?` pattern).
- Line 109–113 (`PrayerReaction`): add `isPraising: boolean` after `isCandle`.

**Guardrails (DO NOT):**
- DO NOT change `ReactionTypeApi` from a string-union to an enum.

**Verification:**
- [ ] `pnpm typecheck` — no errors. All consumers of `ReactionTypeApi` automatically accept `'praising'`.
- [ ] `pnpm build` — succeeds.

**Test specifications:**
| Test | Type | Description |
|---|---|---|
| typecheck | type-only | Compilation is the test. No runtime test needed for type widening. |

**Expected state after completion:**
- [ ] All four enumeration sites in types files include `'praising'` / `praisingCount` / `isPraising`.

---

### Step 8: Extend `reactionsStore.ts` with `togglePraising` + default-fill migration

**Objective:** Add `'praising'` toggle, store-side migration for old-shape data, and pendingMutations key family. Mirror Spec 3.7's `isCandle` migration pattern exactly.

**Files to modify:**
- `frontend/src/lib/prayer-wall/reactionsStore.ts`
- `frontend/src/hooks/usePrayerReactions.ts`

**Details:**

In `reactionsStore.ts`:
- Line 22–23 (pendingMutations comment): extend the `concern` union to mention `'praising'`.
- Line 37–55 (the loosened guard for default-fill): match `isCandle`'s default-fill pattern. Treat `isPraising` as optional during the shape transition; missing field default-fills to `false`. Old-shape data without `isPraising` passes the guard.
- Line 71–74 (`hydrate` default-fill loop): extend to also default-fill `isPraising`. Mirrors lines 71–74 verbatim:
  ```typescript
  const v2 = value as PrayerReaction & { isPraising?: boolean }
  if (typeof v2.isPraising !== 'boolean') {
      result[key] = { ...v2, isPraising: false, ...(typeof v.isCandle !== 'boolean' && { isCandle: false }) }
  }
  ```
  (Coordinate both default-fills into a single pass.)
- Lines 136–155, 153, 164–172, 191–193, 307, 322–325 (toggle paths for praying / candle): extend with parallel `togglePraising` handling. New constants:
  ```typescript
  // After line 522:
  /** Toggles isPraising for prayerId. Returns the PREVIOUS isPraising value. Spec 6.6. */
  export function togglePraising(prayerId: string): boolean { ... }
  ```
  Mirror line 522–548 verbatim, substituting `'praising'` for `'candle'` and `isPraising` for `isCandle`. Use `${prayerId}:praising` as the pendingMutations key.
- All store helpers that construct `PrayerReaction` objects (lines 136–155, 164–172, 191–193, 307, 322–325) must explicitly set `isPraising: partial.isPraising ?? current?.isPraising ?? false`. Audit each construction site.

In `usePrayerReactions.ts`:
- Extend the hook return type to include `togglePraising: (prayerId: string) => boolean`.
- Re-export `togglePraising` from the store at the top of the hook file.
- Return `togglePraising` from the hook at line 56.

**Guardrails (DO NOT):**
- DO NOT introduce a `bb*-v1:` version key — additive migration with safe `false` default mirrors Spec 3.7's pattern (no version key required).
- DO NOT change the storage key (`wr_prayer_reactions` per 11-local-storage-keys.md). Just the shape.
- DO NOT bypass the subscription mechanism in any test that mocks the store (BB-45 anti-pattern, Rule 8).

**Verification:**
- [ ] `pnpm typecheck` — no errors.
- [ ] `pnpm test -- --run reactionsStore` PASSES with new test coverage.

**Test specifications:**
| Test | Type | Description |
|---|---|---|
| `togglePraising` flips `isPraising` and calls backend `toggleReaction('praising')` | unit | Mirror existing `togglePraying`/`toggleCandle` tests. |
| Default-fill migration: old-shape data missing `isPraising` reads as `isPraising=false` | unit | Hydrate test — store JSON with no `isPraising` field, then read; expect default. |
| Default-fill writes back the upgraded shape | unit | After hydrate-and-read, the stored JSON now contains the explicit `isPraising: false`. |

**Expected state after completion:**
- [ ] `togglePraising` is exported from `reactionsStore.ts` and `usePrayerReactions.ts`.
- [ ] Old-shape stored reactions auto-upgrade with `isPraising: false`.
- [ ] All accumulator construction sites populate `isPraising`.

---

### Step 9: Update `prayer-wall-api.ts` response handling

**Objective:** Surface `praisingCount` in the toggle-reaction response. The function signatures need no changes (`ReactionTypeApi` widening handled in Step 7).

**Files to modify:**
- `frontend/src/services/api/prayer-wall-api.ts`

**Details:**

- Line 472–489 (`toggleReaction` response shape destructure): add `praisingCount: number` to the return type and the constructed response object. The HTTP shape change is handled by Step 4 (backend DTO) + Step 5 (OpenAPI).
- Line 495–500 (`removeReaction`): no return body — DELETE returns 204 No Content. No change needed.

**Guardrails (DO NOT):**
- DO NOT change `ReactionTypeApi` here — already extended in Step 7.

**Verification:**
- [ ] `pnpm typecheck` — no errors.
- [ ] `pnpm test -- --run prayer-wall-api` PASSES (if tests exist; otherwise integration coverage via Step 12 page tests).

**Expected state after completion:**
- [ ] `toggleReaction` returns `{ state, reactionType, prayingCount, candleCount, praisingCount }`.

---

### Step 10: Extend `PrayerCard` with `answeredVariant` prop + AnsweredCard wrapper

**Objective:** Implement the non-forking extension mechanism (D-CardExtension, R8) via prop variant. Reuse PrayerCard's internals.

**Files to modify:**
- `frontend/src/components/prayer-wall/PrayerCard.tsx`

**Files to create:**
- `frontend/src/components/prayer-wall/AnsweredCard.tsx`
- `frontend/src/components/prayer-wall/__tests__/AnsweredCard.test.tsx`

**Details:**

In `PrayerCard.tsx`:
- Add `answeredVariant?: boolean` to the `PrayerCardProps` interface at line 58–67.
- Default `answeredVariant = false` in the destructure at line 69.
- At line 269–274 (`prayer.isAnswered && <AnsweredBadge ... />`): branch on `answeredVariant`. When `answeredVariant === true` AND `prayer.isAnswered === true`, render a prominent `<section aria-label={ANSWER_TEXT_REGION_LABEL}>` containing:
  - Eyebrow label (uppercase tracked text per FrostedCard Tier 1 conventions): "HOW THIS WAS ANSWERED" (per D-Copy, constants file).
  - Answer text rendered with `whitespace-pre-wrap` and Tier 1 reading typography (`text-white leading-[1.75] text-[17px] sm:text-lg`).
  - Relative timestamp "answered 3 days ago" computed via `timeAgo(prayer.answeredAt)` from `frontend/src/lib/time.ts`. Lowercase prefix per D-Copy.
- When `answeredVariant === false` (default), preserve the existing AnsweredBadge rendering (W19 — answered prayers on non-Answered-Wall surfaces still show the small inline pill).
- IntercessorTimeline at line 254–265 stays untouched (W17 — comes along via the PrayerCard extension).
- AnsweredBadge import stays for the non-variant path (no fork; no orphan).

In `AnsweredCard.tsx` (NEW):

```tsx
import { PrayerCard } from './PrayerCard'
import type { PrayerRequest } from '@/types/prayer-wall'
import type { PrayerCategory } from '@/constants/prayer-categories'
import type { ReactNode } from 'react'

interface AnsweredCardProps {
  prayer: PrayerRequest
  showFull?: boolean
  onCategoryClick?: (category: PrayerCategory) => void
  children?: ReactNode
  index?: number
  tier?: 'feed' | 'detail'
}

/**
 * Spec 6.6 — Answered Wall card variant. Thin wrapper that sets
 * `answeredVariant` on PrayerCard so the "How this was answered" region
 * replaces the inline AnsweredBadge. Caller supplies its own InteractionBar
 * (with `showPraising={true}`) via children.
 */
export function AnsweredCard(props: AnsweredCardProps) {
  return <PrayerCard {...props} answeredVariant />
}
```

This is the non-fork pattern (Gate-G-CARD-NO-FORK). AnsweredCard adds no internals of its own — it is purely a prop-wrapper.

**Guardrails (DO NOT):**
- DO NOT duplicate any PrayerCard JSX in AnsweredCard (Gate-G-CARD-NO-FORK).
- DO NOT remove the existing AnsweredBadge from PrayerCard (it still renders on `answeredVariant=false` surfaces).
- DO NOT replace `<p>` with `dangerouslySetInnerHTML` (Rule 14).

**Verification:**
- [ ] `pnpm build` — succeeds.
- [ ] `pnpm test -- --run AnsweredCard` PASSES.
- [ ] Visual verification at 375px, 768px, 1440px (per Step 15 Playwright).

**Test specifications:**
| Test | Type | Description |
|---|---|---|
| AnsweredCard renders answer text in prominent region | component | Mount with `answeredVariant`-eligible prayer; assert "HOW THIS WAS ANSWERED" eyebrow + answer text visible. |
| AnsweredCard shows original prayer content (context) | component | The original `content` field still renders above the answer region. W19. |
| AnsweredCard shows relative timestamp "answered N days ago" | component | Use a `answeredAt` 3 days in the past; assert "answered 3 days ago" rendered. |
| AnsweredCard inherits IntercessorTimeline | component | If `prayer.postType === 'prayer_request'`, IntercessorTimeline still renders. W17. |
| AnsweredCard for anonymous prayer shows anonymous attribution | component | `prayer.isAnonymous=true` → "Anonymous" + no userId link. Inherited from PrayerCard. |
| PrayerCard with `answeredVariant=false` (default) renders inline AnsweredBadge | component | Regression — non-Answered-Wall surfaces still show the small pill. W19. |

**Expected state after completion:**
- [ ] AnsweredCard is a 5-line wrapper around PrayerCard.
- [ ] PrayerCard handles the answered-variant rendering internally.
- [ ] IntercessorTimeline + 6.1 receipt + all other PrayerCard internals come along for free.

---

### Step 11: Extend `InteractionBar` with `showPraising` prop

**Objective:** Add the "Praising with you" reaction button to InteractionBar, controlled by the AnsweredCard wrapper. Reuse the existing reaction infrastructure via `togglePraising` (Step 8).

**Files to modify:**
- `frontend/src/components/prayer-wall/InteractionBar.tsx`

**Details:**

- Add `showPraising?: boolean` to `InteractionBarProps` at line 40–49. Default `false`.
- Add `onTogglePraising?: () => void` to the props (caller wires it to `togglePraising(prayer.id)` from the store).
- Compute `isPraising = reactions?.isPraising ?? false` after line 74.
- After the existing pray button block (line 168–207), insert a conditional "Praising with you" button when `showPraising === true`:
  ```tsx
  {showPraising && (
    <button
      type="button"
      onClick={() => {
        onTogglePraising?.()
        triggerPulse?.()
        playSoundEffect('sparkle')  // celebration sound — 'sparkle' from useSoundEffects
      }}
      className={cn(
        btnBase,
        isPraising ? 'font-medium text-violet-300' : 'text-white/50 hover:text-violet-300',
      )}
      aria-label={isPraising
        ? PRAISING_ACTIVE_ARIA_LABEL  // "You're praising with this answered prayer. Tap to remove."
        : PRAISING_INACTIVE_ARIA_LABEL  // "Praising with you" (constant from answered-wall-copy.ts)
      }
      aria-pressed={isPraising}
    >
      <Sparkles className="h-4 w-4" aria-hidden="true" />
      <span className="hidden sm:inline">
        {PRAISING_LABEL} ({prayer.praisingCount ?? 0})
      </span>
    </button>
  )}
  ```
  Import `Sparkles` from `lucide-react` at the top of the file.
- Import the copy constants from `frontend/src/constants/answered-wall-copy.ts` (created in Step 12).

**Guardrails (DO NOT):**
- DO NOT auto-detect `showPraising` from `prayer.isAnswered` — explicit prop control per advisor recommendation. Keeps the rendering surface in caller control.
- DO NOT create a parallel reaction code path (Gate-G-REACTION-REUSE) — the button calls `togglePraising` from the same reactionsStore.
- DO NOT remove the existing pray button — on an AnsweredCard, the prayer is still pray-able (someone may want to keep praying for related concerns).

**Verification:**
- [ ] `pnpm typecheck` — no errors.
- [ ] `pnpm test -- --run InteractionBar` PASSES with showPraising coverage.

**Test specifications:**
| Test | Type | Description |
|---|---|---|
| InteractionBar with `showPraising=true` renders "Praising with you" button | component | Button visible, has `aria-label` + `aria-pressed=false` initially. |
| Tapping "Praising with you" toggles `isPraising` state | component | Mock `onTogglePraising`; assert it's called; assert `aria-pressed` flips after store update. |
| InteractionBar with `showPraising=false` (default) does NOT render praising button | component | Default behavior. Regression for the main feed. |

**Expected state after completion:**
- [ ] InteractionBar accepts `showPraising` + `onTogglePraising` props.
- [ ] The new button reuses `togglePraising` from reactionsStore.

---

### Step 12: Create `answered-wall-copy.ts` constants module

**Objective:** All Answered Wall copy strings live in one i18n-ready module per Universal Rule 5.

**Files to create:**
- `frontend/src/constants/answered-wall-copy.ts`

**Details:**

```typescript
/**
 * Spec 6.6 — Answered Wall copy deck. All strings authored in spec §7 D-Copy.
 * Eric-approved before execute per Gate-G-COPY.
 */

export const ANSWERED_WALL_HEADING = 'Answered' as const
export const ANSWERED_WALL_SUBHEAD = 'Prayers the community has watched God move in.' as const
export const ANSWERED_WALL_EMPTY_STATE =
  'No answered prayers yet. When someone marks a prayer answered, their testimony will live here.' as const

export const PRAISING_LABEL = 'Praising with you' as const
export const PRAISING_INACTIVE_ARIA_LABEL = 'Praising with you' as const
export const PRAISING_ACTIVE_ARIA_LABEL =
  "You're praising with this answered prayer. Tap to remove." as const

export const ANSWER_TEXT_REGION_LABEL = 'How this was answered' as const
export const ANSWERED_TIMESTAMP_PREFIX = 'answered ' as const

export const ANSWERED_NAV_TAB_ALL = 'All' as const
export const ANSWERED_NAV_TAB_ANSWERED = 'Answered' as const
```

**Guardrails (DO NOT):**
- DO NOT inline copy strings into components (Universal Rule 5).
- DO NOT change copy without Eric's re-approval per Gate-G-COPY.

**Verification:**
- [ ] `pnpm typecheck` — no errors.

**Expected state after completion:**
- [ ] All Answered Wall copy is sourced from this module.

---

### Step 13: Create `AnsweredWall` page + route

**Objective:** New page at `/prayer-wall/answered` consuming `GET /api/v1/posts?sort=answered`.

**Files to create:**
- `frontend/src/pages/AnsweredWall.tsx`
- `frontend/src/pages/__tests__/AnsweredWall.test.tsx`

**Files to modify:**
- `frontend/src/App.tsx`

**Details:**

In `AnsweredWall.tsx`:
- Reuse `PageShell` (the post-redesign Prayer Wall page-shell) so layout matches.
- Compose `BackgroundCanvas` per `09-design-system.md` § "BackgroundCanvas Atmospheric Layer" or whatever the current Prayer Wall background is (Spec 14 cinematic-hero rollout applies — verify against the latest pattern on disk).
- Single `<h1>` from `ANSWERED_WALL_HEADING`. Subhead from `ANSWERED_WALL_SUBHEAD`.
- Tab strip at the top (Step 14) — render via shared component or inline.
- Feed: call existing prayer-wall-api with `sort='answered'` query param. Render `<AnsweredCard prayer={p}>` per item, supplying a customized `<InteractionBar showPraising={true} onTogglePraising={...} ...>` as children.
- Empty state when feed length === 0: `<FeatureEmptyState heading={...} description={ANSWERED_WALL_EMPTY_STATE} icon={Sparkles} />` per the canonical empty-state primitive.
- Loading state: `PrayerWallSkeleton`.
- Error state: `RouteErrorBoundary` wraps at the route level (App.tsx).

In `App.tsx` (line 303–307 area):
```tsx
<Route path="/prayer-wall/answered" element={
  <RouteErrorBoundary>
    <Suspense fallback={<PrayerWallSkeleton />}>
      <AnsweredWall />
    </Suspense>
  </RouteErrorBoundary>
} />
```
Lazy-import `AnsweredWall` (mirrors the `PrayerWall` lazy-import pattern).

**Guardrails (DO NOT):**
- DO NOT add page-level counts, tallies, or any "N answered prayers" displays (Gate-G-NO-METRICS, W20–W22).
- DO NOT introduce a sort-order toggle. Only `answered_at DESC`.
- DO NOT auto-fetch crisis-flagged answered prayers separately — the existing visibility filter handles moderation.

**Verification:**
- [ ] `pnpm build` — succeeds.
- [ ] `pnpm test -- --run AnsweredWall` PASSES.
- [ ] Visual verification at 375 / 768 / 1440 (Step 15).

**Test specifications:**
| Test | Type | Description |
|---|---|---|
| AnsweredWall renders heading + subhead | component | `getByRole('heading', { level: 1, name: 'Answered' })`. Subhead present. |
| AnsweredWall renders AnsweredCard for each answered post in fixture | component | Two fixture posts → two AnsweredCard instances rendered. |
| AnsweredWall renders empty state when feed empty | component | Empty fixture → `FeatureEmptyState` with the canonical copy. |
| AnsweredWall calls feed API with `sort='answered'` | component | Mock `prayer-wall-api`; assert the call shape. |
| AnsweredWall has single `<h1>` | a11y | DOM has exactly one `<h1>` element. |

**Expected state after completion:**
- [ ] `/prayer-wall/answered` route works.
- [ ] Page renders heading, subhead, feed of AnsweredCard, or empty state.

---

### Step 14: Add `All` / `Answered` tab strip to PrayerWall + AnsweredWall

**Objective:** Cross-navigation tab strip per Plan-Time Divergence #2. Mobile-safe placement at top of main column. Reuses canonical pill+halo tab-bar pattern.

**Files to create:**
- `frontend/src/components/prayer-wall/PrayerWallViewTabs.tsx` — NEW shared component used by both pages

**Files to modify:**
- `frontend/src/pages/PrayerWall.tsx`
- `frontend/src/pages/AnsweredWall.tsx`

**Details:**

In `PrayerWallViewTabs.tsx`:
```tsx
import { Link, useLocation } from 'react-router-dom'
import { cn } from '@/lib/utils'
import { ANSWERED_NAV_TAB_ALL, ANSWERED_NAV_TAB_ANSWERED } from '@/constants/answered-wall-copy'

const TABS = [
  { label: ANSWERED_NAV_TAB_ALL, to: '/prayer-wall' },
  { label: ANSWERED_NAV_TAB_ANSWERED, to: '/prayer-wall/answered' },
] as const

export function PrayerWallViewTabs() {
  const location = useLocation()
  return (
    <nav
      aria-label="Prayer Wall views"
      className="flex w-full rounded-full border border-white/[0.08] bg-white/[0.07] p-1 backdrop-blur-md"
    >
      {TABS.map(({ label, to }) => {
        const active = location.pathname === to
        return (
          <Link
            key={to}
            to={to}
            aria-current={active ? 'page' : undefined}
            className={cn(
              'flex-1 rounded-full px-4 py-2 text-center text-sm font-medium min-h-[44px] flex items-center justify-center transition-colors',
              'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white/50',
              active
                ? 'bg-violet-500/[0.13] border border-violet-400/45 text-white shadow-[0_0_20px_rgba(139,92,246,0.18)]'
                : 'text-white/60 hover:text-white',
            )}
          >
            {label}
          </Link>
        )
      })}
    </nav>
  )
}
```
This matches the canonical pill+halo tab-bar pattern from `09-design-system.md` § "Active-State and Selection Patterns".

Mount `PrayerWallViewTabs` at the top of the main column on BOTH PrayerWall.tsx and AnsweredWall.tsx, above any filter chips/category bar but below the page hero.

**Guardrails (DO NOT):**
- DO NOT use `aria-pressed` (this is route navigation, not a toggle — use `aria-current="page"` per WAI-ARIA).
- DO NOT add to PrayerWallLeftSidebar — the sidebar is `xl:block`-only and would hide on mobile.
- DO NOT use ring-primary on active (deprecated — use `ring-violet-400/60` and violet-tinted bg per Visual Rollout).

**Verification:**
- [ ] `pnpm typecheck` — no errors.
- [ ] `pnpm test -- --run PrayerWallViewTabs` PASSES.

**Test specifications:**
| Test | Type | Description |
|---|---|---|
| PrayerWallViewTabs renders All + Answered links | component | Both links present. |
| Active tab has aria-current="page" | component | On `/prayer-wall/answered`, the "Answered" link has aria-current. |
| Tabs are keyboard navigable | a11y | Tab order reaches both links; Enter activates navigation. |

**Expected state after completion:**
- [ ] Tab strip works on both pages, both desktop and mobile.
- [ ] Active state correctly reflects the current route.

---

### Step 15: Frontend integration tests — BB-45 cross-mount subscription + InteractionBar `'praising'` wiring

**Objective:** Cover the BB-45 anti-pattern guard (Universal Rule 8) and the end-to-end frontend reaction flow.

**Files to create/modify:**
- `frontend/src/components/prayer-wall/__tests__/AnsweredCard.test.tsx` — extend
- `frontend/src/pages/__tests__/AnsweredWall.test.tsx` — extend
- `frontend/src/hooks/__tests__/usePrayerReactions.test.ts` — extend

**Test specifications:**

| Test | Type | Description |
|---|---|---|
| AnsweredCard re-renders when `'praising'` is added from another mount | BB-45 cross-mount subscription | Mount AnsweredCard A. From a different code path (act() block), call `togglePraising(prayer.id)` on the store directly. Assert AnsweredCard A's `aria-pressed` flips to `true`. **Critical test — proves Pattern A subscription works.** |
| AnsweredCard re-renders when `'praising'` is removed from another mount | BB-45 cross-mount subscription | Mirror above, but starting state is `isPraising=true`. |
| Optimistic update + server reconciliation flow | integration | Tap praising → optimistic flip → mock API returns success with updated count → final state reflects server count. |
| Server failure rollback | integration | Tap praising → optimistic flip → mock API rejects → state rolls back. |

**Guardrails (DO NOT):**
- DO NOT mock the entire reactionsStore (BB-45 anti-pattern). Use the real store, mutate it directly via `togglePraising` or `forceState`.
- DO NOT use `useState` snapshot patterns without subscription in any new consumer (BB-45 anti-pattern).

**Verification:**
- [ ] `pnpm test -- --run AnsweredCard` PASSES all subscription tests.
- [ ] `pnpm test -- --run AnsweredWall` PASSES all integration tests.

**Expected state after completion:**
- [ ] BB-45 anti-pattern guard tests in place.
- [ ] Frontend reaction flow validated end-to-end against the (mocked) backend.

---

### Step 16: Playwright E2E + axe-core a11y scan

**Objective:** End-to-end happy path + zero-violation a11y on populated and empty states (Gate-G-A11Y).

**Files to create:**
- `frontend/e2e/answered-wall.spec.ts`

**Details:**

E2E flow:
1. Navigate to `/prayer-wall`. Confirm the "All / Answered" tab strip is visible.
2. Click "Answered" tab. Assert URL is `/prayer-wall/answered`. Assert `aria-current="page"` on the Answered tab.
3. Assert h1 reads "Answered". Subhead visible.
4. Assert at least one AnsweredCard renders (seeded test data). The card shows the original prayer content + the answer-text region with eyebrow + the relative timestamp.
5. Click "Praising with you" on the first AnsweredCard. Assert `aria-pressed` flips to `true`. Assert per-card count increments.
6. Click again. Assert `aria-pressed` flips back to `false`. Assert count decrements.
7. Click "All" tab. Assert URL is `/prayer-wall`. Assert default feed renders. Verify the main feed default behavior is unchanged (BB).
8. Run axe-core scan on `/prayer-wall/answered` (populated state). Assert zero violations.
9. Seed an empty state (clear answered posts), reload. Assert empty-state copy renders. Run axe-core. Assert zero violations.

**Guardrails (DO NOT):**
- DO NOT add a page-level count assertion (anti-metrics).
- DO NOT skip a11y in either state.

**Verification:**
- [ ] `pnpm test:e2e answered-wall` PASSES.
- [ ] axe-core JSON output saved to `_cutover-evidence/spec-6-6-a11y.json` (optional — not required per Rule 17 since 6.6 is not a cutover spec, but useful evidence to attach to the PR description).

**Expected state after completion:**
- [ ] Full happy-path E2E coverage.
- [ ] a11y zero-violations in both states.

---

## Plan Tightening Audit

After drafting all steps, the plan was audited against 15 lenses. Findings:

1. **Schema state explicit** — **OK.** Database Changes table lists every element with negative/positive verification per Phase 3 Addendum item 8.
2. **Existing entity / class / file reuse** — **OK.** No "create X" where X exists. SortKey.ANSWERED, PostSpecifications.isAnswered, PostMapper, all reused. PrayerCard extended via prop (not forked).
3. **SQL-side counter updates everywhere** — **OK.** Step 2 + Step 3 route all counter mutations through `@Modifying @Query` JPQL UPDATE statements. No load-modify-save.
4. **Activity engine fires on ADD only, not REMOVE / undo** — **OK** for praying (existing behavior preserved). **FLAGGED** for praising (Plan-Time Divergence #4: plan defaults to NO activity event on praising-add, requires Eric sign-off).
5. **SecurityConfig rule ordering** — **OK / N/A.** No new endpoint = no new SecurityConfig rule. Existing rules for `/api/v1/posts/*/reactions` are already explicit-method-rule `.authenticated()` and ordered above permissive defaults. Documented in API Changes section.
6. **Validation surface at controller boundary** — **OK.** Step 3 widens BOTH `ToggleReactionRequest.@Pattern` AND `PostController.removeReaction.@Pattern`. DB CHECK is NEVER the validation surface (would produce a leaky 500).
7. **Pattern A clarification** — **OK.** `usePrayerReactions` (Pattern A via `useSyncExternalStore`) referenced consistently. There is NO "Pattern A migration pattern" anywhere in the plan.
8. **BB-45 cross-mount subscription test** — **OK.** Step 15 includes two cross-mount subscription tests (add + remove from another mount). Critical for proving the new `togglePraising` flow correctly broadcasts.
9. **Step dependency map** — **OK.** See below.
10. **Test count vs brief** — **OK / FLAGGED in Edge Cases.** Brief: ~17. Plan target: ~32. Justified in Edge Cases table; aligned with Phase 3 30-50 range for medium-large specs.
11. **L1-cache trap** — **OK.** ReactionWriteService.toggle at line 117–122 already uses `postRepository.findById(postId)` to fetch a refreshed entity AFTER the SQL-side counter UPDATE (`clearAutomatically=true` already in place). No `entityManager.refresh()` needed because the explicit re-query bypasses the L1 cache. New praising counters ride the same pattern.
12. **`@Modifying` flags complete** — **OK.** Step 2 specifies both `clearAutomatically=true` and `flushAutomatically=true` on both new methods.
13. **Caffeine-bounded caches** — **OK / N/A.** No new caches introduced. Existing `ReactionsRateLimitConfig` is unchanged.
14. **Domain-scoped advice** — **OK / N/A.** No new exception types. `EngagementExceptionHandler` is already package-scoped to engagement.
15. **Crisis content via `CrisisAlertService`** — **OK.** Reactions do not route through CrisisAlertService (D4 from Spec 3.7). 6.6 doesn't change this. Read paths inherit `visibleTo()` moderation filtering. Universal Rule 13 honored by inheritance.

No drift caught during audit. Two items intentionally flagged for Eric sign-off (lens 4 → Plan-Time Divergence #4; lens 10 → test-count overshoot rationale documented).

---

## Plan-Time Divergences from Brief

| # | Decision | Reason | Reversible? |
|---|---|---|---|
| **#1** | Ship `posts.praising_count` denormalized counter column + `incrementPraisingCount`/`decrementPraisingCount` repo methods + `praisingCount` field through `ToggleReactionResponse` / `PostDto` / `PerPostReaction` / `PrayerRequest` / `PerPostReactionApi` / frontend `praisingCount` | Brief W14 flagged this as conditional ("if existing reactions use a per-type denormalized counter"). Recon confirms YES. Pattern parity forces it. Alternative (compute via SELECT COUNT(*)) creates a parallel path that Gate-G-REACTION-REUSE forbids. | Yes (drop column + revert DTO field), but rollback breaks the toggle response shape — requires coordinated frontend rollback. |
| **#2** | Place the "All / Answered" navigation as a top-of-main-column pill+halo tab strip (NEW shared `PrayerWallViewTabs` component) instead of adding an entry to `PrayerWallLeftSidebar` | PrayerWallLeftSidebar renders at `xl:block` only — no mobile fallback. A nav entry there would be invisible to mobile users. Top-of-main-column tab strip is mobile-safe AND reuses the canonical pill+halo pattern from `09-design-system.md` (DailyHub, Music, Local Support, Grow). | Yes (delete component, revert pages). |
| **#3** | Reuse existing `GET /api/v1/posts?sort=answered` instead of any new endpoint or query param | Plan-Time Discovery #1 — backend infra already ships. Brief leaned query-param; recon revealed even simpler answer (already done). | Yes — adding a new endpoint later is additive. |
| **#4** | **`'praising'`-add does NOT fire an `INTERCESSION` activity event** (skip the activity engine call entirely for praising — and arguably for candle as well; see note below) | Brief is silent. D-Scope ("6.6 is a new READ surface") supports skipping. `'praising'` is celebration of someone else's answered prayer — not intercession. Re-using the `INTERCESSION` event type would miscategorize. Note: current `ReactionWriteService.toggle()` at line 109–113 fires INTERCESSION on **every** reaction-add (including candle). Plan recommends gating to `if (PRAYING.equals(reactionType))` — this is a **behavior change for candle reactions as well as praising**, which may be unintended drift from Spec 3.7. **Halt at Step 3 sub-decision for Eric's review.** | Yes (un-gate the activity event). |
| **#5** | Place `praisingCount` between `candleCount` and `bookmarkCount` in `PostDto`; place `isPraising` between `isCandle` and `isBookmarked` in `PerPostReaction` | Keeps reaction-type fields adjacent. Mirrors generated TypeScript ordering. Field order in JPA / Jackson serialization is informational only. | Yes — field order in records is cosmetic. |
| **#6** | Choose `Sparkles` (lucide-react) icon for the "Praising with you" reaction button | Existing reaction icons: HandHelping (praying), Heart (encouragement). `Sparkles` already represents testimony (`PrayerCard.tsx:47`). Semantic fit for celebration-of-answered-prayer. | Yes — swap icon in Step 11. |
| **#7** | Default-fill `isPraising: false` on hydrate in reactionsStore (no version key) | Mirrors Spec 3.7's `isCandle` additive migration. Safe default; no consumer breaks. Spec 3.7 set the precedent. | Yes (add explicit version bump if needed later). |
| **#8** | MPD-1 (from spec body) — narrow to `'praising'` ONLY (NOT `'celebrate'`) | Brief Section 4 directs the plan to honor the brief over the master plan stub. CONTINGENCY: if Eric reaffirms the master plan stub, see "Contingency: if Eric chooses MPD-1 Option B" section below. | Yes — adding `'celebrate'` later is additive. |

**Note on test files for `EngagementSchemaInvariantTest`:** Step 6 extends an existing file. If `EngagementSchemaInvariantTest.java` doesn't have a slot for `posts.praising_count`, Step 6 may create a new schema-invariant test class. Decided at execute time based on the existing file's shape.

### Contingency: if Eric chooses MPD-1 Option B (add `'celebrate'` alongside `'praising'`)

**Delta to plan (additive — does NOT remove existing steps):**

1. Step 1.1 changeset CHECK constraint becomes `('praying', 'candle', 'praising', 'celebrate')`.
2. Step 1.2 adds a second column add: `celebrate_count INT NOT NULL DEFAULT 0` in the same changeset OR in a new -003 changeset.
3. Step 2 adds `celebrateCount` field on Post.java + 2 more PostRepository methods.
4. Step 3 widens BOTH regex patterns to `^(praying|candle|praising|celebrate)$`, adds `CELEBRATE` constant in both services, adds 2 more switch branches.
5. Step 4 adds `celebrateCount` to ToggleReactionResponse + PostDto + PerPostReaction + PostMapper.
6. Step 5 OpenAPI gets `celebrate` in two enum sites + `celebrateCount` in three schemas + `isCelebrate` in PerPostReaction.
7. Step 7 frontend types add `'celebrate'` to ReactionTypeApi + `celebrateCount` to PrayerRequest + `isCelebrate` to PrayerReaction.
8. Step 8 reactionsStore adds `toggleCelebrate` + default-fill `isCelebrate: false`.
9. Step 9 prayer-wall-api response shape adds `celebrateCount`.
10. Step 11 InteractionBar adds a parallel "Celebrate" button when `showCelebrate={true}` (per master plan stub line 5685 — "Light a Candle → Celebrate" swap on answered posts).
11. Step 11 sub-decision per master plan stub: on AnsweredCard, replace the Candle button with the Celebrate button (the master plan's "Light a Candle → Celebrate" semantic swap). This is a significant UI/UX scope addition.
12. New file `frontend/src/components/prayer-wall/CelebrateReaction.tsx` per master plan stub line 5685 (if Eric wants the component decomposition).
13. Tests +6 (regression `'celebrate'` value passes CHECK + write paths + read-side union + counter denormalization + Pattern parity assertions).

**Total contingency scope add:** +1 changeset (or extended existing), +1 column, +2 repo methods, +2 service constants, +2 switch branches each in 2 services, +3 OpenAPI schema fields, +3 TS type extensions, +1 store-side toggle function + default-fill, +1 InteractionBar button conditional, +1 frontend component, +6 tests. **Estimated added effort: ~30%** on top of the base plan.

If Eric chooses Option B, document the choice in the Execution Log and apply the delta to each affected step.

---

## Step Dependency Map

| Step | Depends On | Description |
|---|---|---|
| 1.0 | — | Liquibase filename-collision pre-flight |
| 1.1 | 1.0 | CHECK constraint widening |
| 1.2 | 1.0 | praising_count column add |
| 2 | 1.2 | Post entity + repo methods |
| 3 | 2 | Validator regex + service constants + activity-event gate |
| 4 | 3 | Response DTOs + PostMapper |
| 5 | 4 | OpenAPI spec extension |
| 6 | 5 | Backend tests (depends on all backend code complete) |
| 7 | — | Frontend types (parallelizable with backend — independent file paths) |
| 8 | 7 | reactionsStore + usePrayerReactions extension |
| 9 | 7 | prayer-wall-api response shape |
| 10 | 8, 9 | PrayerCard answeredVariant + AnsweredCard wrapper |
| 11 | 8, 10 | InteractionBar showPraising prop |
| 12 | — | answered-wall-copy.ts constants (parallelizable) |
| 13 | 10, 11, 12 | AnsweredWall page + route |
| 14 | 12, 13 | PrayerWallViewTabs nav + mount on both pages |
| 15 | 13, 14 | Frontend integration tests (BB-45 cross-mount + flow) |
| 16 | 15 | Playwright E2E + axe-core |

Steps that can run in parallel after Step 6: Step 7 + Step 12 + Step 1.0 (already done).

---

## Execution Log

### Pre-execute sign-offs (recorded out-of-band per Eric's chat directives, confirmed at code-review time 2026-05-14)

- **MPD-1 sign-off — ✅ APPROVED (Eric, chat, pre-execute):** Ship `'praising'` only; do NOT add `'celebrate'`. Implementation honors the brief's narrow scope per its Section 4 authority directive. Master plan stub's `'celebrate'` value remains deferred to a future spec when a consumer exists.
- **Plan-Time Divergence #4 sign-off — ✅ APPROVED Option A (Eric, chat, pre-execute):** Skip `INTERCESSION` activity event for BOTH `'praising'`-add AND `'candle'`-add. Reasoning: (a) `INTERCESSION` events are forward-looking-only — no retroactive recompute of past activity_log rows; (b) zero historical blast radius — mock seed has no candle reactions, prod not deployed; (c) corrects suspected Spec 3.7 drift where `INTERCESSION` fired on every reaction-add without semantic justification. Praising is celebration, candle is silent remembrance — neither is intercession. Code comment in `ReactionWriteService.java:119-130` documents the rule for future maintainers.

### Step status

| Step | Title | Status | Completion Date | Notes / Actual Files |
|---|---|---|---|---|
| 1.0 | Liquibase filename-collision pre-flight | ✅ DONE | 2026-05-14 | No collision; `2026-05-14-001` and `2026-05-14-002` were free. |
| 1.1 | CHECK constraint widening (`add-praising-to-post-reactions-check`) | ✅ DONE | 2026-05-14 | `backend/src/main/resources/db/changelog/2026-05-14-001-add-praising-to-post-reactions-check.xml`; `master.xml` include line added. |
| 1.2 | praising_count column add (`add-posts-praising-count-column`) | ✅ DONE | 2026-05-14 | `backend/src/main/resources/db/changelog/2026-05-14-002-add-posts-praising-count-column.xml`; `master.xml` include line added. |
| 2 | Post entity + PostRepository counter methods | ✅ DONE | 2026-05-14 | `Post.java`: `praisingCount` field + getter + setter. `PostRepository.java`: `incrementPraisingCount` / `decrementPraisingCount` with both `@Modifying` flags. |
| 3 | Validation regex + service constants + activity-event gate | ✅ DONE (Divergence #4 Option A applied with Eric sign-off above) | 2026-05-14 | `ToggleReactionRequest.@Pattern` + `PostController.removeReaction.@Pattern` widened. `PRAISING` constants added in both `ReactionWriteService` and `EngagementService`. `INTERCESSION` gated to `if (PRAYING.equals(reactionType))` per Divergence #4 Option A — candle adds no longer fire. |
| 4 | Response DTOs (ToggleReactionResponse, PerPostReaction, PostDto) + PostMapper | ✅ DONE | 2026-05-14 | `praisingCount` added to `ToggleReactionResponse` + `PostDto`. `isPraising` added to `PerPostReaction` between `isCandle` and `isBookmarked` (Divergence #5). All test fixtures constructing `PostDto` updated. |
| 5 | OpenAPI spec extension | ✅ DONE | 2026-05-14 | Both `reactionType` enum sites widened; `praisingCount` added to `PostDto`/`ToggleReactionResponse`; `isPraising` added to `PerPostReaction`. `praisingCount` in PostDto `required` array. |
| 6 | Backend tests | ✅ DONE | 2026-05-14 | `EngagementSchemaInvariantTest` extended with CHECK clause assertion + `praising_count` column assertion. `EngagementServiceTest` +2 tests for praising read-side. `ReactionWriteIntegrationTest` +6 tests covering write paths + activity-engine boundary. `LiquibaseSmokeTest` totals updated (changeset count 30 → 32, posts columns 30 → 31). |
| 7 | Frontend types (ReactionTypeApi, PerPostReactionApi, PrayerReaction, PrayerRequest) | ✅ DONE | 2026-05-14 | `ReactionTypeApi` widened to include `'praising'`. `praisingCount` added to `PostDto`, `PerPostReactionApi`, `ToggleReactionResponse`. `isPraising` added to `PerPostReactionApi` + `PrayerReaction`. `praisingCount?` added to `PrayerRequest` (optional for back-compat parity with `candleCount?`). |
| 8 | reactionsStore.ts + usePrayerReactions extension | ✅ DONE | 2026-05-14 | `togglePraising` exported from store; mirror of `togglePraying` flag-on path with `${prayerId}:praising` pendingMutations key. Default-fill migration extended to default-fill `isPraising: false` on hydrate. `usePrayerReactions` returns `togglePraising`. Two BB-45 cross-mount subscription tests in `usePrayerReactions.subscription.test.tsx`. |
| 9 | prayer-wall-api.ts response shape | ✅ DONE | 2026-05-14 | `toggleReaction` return type extended with `praisingCount`. `ListPostsParams.sort` widened to include `'answered'` for the Answered Wall feed consumer. |
| 10 | PrayerCard answeredVariant prop + AnsweredCard wrapper | ✅ DONE | 2026-05-14 | `PrayerCard` accepts `answeredVariant?: boolean`; default `false` preserves existing inline AnsweredBadge for non-Answered-Wall surfaces (W19). `AnsweredCard.tsx` is a thin prop-wrapper (Gate-G-CARD-NO-FORK). |
| 11 | InteractionBar showPraising prop | ✅ DONE | 2026-05-14 | `showPraising?: boolean` + `onTogglePraising?` props. `Sparkles` icon. `aria-pressed` + dynamic `aria-label`. Auth-gated like other write actions. |
| 12 | answered-wall-copy.ts constants module | ✅ DONE | 2026-05-14 | `frontend/src/constants/answered-wall-copy.ts` with heading, subhead, empty state, praising labels, region label, timestamp prefix, tab labels. |
| 13 | AnsweredWall page + route | ✅ DONE (with code-review fix) | 2026-05-14 | `frontend/src/pages/AnsweredWall.tsx`. Route mounted in `App.tsx` at `/prayer-wall/answered` BEFORE `/prayer-wall/:id`. **Code-review fix:** `CrisisResourcesBanner` mounted inline (Gate-G-CRISIS-RESOURCES-ALWAYS-VISIBLE) via `useWatchMode()`; `id="main-content"` added to `<main>`; main column unified to `max-w-[720px]` to match `PrayerWall.tsx`. |
| 14 | PrayerWallViewTabs nav + mount | ✅ DONE | 2026-05-14 | `frontend/src/components/prayer-wall/PrayerWallViewTabs.tsx`. Mounted at the top of the main column on both `PrayerWall.tsx` and `AnsweredWall.tsx`. `aria-current="page"` for active route. |
| 15 | Frontend tests (BB-45 subscription + integration) | ✅ DONE (with code-review fill-in) | 2026-05-14 | BB-45 cross-mount subscription tests added in `usePrayerReactions.subscription.test.tsx`. **Code-review fill-in:** the three plan-required test files (`AnsweredCard.test.tsx`, `AnsweredWall.test.tsx`, `PrayerWallViewTabs.test.tsx`) were missing from execute and were added during the code-review fix-up pass — see "Documented drift / follow-up" below. |
| 16 | Playwright E2E + axe-core a11y | ⏸ DEFERRED PER ERIC | 2026-05-14 | `frontend/e2e/answered-wall.spec.ts` is checked in but not executed (Eric's "do not start verify-with-playwright" directive). Run later via `pnpm test:e2e answered-wall`. |

### Documented drift / follow-up

- **Cinematic hero pattern not applied to AnsweredWall.** Per Eric's review-time decision, an `AnsweredWallHero.tsx` (matching `PrayerWallHero.tsx` cinematic) is OUT of scope for Spec 6.6. The Prayer Wall Redesign side quest owns Prayer Wall family visual coherence — the cinematic-hero alignment for `/prayer-wall/answered` belongs in that brief, not as an improvised review fix. AnsweredWall ships with the plain `BackgroundCanvas` + bare `<section>` hero. Action: add cinematic-hero alignment to the Prayer Wall Redesign side quest's scope.
- **PostMapper unit test not added.** Integration coverage via `ReactionWriteIntegrationTest.togglePraising_addPath_returns201WithStateAddedAndAllThreeCounts` and `EngagementServiceTest.reactionsFor_includesPraisingReactions_perSpec66` exercises the `PostMapper.buildDto` → `praisingCount` propagation indirectly. A dedicated `PostMapperTest.includesPraisingCount` would be cheap but redundant; skipped during code-review fix-up.
